[package]
name = "provider-compat-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
serde_json = "1"
