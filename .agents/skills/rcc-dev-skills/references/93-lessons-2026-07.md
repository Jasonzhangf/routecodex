# 2026-07 lessons

## L93-140 Debug stage records are still Rust effect contracts

- Trigger: TS records provider-response debug stages and locally chooses stage IDs, protocol labels, passthrough flags, or payload envelopes.
- Action: Rust emits a closed stage-recorder effect plan containing ordered `{stage, payload}` entries. TS may skip recorder IO when no recorder exists and may call `recorder.record`, but it must not synthesize stage names, parse string payloads, or choose debug protocol/envelope fields.
- Boundary: snapshot/debug observations may remain internal and non-user-visible, but they still describe lifecycle truth. Recorder IO failures must propagate fail-fast; every serializable record field remains Rust-owned.
- Evidence target: body and stream positives, malformed input and recorder-IO negatives, TS stage-name/envelope/normalizer revival residue gate, focused Rust/Jest, native export/base gates, and live replay leakage scan.

## L93-139 Snapshot runtime metadata InvalidData is marker integrity, not VR

- Trigger: log shows normal `[virtual-router-hit]` followed by `[hub_snapshot_hooks] runtime metadata write skipped ... kind=InvalidData`.
- Rule: first inspect `snapshot.stage_contract`: current `__runtime.json` parseability, TS `ensureSnapshotRuntimeMarker` atomic publication, and Rust `hub_snapshot_hooks.rs::upsert_runtime_metadata_file`. Treat broad log counts across requests/ports as marker race evidence, not route-selection evidence.
- Anti-pattern: debugging VR/longcontext/SSE because the log is adjacent to `virtual-router-hit`, or accepting `write skipped` as a harmless debug warning. Runtime metadata must either publish atomically or repair invalid existing metadata with an explicit diagnostic.
- Evidence target: Rust `runtime_metadata` tests, snapshot marker Jest, root `tsc`, native hotpath build, touched-file diff check, and live installed replay only when global install/restart is part of the current scope.

## L93-138 Responses direct model compat must be bounded and fail-fast

- Trigger: router-direct/provider-direct restores client-visible model for non-ChatGPT `/v1/responses` providers, or dry-run replays provider-response SSE snapshots.
- Rule: compat may rewrite only protocol-visible response model fields, such as top-level JSON `model` and SSE/JSON `response.model`. Nested diagnostic/tool/metadata `model` fields are provider payload truth and must not be recursively rewritten. Unknown `sseStream` values must throw; offline response dry-run must reject serialized live readable objects unless materialized `bodyText/raw/text/sseBodyText` is present.
- Anti-pattern: recursive `model` rewrite across all response objects, empty-stream materialization for unknown stream-like values, or letting serialized Readable snapshots fall through to generic body/data errors.
- Evidence target: router-direct whitebox covers allowed model restore, nested model preservation, and non-readable SSE fail-fast; response dry-run temp sample with serialized `_readableState` fails with the explicit unreplayable-stream message; `test:pipeline-dry-run` stays green.

## L93-137 Stopless guidance is schema/help text, not task-state judgment

- Trigger: stopless / reasoningStop guidance, CLI summary, or continuation prompt tells the model the task is done, ready to close, must stop, should not continue, or otherwise decides terminal state for the model.
- Rule: keep production prompt text neutral. First no-schema continuation is exactly `继续。`; invalid-schema continuation is feedback repair such as `继续；按上一轮反馈修正。`; non-terminal CLI summary is `继续`; `stopreason=2` next-turn prompt is the model-provided `next_step` itself. Schema guidance may state field requiredness and numeric branch meanings only.
- Anti-pattern: hardcoded "suggested next step" prose, final-round wrap-up instructions, summary text that claims progress state, or any handler/SSE/outbound patch that tries to repair stopless behavior outside Rust Chat Process/servertool owners.
- Evidence target: Rust stopless/cli_contract tests, router-hotpath resume-guidance tests, stopless invalid-schema and contract blackboxes, `verify:servertool-rust-only`, global install/restart, installed `routecodex hook run reasoningStop` outputs, and runtime `dist` grep showing no terminal-state judgment wording.

## L93-136 Deleted source JS bridge shells must not be patched back for Jest

- Trigger: a Jest source import fails because tracked `src/**/native-exports.js` or another legacy JS bridge shell lacks an export that TS/dist already has.
- Rule: do not patch the legacy `.js` shell back. Treat this as a rustification/source-surface violation: either move the test/import to the Rust/TS canonical owner, regenerate through the approved build surface, or continue physical deletion of the stale JS shell. The runtime truth remains Rust/NAPI plus minimal TS glue, not source JS compatibility repair.
- Anti-pattern: copying a wrapper from `dist` into `src/**/*.js` to make one Jest suite pass, or treating generated/legacy JS as an acceptable second owner.
- Evidence target: source JS diff stays empty; focused failure is reported as a separate source-surface cleanup item unless the current task explicitly owns deleting/migrating that shell.

## L93-135 Relay Responses SSE completed-without-DONE is transport sentinel closeout only

- Trigger: relay `/v1/responses` SSE delivers terminal `response.completed` / native terminal evidence, then upstream ends without `data: [DONE]`; Codex or tool-loop client appears stuck and may not execute the returned tool call.
- Rule: server SSE transport may append only the protocol close sentinel `data: [DONE]` after Rust/native transport terminal evidence is observed and no `[DONE]` was seen. This applies to relay Responses streams only; direct passthrough must remain untouched. The handler must not synthesize `response.completed`, `response.done`, `required_action`, tool calls, continuation state, or stopless/servertool guidance.
- Anti-pattern: parsing terminal event names locally in `handler-response-sse.ts`, treating `[DONE]` insertion as business completion repair, swallowing upstream terminal frames when not adding `[DONE]`, or moving stopless/tool governance into SSE/handler.
- Evidence target: focused SSE handler regression proves completed-without-DONE gains final `[DONE]`; SSE architecture/business gates prove handler remains transport-only; installed runtime replay must show terminal event plus final `[DONE]` and no `event:error`.

## L93-134 Completed-before-close is transport closeout evidence, not SSE handler parsing

- Trigger: `/v1/responses` client closes after it has already received `response.completed`, but server snapshots/logs `client_close_before_terminal`, making a completed turn look like a silent failure.
- Rule: closeout classification may consume Rust native SSE transport evidence through the dedicated `responses-sse-bridge.ts` facade. The handler must not parse terminal frames locally, repair response semantics, synthesize `response.done`, or suppress real pre-terminal client disconnects.
- Anti-pattern: scanning strings in `handler-response-sse.ts`, updating only `.ts` while Jest imports committed `.js`, widening gates without documenting the native facade boundary, or marking all provider-completed/client-disconnected samples as success when the client never received terminal.
- Evidence target: focused SSE finish-reason tests cover completed-after-close, spaced terminal frames, large terminal frames, and chunk-boundary terminal detection; bridge `.ts/.js/.d.ts` stay aligned; `verify:responses-handler-single-bridge-surface`, `verify:responses-sse-business-module`, function-map gate, build/install, managed restart, and live sample prove `client-response.json` contains `response.completed` with no sibling `client-response.error.json`.

## L93-132 Vercel AI SDK transport must share provider timeout truth

- Trigger: 5555/5520 log shows `provider.sse_decode` plus `error_action_backoff_wait delayMs=1000`, but request timing shows minutes under `request.internal` before a transport error such as `ECONNRESET`.
- Rule: distinguish the 1s provider error-action backoff from the real upstream wait. If the selected provider uses `transportBackend = "vercel-ai-sdk"`, inspect `VercelAiSdkOpenAiTransport.executePreparedRequest()` before touching SSE handler, Hub Pipeline, VR, or backoff queue. The SDK prepared path must enforce headers timeout and stream byte-idle timeout, and it must consume the same env/runtime/profile/config override truth as `HttpClient.postStreamOrResponse`.
- Anti-pattern: tightening server/client SSE timeout, adding handler business semantics, treating `error_action_backoff_wait` as the stall, or leaving `overrides.streamIdleTimeoutMs` visible only to `HttpClient` while SDK transport bypasses it.
- Evidence target: red SDK transport tests first hang on never-returning headers and no-byte SSE body, then pass with `UPSTREAM_HEADERS_TIMEOUT` / `UPSTREAM_STREAM_IDLE_TIMEOUT`; service-profile resolver passes stream timeout overrides into the resolved profile; provider failure policy classifies `UPSTREAM_STREAM_IDLE_TIMEOUT` as recoverable; `build:base`, function-map gate, SSE boundary, and fallback denylist pass. Live closeout still requires installed-runtime replay after Jason asks for global install/restart.

## L93-133 Deleted native split helpers must leave zero live imports

- Trigger: `verify:llmswitch-core-tsc` or `build:base` fails with `Cannot find module './native-*-shared.js'` after a zero-ref deletion commit.
- Rule: before committing deletion of a native split/helper facade, run a live-source import scan and `verify:llmswitch-core-tsc`. If imports remain, move only binding/stringify/parse/error glue into an existing aggregate/core owner such as `native-router-hotpath-loader.ts`; do not restore the retired split helper as a new facade.
- Anti-pattern: trusting a previous zero-ref audit after parallel commits, or relying on dist files. Source TS imports are the truth; dist is regenerated by build.
- Evidence target: grep active source for the deleted helper path returns no hits, `verify:llmswitch-core-tsc` passes, and `build:base` passes.

## L93-131 Client tool governance must preserve next-turn feedback

- Trigger: response-side governance blocks/drops a client-visible tool call because it is unrequested, looks like apply_patch fallback, or uses ordinary shell write syntax.
- Rule: only intercept dangerous/destructive commands; otherwise preserve the tool call so client execution or client error becomes the next model-turn feedback.
- Anti-pattern: silently dropping tool_calls and changing `finish_reason=tool_calls` to `stop`, or rewriting non-dangerous shell attempts into `APPLY_PATCH_ERROR`.
- Evidence target: focused `resp_process_stage1_tool_governance` proves shell writes, unrequested structured tools, and apply_patch shell fallback stay client-visible while dangerous commands remain feedback-blocked.

## L93-130 Session color collisions belong to the Rust VR hit-log color owner

- Trigger: different visible sessions or ports show the same log color after sessionId extraction, response recolor, and request/VR/usage alignment have already been fixed.
- Rule: check `vr.hit_log_projection` and `virtual_router_hit_log.rs::resolve_session_color` before touching usage/logger/handler layers. A small fixed ANSI palette can collide even when session identity is correct; the Rust owner should generate deterministic high-cardinality session colors from sessionId.
- Anti-pattern: adding TS-side recolor maps, route-color exceptions, request-id colors, or per-port color hacks. That splits color ownership and hides the real palette collision.
- Evidence target: a red pair of distinct session ids that previously returned the same ANSI; Rust unit proves same-session stability and different-session non-collision; focused VR hit-log Jest proves installed NAPI output; release/global smoke proves 5520/5555 session samples resolve to different truecolor ANSI and hit lines start with their own session color.

## L93-129 Rustification type-shell blockers must be re-proven against current imports

- Trigger: a zero-TS closeout note says a TS type shell cannot be deleted until Rust-generated declarations exist, but current source may have already moved consumers to local `Record<string, unknown>` or adjacent owner-local types.
- Rule: before building declaration infrastructure, run a source/doc-only import proof over tracked source/tests/scripts and open the actual type shell. If it only contains zero-consumer residue, delete it physically, remove it from `minimal-ts-surface.json`, migrate any remaining test-only type imports to the real adjacent owner, and add a residue red test requiring the old file to stay absent.
- Anti-pattern: trusting stale rustification audit text, keeping "reference only" TS files in the manifest, or leaving tests importing a deleted Hub type shell when the type already belongs to `servertool/types.ts` or another adjacent owner.
- Evidence target: red residue test fails while the stale file exists; after deletion, focused residue/provider tests pass, sharedmodule and root `tsc` pass, `verify:llmswitch-minimal-ts-surface` reports one fewer entry/non-native file, `verify:llmswitch-rustification-audit` passes, and function/mainline map gates pass.

## L93-35 Hub response empty-result can be native Error masking

- Trigger: `/v1/responses` fails after provider response with `native executeHubPipelineJson is required but unavailable: ... empty result`, especially on 5555/provider-response paths.
- Rule: first distinguish native non-string/Error returns from actual empty string. Preserve native Error object messages in TS wrappers, then inspect Rust HubPipeline lib initialization. Response-only provider conversion may run with empty `{}` config and must not initialize Virtual Router unless config has real VR runtime shape (`providers` / `routing` / `forwarders`).
- Usage side-rule: router-direct response model may be restored to the client alias; record provider target model immediately after direct-route hooks and render usage as `requestModel->providerModel`.
- Anti-pattern: adding fallback providerProtocol/config, making response conversion require VR config, hiding native Error as `empty result`, or deriving usage provider model from the already client-restored response body.
- Evidence target: Rust `hub_pipeline_lib` tests, native hotpath rebuild, provider-response/API Jest, direct-result/usage logger Jest, global release install, 5555 same-entry live smoke without `executeHubPipelineJson/empty result`, and installed-runtime usage smoke showing `gpt-5.4->gpt-5.5`.

## L93-34 TS owner ban must separate shell classification from semantic migration

- Trigger: `verify:architecture-ts-owner-ban` fails for TS owner modules that function-map already describes as transitional shell, host glue, debug authoring surface, or manager persistence bridge.
- Rule: first read function-map, verification-map, and mainline edges. If the feature is explicitly shell/glue with Rust or runtime semantic owner elsewhere, classify it in the TS owner gate with a reason and keep `migration_target`. If the map claims Rust ownership but points `owner_module` at a TS bridge, move `owner_module` and canonical builders to real Rust symbols instead of whitelisting the bridge.
- Anti-pattern: treating every TS owner-ban hit as immediate runtime migration, or whitelisting a TS bridge for a feature whose `owner_kind` already says Rust Hub Pipeline.
- Evidence target: `verify:architecture-ts-owner-ban`, `verify:architecture-feature-anchor-coverage`, `verify:function-map-canonical-builder-definitions`, `verify:architecture-mainline-call-map`, and full `verify:function-map-compile-gate` all pass.

## L93-33 Responses SSE closeout is `[DONE]`, not synthetic business terminal

- Trigger: `/v1/responses` SSE client waits, repeats requests, or samples end at `response.completed` without final `data: [DONE]`.
- Rule: append the transport sentinel at the owning projection edge: direct provider passthrough for direct streams, Rust `build_responses_sse_stream_frames_json` for relay/JSON-to-SSE projection. Do not synthesize `response.done` or business semantics.
- Relay check: first prove a request actually used relay. Current config can make a suspected relay symptom actually direct when `sameProtocolBehavior` is explicit/default `direct`.
- Anti-pattern: fixing this in `handler-response-sse.ts`, adding fake `response.done`, tightening timeout, or using continuation/history repair to close a transport stream.
- Evidence target: direct live sample has `response.completed` and final `data: [DONE]`; installed native encoder probe returns last frame `data: [DONE]\n\n`; `verify:responses-sse-business-module` and `verify:sse-architecture-boundary` pass.

## L93-32 ErrorErr05 consumers must preserve default-pool non-terminal truth

- Trigger: provider auth/quota/5xx failure is projected to client while `defaultPoolAvailable` / `defaultTierAvailable` is still true, or a consumer recomputes exhaustion from only the optional/current route pool.
- Rule: executor/direct consumers must treat default pool availability as non-terminal. Recompute projection permission from the Rust ErrorErr05 decision plus local args, and only project provider error when the selected route pool is empty and no default pool remains.
- Anti-pattern: trusting a stale `mayProject` boolean, checking only `remainingTargets.empty`, or letting TS retry helpers override Rust route-availability/default-floor truth.
- Evidence target: Rust `error_err05_availability`, focused retry execution-plan/provider-failure Jest, `verify:error-pipeline-contract`, `verify:vr-route-availability-default-floor`, and `verify:provider-failure-ban-blackbox` all pass.

## L93-31 Forbidden-path gate fixes must remove owner-name leaks

- Trigger: `verify:architecture-forbidden-path-growth` reports canonical owner terms such as `MetadataCenter`, `ResponsesSseEvent`, or `ContinuationLookupResult` under provider/runtime/executor/store paths.
- Rule: fix the boundary by moving access through owner-owned helper/projection APIs or renaming local projection shapes away from canonical contract names. The provider/runtime/executor layer may use `request-truth-readers.ts` helpers for runtime carriers and observations; it must not import or directly name the owner class/type.
- Anti-pattern: widening allowlists, keeping owner names in helper aliases, or using forbidden-path text changes that hide a second semantic owner instead of removing the cross-module dependency.
- Evidence target: rerun `verify:architecture-forbidden-path-growth` plus the owner-specific gates it protects, especially metadata-center write/dualwrite, responses history protocol, SSE architecture boundary, error pipeline contract, and focused provider/executor tests.

## L93-30 Provider failure blackbox needs Rust config materialization before VR runtime changes

- Trigger: `verify:provider-failure-ban-blackbox` or startup fails with `Virtual Router requires at least one provider in configuration`.
- Rule: check the Rust `RouteCodexRuntimeManifest.virtualRouterBootstrapInput.providers` produced by `compileRouteCodexRuntimeConfigManifest()` first. Provider v2 file reading remains TS IO, but provider-port bindings, routing-policy-group flattening, forwarder target expansion, and applyPatch normalization belong to Rust runtime config materialization.
- Anti-pattern: debugging VR selection/provider-error policy before proving the Rust runtime manifest contains the expected provider configs and routing artifacts.
- Evidence target: config materialization Jest passes, Rust materialization cargo test passes, root `tsc` passes, native build refreshes binding/dist, then `verify:provider-failure-ban-blackbox` proves 503 and provider auth/quota failures reroute until the optional pool plus default pool are exhausted.

## L93-29 Store lifecycle decisions should return native action plans

- Trigger: TS store/map code still branches on retention or lifecycle semantics while Rust already owns Responses conversation history.
- Rule: move decision branches to Rust as explicit action plans, for example `noop|clear|release` plus reason, and keep TS as IO executor only. Preserve old debug/log shape in TS if needed, but do not keep duplicate TS condition trees.
- Anti-pattern: migrating payload builders but leaving finalize/release/retention policy in TS, or returning booleans that force TS to reconstruct the reason/action branch.
- Evidence target: Rust unit covers each action/reason branch; focused store Jest passes with real native addon; rustification audit decreases non-native LOC; function-map and response history protocol gates pass.

## L93-28 Router-hotpath NAPI export moves require native rebuild before Jest

- Trigger: moving TS semantics into `shared_responses_conversation_utils.rs` or adding any `#[napi(...)]` helper, then focused Jest fails with `[virtual-router-native-hotpath] native <capability> is required but unavailable`.
- Rule: after adding/renaming router-hotpath NAPI exports, update required exports and TS native facade, then run `npm run build:native-hotpath` before Jest that imports sharedmodule `src`. TSC only proves TypeScript facade shape; it does not update the loaded `.node` addon.
- Anti-pattern: reintroducing TS fallback logic to make tests pass, mocking native-required store semantics, or accepting TSC/cargo unit tests as proof that JS runtime can see the new binding.
- Evidence target: native build passes, focused Jest using real native binding passes, `verify:responses-history-protocol-contract` passes, and `verify:llmswitch-rustification-audit` shows the TS residue did not grow.

## L93-27 Forbidden TS semantic paths need residue gates before closeout claims

- Trigger: rustifying a function-map feature where TS files are marked `forbidden_paths` but still exported historically, such as `conversion.shared.anthropic`.
- Rule: collapse TS to a native shell/barrel only after Rust owns the semantic behavior, then add a focused residue gate that rejects the old semantic function names/allowlists/parsers in those forbidden TS files. Update the closeout plan with current audit metrics and keep live replay caveats explicit when samples are missing.
- Anti-pattern: claiming rustification from LOC reduction, leaving old public helper names as dead exports, or relying only on aggregate `verify:llmswitch-rustification-audit` without a per-feature resurrection guard.
- Evidence target: Rust owner tests, focused JS contract tests, required function-map gates, rustification audit metrics, native build/build:base, and a residue audit that fails if the forbidden TS files regain parser/sanitizer/normalizer semantics.

## L93-26 `x-stainless-timeout` is seconds, not milliseconds

- Trigger: `/v1/responses` live log shows `timeoutMs=900000`, repeated new requests every ~2s, growing `rawInputItems`, `usage=unreported`, and `response.sse.client_close ... detectedBeforeStreamStart=true`.
- Rule: treat `x-stainless-timeout` as seconds in `trackClientConnectionState`; `x-request-timeout-ms` remains the millisecond override. The owner is server transport connection-state parsing, not Hub Pipeline, VR, continuation, SSE projection, or provider runtime.
- Anti-pattern: fixing repeated requests in Virtual Router/continuation/store, interpreting `x-stainless-timeout: 900` as `900ms`, or validating only one `rcc` path while another executable/root is still stale.
- Evidence target: red/green timeout unit proves `x-stainless-timeout: 1` is not disconnected at ~320ms and is disconnected after ~1.25s; installed active runtime import proves the same; `which -a rcc`, `/opt/homebrew/bin/rcc --version`, bare `rcc --version`, live `/health.version`, and process command root must all agree before closeout. Live post-install log after the first fixed request must have no new `detectedBeforeStreamStart` and must show multi-second completions instead of 160ms/2s self-retry.

## L93-25 Stopless Responses resume splits control evidence from restored data

- Trigger: invalid-schema stopless blackbox shows next provider turn has corrective guidance, but response-side stopless runtime still starts from `repeatCount=0`, or request-context tests show `.js` bridge behavior that `.ts` source no longer owns.
- Rule: `/v1/responses.submit_tool_outputs` relay resume has two separate facts. Current-turn CLI stdout/repeat evidence belongs only in `responsesResume.toolOutputsDetailed` under MetadataCenter continuation context so ReqChatProcess can rebuild stopless runtime control. Restored request data such as `fullInput` and `restoredTools` must stay data-plane and be passed only into `buildCapturedRelayResumeRequestContextForHttp -> captureReqInboundResponsesContextSnapshot`, not copied into MetadataCenter.
- Anti-pattern: copying `fullInput`, route/provider pins, or session/conversation scope into MetadataCenter to fix stopless count; using folded guidance text as the repeat-count truth; changing only generated `.js` while `.ts` source and request-context normalization gate still miss the relay resume edge.
- Evidence target: `responses-request-bridge.metadata-center.spec.ts` proves `toolOutputsDetailed` is sanitized to `callId/originalId/outputText` and route/session/provider/fullInput are not copied; `responses-request-bridge.request-context-normalization.spec.ts` proves relay `fullInput/restoredTools` reach native req-inbound capture; `verify:stopless-invalid-schema-blackbox` proves repeatCount advances across provider/client turns.

## L93-24 Runtime key resolution must be exact, not compatibility probing

- Trigger: a provider runtime miss is proven to be config identity drift, but runtime code still has `key1 <-> 1`, alias-scope, model-suffix, or prefix scanning that can hide the drift.
- Rule: server runtime resolution may only use exact `providerKey -> runtimeKey` map entries, exact runtime handles, and the Virtual Router supplied `runtimeKey` hint. Missing bindings must fail fast.
- Anti-pattern: adding or keeping resolver-side normalization, case folding, parent-binding recursion, or model suffix probing to make stale/mismatched config work.
- Evidence target: provider binding tests prove exact map works and compatibility probes return `undefined`; provider runtime resolver tests prove no raw/normalized retry after selected runtime key misses; fallback denylist and root `tsc` pass.

## L93-23 Provider runtime key miss can be config identity drift

- Trigger: `Provider runtime <ProviderId>.keyN not found` while `routecodex port status <port> --json` shows the same target as health-healthy under a different provider-id case, for example route target `XL.key1.*` but health key `xl.1.*`.
- Rule: check `~/.rcc/config.toml` forwarder target `providerId`, `~/.rcc/provider/<dir>/config.v2.toml` `providerId` / `[provider].id`, and the actual provider directory name before code changes. The provider directory name is loader truth; all three identities must match exactly. On case-only rename on macOS, rename through a temporary directory first.
- Anti-pattern: adding runtime resolver fallback/compatibility probing for provider-id case drift, changing VR selection, or only editing root forwarders while leaving provider directory/file identity mismatched.
- Evidence target: `routecodex config validate` passes; managed restart passes; status/dry-run shows the target as `providerKey=<id>.key1.<model>`, `runtimeKey=<id>.key1`, `providerRegistered=true`, `available=true`; a temporary-priority live probe hits that provider and reaches provider send/reroute instead of `ERR_PROVIDER_NOT_FOUND`, then priority is restored.

## L93-22 Responses continuation released prefix must stay side-channel

- Trigger: `/v1/responses.submit_tool_outputs` or scope materialize repeats the same pending function_call batch, replays duplicate function_call_output rows, or third stopless resume still contains old stopless tool pairs/guidance.
- Rule: current live `input` and released retained history are different facts. TS bridge must pass live `entry.input` as `input` and pass `releasedInputPrefix` separately; Rust `shared_responses_conversation_utils.rs` owns collapse of replayed pending batches, duplicate outputs, and completed stopless hook pairs. Completed stopless function calls should be removed by completed output `call_id`, and generated guidance should collapse to the latest guidance only.
- Anti-pattern: using `releasedInputPrefix` as a fallback/current input, doing duplicate collapse in `responses-conversation-store.ts`, or fixing stopless history by filtering only rows that look like stop-hook function calls while ignoring completed call ids.
- Evidence target: Rust `shared_responses_conversation_utils` whitebox proves suffix and continuation-delta collapse; `tests/sharedmodule/responses-continuation-store.spec.ts` proves duplicate pending batch and third submit latest-guidance-only cases; release/runtime closeout still needs a store-specific installed-runtime replay, not only a generic stopless smoke.

## L93-21 Stopless counter is a same-session consecutive schema-error budget

- Trigger: stopless UI/runtime reports repeatCount continuing after a non-stop/tool response, or a new session inherits a previous session's missing/invalid schema count.
- Rule: response-side stopless must require current request-truth `sessionId` before any interception or reset write. Missing/blank sessionId means natural pass-through plus diagnostic alarm `stopless_missing_session_id`, with no CLI projection and no stopless runtime state write. With a sessionId, repeatCount counts only consecutive missing/invalid schema stops inside that same session; non-stop progress, ordinary tool calls, valid terminal schema, `simple_question=true`, and session changes reset the streak.
- Anti-pattern: treating `finish_reason=stop` as enough to count, using old runtime_control session as current truth, writing reset state when the current request lacks sessionId, or proving only the three-stop happy loop without reset/isolation negatives.
- Evidence target: Rust Hub Pipeline tests cover non-stop reset, missing-session alarm, session isolation, and `simple_question` cleanup; native Hub Pipeline blackbox in `tests/servertool/stopless-cli-continuation.spec.ts` must assert client payload, diagnostics, and `stoplessMetadataCenterWrite`; provider/client invalid-schema blackbox still proves the three-round positive path.

## L93-20 Build/pack safety must be gated separately from install

- Trigger: fixing release/dev packaging while a working global `rcc` is already installed, or adding package-shim exports to satisfy TypeScript during servertool Rustification.
- Rule: build/dev/pack scripts must not call global install/uninstall or delete global `rcc`; release verification should use normal npm install only under temporary `--prefix` roots. For `rcc-llmswitch-core/native/servertool-wrapper`, generated d.ts function declarations must match active package-shim imports/re-exports; local native-core-only wrappers must not be kept as package-shim declarations.
- Anti-pattern: hiding `npm run install:global` inside `build:dev:full`, using release install smoke against `/opt/homebrew/lib/node_modules/rcc`, adding unused `servertool-wrapper.d.ts` declarations because tsc needs a name elsewhere, or restoring deleted bridge files instead of fixing the active owner.
- Evidence target: `verify:function-map-build-wiring` proves build/pack have no global install mutation and release smoke uses temp prefix; `verify:servertool-rust-only` AST gate reports `servertool-wrapper.d.ts` declarations match active package-shim imports/re-exports; `pack:rcc` and `verify:rcc-release-install` pass for both `routecodex` and `rcc` tarballs.

## L93-19 Stop schema terminal exceptions must update the whole stopless contract surface

- Trigger: adding a stop schema option that allows terminal stop without the normal `stopreason` contract, such as `simple_question=true`.
- Rule: update the Rust schema gate, CLI contract schema/feedback, client-visible schema stripping, bridge/runtime stopless handling, function map, verification map, docs, and both client-facing and provider-facing blackboxes together. The canonical schema key must be single-source; for `simple_question` it is snake_case only, with no alias or fallback path.
- Anti-pattern: changing only the schema parser or prompt text, accepting both camelCase and snake_case, or letting terminal-control schema text leak to the client after allowing natural stop.
- Evidence target: focused Rust schema tests prove `true` allows stop and `false`/absent still requires `stopreason`; client blackbox proves no `exec_command` and no raw `reasoningStop`/schema leakage; provider-facing stopless/Responses bridge tests prove normal tool surface and guidance are still preserved; build/install gates pass; live closure still requires managed restart plus same-entry `/v1/responses` probe on the installed version.

## L93-18 Retry attempt metadata must preserve providerProtocol until route owner replaces it

- Trigger: provider failover blackbox logs `provider-switch ... exclude_and_reroute`, then the next Hub attempt fails before backup send with `HubPipeline requires metadata center runtime_control.providerProtocol`.
- Rule: `decorateMetadataForAttempt()` may release single-use provider pins (`preselectedRoute`, `retryProviderKey`) on retry, but must preserve `runtime_control.providerProtocol`. Hub request materialization reads providerProtocol before VR commits the next selected target; the request-route owner may later replace it atomically with the new target protocol.
- Anti-pattern: treating providerProtocol like a selected-provider pin and releasing it during retry metadata cleanup, or fixing the failure in Hub Pipeline with endpoint-derived fallback. ProviderProtocol remains MetadataCenter runtime control, not client entry-derived payload data.
- Evidence target: focused providerProtocol metadata tests pass, `tsc` refreshes `dist`, and `verify:provider-failure-ban-blackbox` proves 503/401/403/429 primary failures reroute to backup instead of failing before backup send.

## L93-17 Plain start is foreground takeover; daemon is explicit

- Trigger: `rcc start` / `rcc start --snap` only prints `Supervisor PID`, hides runtime startup logs, reports an existing managed server instead of refreshing it, or multiple daemon supervisors fight over `/shutdown`.
- Rule: plain release `routecodex start` / `rcc start` defaults to foreground with runtime stdout/stderr visible, and still owns the managed runtime slot by default. It must gracefully stop the existing RouteCodex child, start the new release snapshot, and show real startup logs. `--no-restart` is probe-only mode; daemon mode requires explicit `ROUTECODEX_START_DAEMON=1` / `RCC_START_DAEMON=1`.
- Daemon rule: explicit daemon takeover stop-intent is a broadcast to old supervisors; the new supervisor must ignore the parent takeover pid so it does not consume its own start intent. Old supervisors stop instead of scheduling another child restart.
- Start-lock rule: if another start already holds the same port-group takeover lock, the second start must not silently sleep. It should probe live `/health` first and exit 0 once the managed runtime is ready; while health is not ready it must emit waiting progress, retry acquiring a released/dead-owner lock, and then continue startup or fail with an explicit timeout.
- Startup log rule: transient `network_error` while the new child has not opened `/health` is not a user warning. Only final timeout/exit/startupError should be surfaced; successful foreground start must show `RouteCodex version`, port registration, active listeners, and health URL.
- Install-surface rule: lifecycle closeout must verify `rcc --version`, `routecodex --version`, npm-owned `/opt/homebrew/bin/rcc`, `/opt/homebrew/lib/node_modules/rcc/package.json`, and all configured `/health.version` values. If `/opt/homebrew/bin/routecodex` is a non-npm stale shim, do not force overwrite without explicit approval; prove the active `rcc` executable and live runtime version instead.
- Test safety: lifecycle Jest must never hit live `127.0.0.1/localhost` `/shutdown` through an unmocked fetch. Daemon tests must set `ROUTECODEX_START_DAEMON=1`; default-release tests must assert foreground stdio/log behavior.
- Anti-pattern: flipping `resolveReleaseDaemonEnabled({})` to true, accepting CLI success before real health, clearing takeover intent before old supervisors observe it, silently waiting on a start lock with no progress, validating only one `rcc` path while another global install root is stale, or letting focused tests stop Jason's live server.
- Evidence target: focused lifecycle Jest locks default `daemon=false`, explicit daemon health wait, default restart/takeover, and start-lock progress; real global `rcc start --snap` prints runtime startup logs and four configured `/health.version` values match the installed version; explicit `ROUTECODEX_START_DAEMON=1 rcc start --snap` restores background service without health_probe warning spam.

## L93-16 Deleted native wrapper source must be replaced by typed owner exports, not restored casually

- Trigger: a Rustification slice deletes or deprecates `native-chat-process-servertool-orchestration-semantics.ts`, then TypeScript shells import servertool functions from `native-servertool-core-semantics.ts` and `tsc` reports package-shim return values as `unknown`.
- Rule: keep the current Rust owner wrapper as the single TS import surface and add explicit local type signatures for package-shim functions that TS shells consume. Cast only the imported shim function to the known contract; do not cast runtime values at call sites and do not restore deleted source unless function-map/mainline explicitly requires that file as owner.
- Anti-pattern: reintroducing the deleted wrapper to satisfy imports, adding new prod TS type shim files, or spreading `as any` / `as Record<string, unknown>` through servertool shells. That creates a second bridge owner and can break rustification audits.
- Evidence target: sharedmodule `tsc` passes; `verify:servertool-rust-only` passes; focused servertool shell/Jest proves runtime still calls the native bridge; residue scan over `sharedmodule/llmswitch-core/src/servertool` shows no `.action` application branches.

## L93-15 Model-capacity provider text belongs to retryable HTTP_429

- Trigger: provider/upstream returns only text like `Selected model is at capacity. Please try a different model.` and RouteCodex needs to retry/reroute it as 429.
- Rule: normalize provider-agnostic capacity saturation text in `normalizeKnownProviderError()` to existing recoverable `HTTP_429` (`429.1000`). This is transient capacity pressure, not account quota depletion; do not map it to `INSUFFICIENT_QUOTA`.
- Anti-pattern: adding string checks in request executor/router/direct code, using provider-specific config mapping for a generic text, or treating "try a different model" as a model-not-supported/direct-return condition.
- Evidence target: catalog test proves key/status/class; policy test proves health-impacting recoverable retry with `exclude_and_reroute`; HTTP error projection test proves no-status Error objects get `code=HTTP_429` and `statusCode/status=429`. Then run the provider failure blackbox/build/live gates when the broader worktree can build.

## L93-14 rcc lifecycle must expand config port groups and trust HTTP shutdown over PID cache

- Trigger: `rcc stop` says only one configured port has no server, while `rcc start --snap` resolves a larger port group and exits with `already_running_unmanaged` on a sibling port.
- Rule: for release lifecycle commands, a matched `targetPort` inside `httpserver.ports` must expand to the full configured port group unless the caller explicitly asks for check-only/single-port dev semantics. PID cache/listener PID discovery is a hint only; `stop` must still attempt RouteCodex `/shutdown` and verify `/health` disappears when PID discovery returns empty.
- Anti-pattern: treating no PID as no server, treating guardian health/finalize network errors as stop failure after ports are already stopped, or making plain `start --snap` stop a live server by default.
- Evidence target: focused CLI tests cover stop full-group expansion, no-PID HTTP shutdown, guardian finalize non-blocking, and plain start non-disruptive behavior with explicit `--restart` opt-in. Live validation must use `/health` before/after and confirm no unexpected shutdown for plain start.

## L93-12 Release/live SSE replay must use the installed `rcc` runtime and captured provider response

- Trigger: `/v1/responses` SSE fails in live with `Responses SSE event sequence missing request_id`, while source replay passes or source already contains a suspected fix.
- Rule: replay the exact captured provider response through the installed package root that live uses (`/opt/homebrew/lib/node_modules/rcc/...` when registry command is `rcc start`). Check snake_case dispatch fields such as `request_id` / `body_text` at the Rust NAPI boundary, then install both `routecodex` and `rcc` tarballs before live restart.
- Anti-pattern: validating only repo `dist`, installing only `routecodex`, or treating `/health` on 5555 as proof that the same package root is live. 5555 may be a localPort under serverId 5520, and live may consume `rcc`.
- Evidence target: installed replay of the old `provider-response_*.json` emits `response.created` / `response.done` with the original request_id; live `/v1/responses` smoke on 5555 returns SSE events with request_id; `/health` reports the installed version for both 5520 and 5555.

## L93-13 Provider cooldown must never persist across restart

- Trigger: after restart a provider remains unavailable because stale `provider-health.json`, `health.jsonl` snapshot state, or imported `providerCooldowns` survived process death.
- Rule: cooldown/availability truth is process-local Rust health state only. Delete load/persist/import/export paths for provider cooldown; old provider-health files may exist only as red-test fixtures proving they are ignored.
- Anti-pattern: preserving persisted cooldown for compatibility, pruning expired disk state, hand-deleting runtime files, or letting TS HealthManager expose `loadInitialSnapshot` / `persistSnapshot`.
- Evidence target: Rust selection/preselected-route tests show stale provider-health does not affect routing; provider-error tests and blackbox prove no `provider-health.json` is written; architecture TS-owner gate forbids cooldown persistence symbols in production source.

## L93-11 `/v1/models` `tool_mode=code_mode_only` changes Codex tool planning

- Trigger: Codex starts printing tool-call transcripts as assistant text (`Assistant requested tool calls...`, `apply_patch [{...}]`) after a model metadata/capability change, while `apply_patch_tool_type` and tool declarations still look present.
- Rule: inspect `/v1/models` first, then compare Codex client source for how the capability is consumed. `tool_mode: "code_mode_only"` is not a harmless capability flag: Codex hides direct nested tools and exposes code-mode executors. Omit `tool_mode` unless intentionally switching that model to code-mode executor semantics.
- Anti-pattern: fixing only response text sanitizers, patching SSE transport, or treating `experimental_supported_tools` as proof that direct/native tools are still model-visible. SSE is transport; the capability source of truth is the model metadata contract plus Codex planning code.
- Evidence target: route test proves `tool_mode` absent while freeform `apply_patch`, `web_search`, parallel tool calls, and modalities remain; live installed `/v1/models` confirms the same; latest `~/.rcc/codex-samples/openai-responses/ports/<port>/` client/provider files scan clean for transcript markers; structured tool-call turns finish with `tool_calls` where expected.

## L93-10 Responses SSE timeouts must align across semantic, transport, and provider request windows

- Trigger: `/v1/responses` direct SSE fails with `UPSTREAM_STREAM_CONTENT_IDLE_TIMEOUT` on long LM Studio prefill, or a fix changes only `providerStreamNoContentTimeoutMs` / `providerStreamContentIdleTimeoutMs`.
- Rule: 15 minute semantics are valid only when all relevant windows align: `HTTP_SSE_TOTAL_MS`, provider stream headers cap, byte-idle cap, semantic no-content/content-idle timeout, and `PROVIDER_TIMEOUTS.RESPONSES` / `BASE_SERVICE_PROFILES.responses.timeout`. Verify both `routecodex` and `rcc` installed package roots, then restart/reload the live managed process and prove the new PID handles a real `/v1/responses` SSE request.
- Anti-pattern: adding per-provider hidden timeout overrides, downgrading LM Studio Responses direct to chat, or claiming closure from source constants while global installed `dist` or the live process still has old `RESPONSES=240000`.
- Evidence target: focused timeout/unit gates pass; `routecodex` and `rcc` global constants all report `900000`; live reload creates a new server PID; 4444 `/v1/responses` SSE returns `response.completed` with `model=ornith-1.0-397b`; no new matching `UPSTREAM_STREAM_CONTENT_IDLE_TIMEOUT` appears for the smoke request. Long-prefill closure still requires old failing sample replay or observing beyond the old 5 minute cutoff.

## L93-12 Release install readiness is not runtime adoption

- Trigger: `install:release` or `routecodex restart --port <port>` reports success, CLI/shim versions are current, but live `/health.version` still reports an older build or the live command/root is not the expected runtime.
- Rule: release/runtime closeout must gate on live `/health.version === package.json.version`, not only `status=ok`, `ready=true`, CLI `--version`, or `/health` reachability. If ready health reports a different version, use a single-port managed adoption path: write stop intent, local `/shutdown`, wait for unavailability, start through the current release shim, then repeat the same strict health gate.
- Anti-pattern: accepting restart success, CLI version, or package install success as proof that the managed listener adopted the new runtime; broad process kill; stopping config groups when the task is a single-port install verification.
- Evidence target: install log shows strict version gate; independent curl confirms the live port version; process command/root is inspected when snapshot-root adoption matters; servertool/runtime closeout then runs a same-entry live replay such as stopless first-turn `requires_action` -> `submit_tool_outputs` -> final `completed`.

## L93-13 Request log colors require registered color tokens, not only session keys

- Trigger: request start/completion/`[usage]` lines share one color, but `[virtual-router-hit]` stays on route color for the same `req=...` request id.
- Rule: the live path must carry a session color key. Direct-route metadata projection must preserve `logSessionColorKey` and tmux/session aliases into VR route metadata; `src/server/utils/request-log-color.ts` must then recolor `virtual-router-hit` from the registered `req=...` token before parsing textual `sid=` / bracket session. A VR line with omitted `sid` is allowed only as display config; the underlying request color carrier must still exist.
- Anti-pattern: fixing this in VR route-color tables, usage logger, port prefix wrapper, or by inventing request-id hash colors. Route color is only the raw VR formatter color; console/request-log-color owns cross-line visual alignment.
- Evidence target: red/green `tests/server/runtime/http-server/direct-runtime-metadata.spec.ts` proving color carriers survive router-direct projection, plus `tests/server/utils/request-log-color.spec.ts` / `tests/server/runtime/http-server/executor/usage-logger.spec.ts` proving request start, virtual-router-hit, response, and usage lines all start with the same registered session color.

## L93-09 Codex tool-call transcript leaks must be fixed at Responses client projection

- Trigger: Codex UI shows plain assistant text like `Assistant requested tool calls: - id=call_... type=function name=exec_command arguments=...`, or client-response samples contain that text in `output_text` / `response.output_text.delta`.
- Rule: first prove whether the leak is structured tool rendering or client-visible text by checking Codex session JSONL and `~/.rcc/codex-samples/openai-responses/ports/<port>/<requestId>/client-response.json`. For `/v1/responses`, the live projection owner is `hub.response_responses_client_projection`; sanitize only client-visible display text fields (`output_text`, output_text content `text`, `response.output_text.delta`, `response.output_text.done`) and preserve structured `function_call.arguments`.
- Anti-pattern: declaring closure from `governResponseJson` / generic display sanitizer tests alone, patching SSE transport, or stripping every field named `arguments`. SSE is transport; tool argument payloads are structured protocol data, not display text.
- Evidence target: Rust tests for both full Responses client payload projection and SSE frame projection; native build and projection gate pass; release tarballs for both `routecodex` and `rcc` installed globally; live `/v1/responses` replay against installed port returns `response.completed` and grep for the leaked transcript/call id/command returns zero.

## L93-08 routecodex+rcc release install must use normal npm install gate

- Trigger: `npm install -g artifacts/pack/rcc-<version>.tgz` fails, release verification only uses offline/prefer-offline checks, or live managed ports still execute global `routecodex` after only `rcc` was freshly installed.
- Rule: prove the actual npm install path for both package names. The release must produce `routecodex-<version>.tgz` and `rcc-<version>.tgz`; both tarballs must pass `npm install -g <tgz> --prefix <tmp>` with normal npm behavior, then real global install when authorized. Check installed package roots are not repo-linked, `rcc-llmswitch-core` is not a symlink and has `dist`, all production dependencies are bundled, and build-only packages such as `esbuild` are absent from production dependencies and bundled tarball contents.
- Anti-pattern: using offline install as the solution, treating tarball file listing as equivalent to npm lifecycle install, bundling every package from an accidentally polluted `dependencies` block, or assuming identical semver means `routecodex` and `rcc` have the same build.
- Evidence target: `scripts/verify-rcc-release-install.mjs` PASS for both packages; `npm run pack:rcc` produces both tarballs; `routecodex --version` and `rcc --version` from installed packages return the packed version; both installed package JSON/buildInfo/real paths do not contain stale repo roots or mismatched buildTime; managed port live replay confirms the runtime consumed the synchronized `routecodex` package.

## L93-07 LM Studio Responses direct must not be downgraded to chat

- Trigger: 4444 `/v1/responses` with `provider.type=responses` reports `PROVIDER_NOT_AVAILABLE` or LM Studio upstream 400 such as missing `text.format`.
- Rule: first prove config/runtime truth (`~/.rcc/provider/lmstudio/config.v2.toml`, `routecodex port status/dry-run`) and keep same-protocol Responses direct. Fix only the unique owner: VR availability/filtering in Rust if selection is wrong, or Rust `responses:lmstudio` compat if LM Studio requires a provider-wire field such as `text.format`.
- Anti-pattern: switching LM Studio to chat, adding Responses-to-Chat conversion, patching handler/SSE/direct response shells, or treating `provider.type=responses` as evidence that it should leave direct passthrough.
- Evidence target: exact old `/v1/responses` sample dry-run must select `lmstudio.key1.ornith-1.0-397b` with `providerProtocol=openai-responses` and `wouldReturnProviderNotAvailable=false`; live replay with `Accept: text/event-stream` must return HTTP 200, `response.completed`, no `event:error`, `created.model=ornith-1.0-397b`, and `created.text.format.type=text`.

## L93-06 Direct runtime metadata must be primitive control projection

- Trigger: image-bearing `/v1/responses` or multimodal direct requests fail before provider response with `JSON stringify failed: Maximum call stack size exceeded` or bare `Maximum call stack size exceeded`.
- Rule: native/direct/provider runtime bridge inputs may receive only minimal serializable control carriers. Project primitives at the direct runtime boundary: endpoint, port/scope, routing group, client request id, timeout controls, and protocol-specific direct flags.
- Anti-pattern: passing full `metadataForHub`, `metadataCenterSnapshot`, request `input`, image payload arrays, entry request bodies, or MetadataCenter-bound live objects into native JSON bridges/provider runtime metadata. Do not fix this in SSE, handler, snapshot writer, or outbound projection; SSE is transport only.
- Evidence target: unit regression with cyclic metadata plus `input_image` must prove `JSON.stringify(projected)` succeeds and forbidden live fields are absent; live `/v1/responses` image replay must reach VR/provider switching without `Maximum call stack` / `evaluateResponsesDirectRouteDecisionJson` logs.

## L93-05 Responses conversation capture must use entry payload, not provider wire body

- Trigger: `/v1/responses` logs `RESPONSES_STORE_MISSING_REQUEST_CONTEXT` after VR/provider id rewrite even though request capture/rebind code appears present.
- Rule: if server swaps `body=hubBody`, preserve the original entry body as same-request data-plane truth and let request-executor capture use it before provider wire body. Provider wire payload (`data/messages`, provider model) is not valid Responses continuation context source.
- Anti-pattern: fixing this by scope fallback, by using provider response fields, by relying on debug `contextSnapshot`, or by moving request context into MetadataCenter control state.
- Evidence target: focused capture red/green where Hub body has provider wire shape and `__raw_request_body` has Responses `input/tools`; live `/v1/responses` first turn plus `submit_tool_outputs` continuation must produce no new `RESPONSES_STORE_MISSING_REQUEST_CONTEXT` / `record.missing_request_context` logs.

## L93-01 Codex capability/tool audits need one-shot live replay

- Trigger: debugging Codex `tool_search`, MCP, browser/computer, or node_repl exposure through RouteCodex.
- Rule: first verify `/v1/models` model-level metadata, then inspect Codex source for client/deferred tool contract, then run a one-shot `codex exec -p rcm -m gpt-5.5` replay that instructs the model to call `tool_search` exactly once and stop.
- Anti-pattern: prompts like "must call tool_search" without a one-shot stop condition can cause repeated `tool_search_call/tool_search_output` loops; do not misclassify that as RouteCodex tool loss.
- Evidence target: `~/.rcc/codex-samples/openai-responses/ports/<port>/<requestId>/provider-request.json` must show structured `tool_search` and relevant capability terms, while leak scans for `<function_calls>`, `Function_37m6_Start`, `minimax:tool_call`, and `<tool_call` stay empty.

## L93-02 Provider protocol mismatch is not a VR hit filter

- Trigger: priority/provider failover logs show selected backup excluded or failed because `providerProtocol` / `outboundProfile` differ, especially `openai-responses` entry selecting `anthropic-messages` or `openai-chat` targets.
- Rule: VR decides candidate hit and ordering only from route/capability/health/exclusion/default-floor truth. Direct-vs-relay protocol compatibility belongs to server direct/relay execution boundary, not VR hit acceptance or executor selection commit.
- Anti-pattern: throwing `ERR_VR_SELECTION_PROVIDER_PROTOCOL_MISMATCH` or filtering candidates because the selected provider protocol differs from the inbound/client protocol; this turns execution-mode choice into false provider unavailability.
- Evidence target: Rust VR `protocol` tests must keep cross-protocol route/forwarder targets selectable, and executor pipeline-attempt tests must accept a selected cross-protocol target while still failing fast on excluded-provider reselection.

## L93-03 Response-stage prepass/finalize payloads belong to native result plans

- Trigger: `response-stage-prepass-shell.ts` or `response-stage-finalize-shell.ts` constructs `{ action: "continue_to_execution" }`, `{ action: "return_result" }`, passthrough engine results, or returns `responseStageAutoHook.result` / `passthroughResult` directly.
- Rule: Rust `plan_servertool_response_stage_runtime_action` must return `prepassResult` and `finalizeResult`; TS may run required auto-hook IO and switch on native action discriminants only.
- Anti-pattern: moving hook matching to Rust but leaving prepass/finalize result object materialization in TS; that keeps response-stage as a second business owner.
- Evidence target: focused response-stage Jest, servertool active orchestration audit, native bridge spec, Rust servertool-core/NAPI tests, native rebuild, sharedmodule tsc, and `verify:servertool-rust-only` must all pass before committing the slice.

## L93-04 Request/usage release logs should print compact truth only

- Trigger: release logs show raw/prepared input shape JSON, `finish_reason=unknown`, `usage=in:n/a`, or a standalone `[router-direct.send] completed` line next to usage.
- Rule: request-start logs may keep scalar counts and mode, but must not print shape/debug payload summaries; usage logs should say `usage=unreported` when upstream omits metrics and omit finish_reason entirely when unavailable.
- Anti-pattern: inventing placeholder truth (`unknown`, `n/a`) or splitting one terminal request summary across `[router-direct.send] completed` plus `[usage]`, which obscures the request outcome.
- Evidence target: focused logging Jest plus live `codex exec -p rcm -m gpt-5.5` replay against 5520 and grep new server log lines for forbidden markers.

## L93-14 Release CLI verification must cover every resolved rcc path

- Trigger: `rcc start --snap` still prints `Use 'rcc start --restart'` after source/release snapshot says the fix is installed.
- Rule: before claiming release CLI behavior, run `which -a rcc`, inspect every real package root/version, and verify the exact path Jason's shell may execute, especially `/opt/homebrew/bin/rcc -> /opt/homebrew/lib/node_modules/rcc/dist/cli.js` and `~/.local/bin/rcc`. If any resolved path is stale, install the `rcc-<version>.tgz` package into that global root and rerun the failing command by absolute path.
- Anti-pattern: accepting `~/.local/bin/rcc --version`, release snapshot health, or shim version as proof that Homebrew/global `rcc` was updated.
- Evidence target: `/opt/homebrew/bin/rcc --version`, `/Users/fanzhang/.local/bin/rcc --version`, and bare `rcc --version` all match; installed `/opt/homebrew/lib/node_modules/rcc/dist` contains the expected code marker; `/opt/homebrew/bin/rcc start --snap` takes over running ports instead of exiting `already_running`.

## L93-15 Release `rcc start --snap` must stay foreground/log-visible by default

- Trigger: `rcc start --snap` printed only `RouteCodex daemon supervisor started ... Supervisor PID`, so the startup terminal no longer showed RouteCodex runtime logs.
- Rule: release `rcc start` defaults to foreground with inherited stdio. Daemon supervisor is opt-in only via `ROUTECODEX_START_DAEMON=1` / `RCC_START_DAEMON=1`. If automation needs a persistent service after foreground verification, use the explicit daemon env and state that it is not the default user-facing start path.
- Anti-pattern: flipping `resolveReleaseDaemonEnabled({})` to true to keep a background service alive, or treating PID-only supervisor output as equivalent to startup logs.
- Evidence target: `tests/cli/start-command.spec.ts` asserts `resolveReleaseDaemonEnabled({}) === false`; installed `/opt/homebrew/lib/node_modules/rcc/dist/cli/commands/start-utils.js` returns default daemon=false; real `rcc start --snap` prints runtime startup logs and all configured `/health.version` values match the installed package.

## L93-16 Hub Rustification closeout requires isolated core build and direct request capture

- Trigger: `install:release` fails `verify:servertool-rust-only` with missing `sharedmodule/llmswitch-core/dist/native/servertool-wrapper.js` / `.d.ts`, or direct `/v1/responses` first turn with tool continuation fails `RESPONSES_STORE_MISSING_REQUEST_CONTEXT`.
- Rule: release snapshot builds are isolated and exclude llmswitch-core dist, so generate core dist in the isolated build root before release gates. For direct same-protocol Responses tool continuations, capture the current entry request `input/tools` as same-request data-plane store context before recording the direct response.
- Anti-pattern: treating repo-local dist as proof the isolated release build has native wrappers, recording direct continuation responses before request context exists, fixing direct continuation by scope fallback, provider response inference, MetadataCenter control state, or Hub response conversion.
- Evidence target: release install log shows `build-core` before `build:min` and strict `/health.version === package.json.version`; direct continuation blackbox first turn returns 200 and cross-port second turn rejects with 400 without provider-B hit; live stopless replay on the installed version shows first-turn structured `requires_action` then submit completion without metadata/control/stop-schema leakage.

## L93-17 Responses SSE disconnect fixes must stay transport-only

- Trigger: `/v1/responses` SSE becomes easier to disconnect after state-machine or timeout changes, especially when the stream is valid but frames appear buffered on the client side.
- Rule: if the fix is transport flushing, only flush already-finalized bytes after `res.write()` when `res.flush()` exists. Keep the keepalive as the SSE comment `: keepalive\n\n`; do not tighten timeout to hide buffering, and do not add synthetic business/protocol events.
- Anti-pattern: adding `event: ping`, `{"type":"ping"}`, terminal-state parsing, `required_action` inference, continuation save/restore, stopless/servertool repair, or provider semantic patching in `handler-response-sse.ts`. SSE is transport; protocol projection stays in Rust `sse.responses_encode_projection`.
- Evidence target: focused SSE keepalive/flush test proves keepalive and upstream frames are flushed while no ping event/type appears; `verify:responses-sse-business-module` passes; installed `rcc`/`routecodex` dist contains the flush marker; same-entry live `/v1/responses` SSE smoke returns HTTP 200, `response.completed`, and `[DONE]`.

## L93-18 Responses store scope-match must return native action plans

- Trigger: `responses-conversation-store.ts` selects latest continuation by scope and locally filters `allowContinuation`, `lastResponseId`, `continuationOwner`, dedupe keys, or direct/relay ambiguity.
- Rule: TS may read Maps and pass minimal candidate projections to Rust, but the selection decision must be a native plan from `shared_responses_conversation_utils.rs`. The plan should return `restore/materialize/none` plus `scopeKey` and reason; TS only executes the chosen IO path.
- Anti-pattern: moving scope-key construction to Rust while leaving match filtering or ambiguity policy in TS; duplicating Rust owner decisions in a facade fallback; treating facade LOC growth as semantic progress without a Rust whitebox test.
- Evidence target: Rust unit covers relay selection, direct exclusion, mixed direct+relay ambiguity, and multi-match ambiguity; rebuild native hotpath before Jest; focused responses store Jest, `verify:responses-history-protocol-contract`, `verify:llmswitch-rustification-audit`, function-map, and mainline gates pass.

## L93-20 Responses submit resume entry matching belongs to native plan

- Trigger: `resumeConversation()` locally checks response-index hits, scans requestMap to recover missing responseIndex, detects ambiguous response ids, or falls back through submit-payload scope keys.
- Rule: TS may project response-index/request-map/scope candidates from Maps, but Rust must decide selection order and validity. Native plan owns responseIndex precedence, requestMap recovery ambiguity, scope fallback, port scope, entryKind, continuationOwner, and allow-continuation expiry.
- Anti-pattern: leaving `recoverResponseIndexEntry()` or equivalent TS loops that compare `lastResponseId`, `portScopeKey`, `entryKind`, `continuationOwner`, or `allowContinuation`; adding a TS fallback if native plan is missing.
- Evidence target: Rust unit covers indexed precedence, requestMap recovery, requestMap ambiguity, scope fallback with port scope, and expired entry; rebuild native hotpath; focused responses store Jest, responses history protocol contract, rustification audit, function-map, and mainline gates pass.

## L93-21 Responses lookup projection gates belong to native plan

- Trigger: lookup helpers such as `lookupContinuationByResponseId()` locally check response id, `lastResponseId`, port scope, `entryKind`, or `continuationOwner`, then project `providerKey` / owner / kind / requestId.
- Rule: TS may read the index and call native with entry/options, but Rust must validate match and return the projection. This keeps lookup semantics aligned with submit/resume/materialize plans.
- Anti-pattern: treating lookup as harmless read-only code and leaving isolation/projection rules in TS; reimplementing providerKey/owner/kind normalization in the store after Rust has a plan helper.
- Evidence target: Rust unit covers selected lookup plus port/owner/kind/missing-entry rejection; rebuild native hotpath; focused store Jest, responses history protocol contract, rustification audit, function-map, and mainline gates pass.

## L93-22 Responses persistence eligibility belongs to native plan

- Trigger: `responses-conversation-store.ts` persistence load/flush code filters entries by missing response, `continuationOwner`, `allowContinuation`, TTL, or projects `lastResponseId`.
- Rule: Rust `shared_responses_conversation_utils.rs` must return a persistence eligibility action plan. TS may only serialize/deserialize persisted records, iterate files/Maps, and execute native-selected save/load decisions.
- Anti-pattern: keeping a TS `shouldPersistLocally()` helper, treating persistence as pure IO while embedding continuation eligibility there, or adding TS fallback filters when the native export is unavailable.
- Evidence target: Rust unit covers direct-owner skip, missing response skip, flush-time unallowed skip, load-time expired skip, and eligible `lastResponseId`; rebuild native hotpath before Jest; focused store Jest, responses history protocol contract, rustification audit, function-map, and mainline gates pass.

## L93-23 Delete generic isolation facades after concrete native plans own decisions

- Trigger: a generic helper such as `entryMatchesIsolation()` / `responsesConversationEntryMatchesIsolationJson` remains after scope-match, submit-resume, lookup, or persistence operations have concrete native action plans.
- Rule: once no production caller remains, remove the TS wrapper, native facade export, required-export entry, Rust NAPI export, private Rust helper, and checked generated JS/d.ts outputs. Future behavior must be added to the operation-specific native plan, not to a generic TS-callable isolation helper.
- Anti-pattern: keeping "just in case" native exports, relying on no current import as sufficient cleanup, or only deleting TS source while checked JS/d.ts still expose the old surface.
- Evidence target: repository search for old symbols returns zero matches; rebuild native hotpath and llmswitch-core checked outputs; sharedmodule tsc, focused store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-24 Capture-time pending cleanup belongs to native plan

- Trigger: `responses-conversation-store.ts` `captureRequestContext()` scans request-map entries and decides whether to detach old unresolved entries in the same continuation scope.
- Rule: TS may project current candidates from Maps and execute native-selected detach ids, but Rust must decide requested scope validity, same-request exclusion, unresolved-only filtering, scope overlap, and dedupe.
- Anti-pattern: leaving `candidate.scopeKeys.some(...)` / `!candidate.lastResponseId` cleanup decisions in TS because they look like local Map maintenance; this is continuation scope isolation policy and belongs to the Rust store plan.
- Evidence target: Rust unit covers detach, resolved skip, different-scope skip, self skip, dedupe, and missing-scope noop; rebuild native hotpath and llmswitch-core checked outputs; focused store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-25 Record-time cleanup and fallback scope selection belong to native plans

- Trigger: `recordResponse()` locally decides which completed same-scope entries to detach, or loops requested fallback scope keys and picks the first `scopeIndex` hit when request-context lookup misses.
- Rule: TS may build requested scope keys, read Maps, and project minimal candidate rows, but Rust must decide record-time cleanup and fallback scope selection. Use native plans that return `detachRequestIds` and selected `scopeKey/requestId`; TS only executes the chosen detach/lookup IO.
- Anti-pattern: leaving `lastResponseId` completed-vs-pending cleanup rules, self exclusion, dedupe, or fallback requested-scope iteration order in `responses-conversation-store.ts` because they appear to be local index maintenance.
- Evidence target: Rust units cover completed same-scope detach, missing-scope noop, first requested matching scope selection, and no-match behavior; rebuild native hotpath plus llmswitch-core checked outputs; focused responses store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-26 Store lifecycle sweep belongs to native plan

- Trigger: `responses-conversation-store.ts` locally decides which entries `clearUnresolvedRequests()` or `prune()` should detach by checking `lastResponseId` and `updatedAt/TTL`.
- Rule: TS may read `requestMap`, project minimal candidate rows, and execute native-selected `detachRequestIds`, but Rust must decide unresolved-only cleanup, TTL expiry cleanup, invalid-mode noop, and dedupe. Lifecycle detach policy is store semantics, not TS map maintenance.
- Anti-pattern: leaving `if (entry.lastResponseId) continue` or `if (now - entry.updatedAt > TTL_MS)` loops in TS after other continuation lifecycle policies already moved to Rust.
- Evidence target: Rust unit covers unresolved sweep, expired sweep, and missing-timing noop; rebuild native hotpath and llmswitch-core checked outputs; focused responses store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-27 Release request payload belongs to native plan

- Trigger: `responses-conversation-store.ts` `releaseRequestPayload()` locally strips stored-context media, writes `previous_response_id`, extracts pending tool-call ids, or clears released `input`.
- Rule: TS may load the entry, call the native release plan, and execute store IO, but Rust must own released payload shaping, media stripping, `previous_response_id` projection, pending tool id extraction, and input clearing.
- Anti-pattern: treating release as harmless mutable store bookkeeping and leaving payload transformation in TS, or adding TS fallback behavior when the native plan is unavailable.
- Evidence target: Rust unit covers previous response projection, pending tool-call id extraction, media stripping, and input clearing; rebuild native hotpath and llmswitch-core checked outputs; focused responses store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-28 Attach-scope collision belongs to native plan

- Trigger: `responses-conversation-store.ts` `attachEntryScopes()` locally checks `scopeIndex`, compares previous entry identity, dedupes scope keys, or decides which old request ids to detach.
- Rule: TS may project current scope-index candidates and perform Map IO, but Rust must return the attach plan: normalized scope keys plus detach request ids. Scope collision is continuation isolation policy, not harmless local index maintenance.
- Anti-pattern: leaving `previous && previous !== entry` detach policy in TS, or adding TS fallback behavior if the native attach plan is unavailable.
- Evidence target: Rust unit covers conflict detach, duplicate scope-key dedupe, same-entry exclusion, missing-scope noop, and missing-request noop; rebuild native hotpath and llmswitch-core checked outputs; focused responses store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-29 Deprecated TS shims still need explicit map truth

- Trigger: a source file has `feature_id:*` but is only a deprecated/throwing compatibility shim, such as `config.virtual_router_types`.
- Rule: register it as `status: deprecated`, `owner_kind: ts_shell`, file-scoped, with a clearly named boundary marker as the canonical builder. Deprecated throwing compatibility functions are not canonical builders and must be documented as non-runtime symbols.
- Anti-pattern: deleting the map entry because the file "does nothing", or marking deprecated throwing functions as canonical builders, which can turn stale server imports into false owner leakage.
- Evidence target: `verify:function-map-compile-gate` passes, `verify:function-map-forbidden-mentions` has no stale caller leaks for the canonical marker, and the notes explicitly point runtime truth back to the Rust owner.

## L93-30 Request-id rebind belongs to native plan

- Trigger: `responses-conversation-store.ts` `rebindRequestId()` locally checks old/new ids, same-id, old-entry existence, or overwrites a destination request id.
- Rule: TS may read `requestMap` existence and execute the native-selected Map mutation, but Rust must decide whether rebinding is allowed. Destination request id conflict is a native-plan noop unless a future explicitly tested Rust policy says otherwise.
- Anti-pattern: leaving `if (!oldId || !newId || oldId === newId)` and `if (!entry) return` policy in TS, or silently overwriting an existing `newId` entry from TS.
- Evidence target: Rust unit covers valid rebind, missing old/new id, same id, missing old entry, and new id conflict; rebuild native hotpath and llmswitch-core checked outputs; focused responses store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-31 Persisted entry shaping belongs to native plan

- Trigger: `responses-conversation-store.ts` persistence code serializes/deserializes entries and locally filters fields, validates required ids, defaults `entryKind`, checks `continuationOwner`, or cleans `scopeKeys` / pending ids / record arrays.
- Rule: TS may JSON-clone disk/Map values and perform filesystem/Map IO, but Rust must return the canonical persisted-entry action plan. Required persisted truth, field allowlist, owner/kind normalization, timestamp defaults, and array cleanup all belong to `shared_responses_conversation_utils.rs`.
- Anti-pattern: treating persistence serialization as harmless IO and leaving `serializeEntry()` / `deserializeEntry()` as TS policy, or adding TS fallback shape cleanup if the native export is unavailable.
- Evidence target: Rust unit covers canonical field shaping and missing required truth rejection; rebuild native hotpath and llmswitch-core checked outputs; focused responses store Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-32 Continuation meta projection belongs to native plan

- Trigger: `responses-conversation-store.ts` locally merges `providerKey`, `continuationOwner`, or `entryKind` into restored/resumed/materialized continuation meta.
- Rule: TS may pass the native-produced meta and selected store entry to Rust, but Rust must decide which missing projection fields to fill and which existing meta fields to preserve.
- Anti-pattern: treating meta merge as harmless wrapper code; this can reintroduce TS continuation-owner/provider pin policy after store selection moved to Rust.
- Evidence target: Rust unit covers preserving existing meta fields, filling missing providerKey/continuationOwner/entryKind, then rebuild native hotpath, llmswitch-core build, focused store/residue Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check.

## L93-33 Responses store token normalization belongs to native plan

- Trigger: `responses-conversation-store.ts` locally trims scope tokens, defaults `entryKind`, or validates `continuationOwner` while projecting Map candidates.
- Rule: TS may pass raw token/kind/owner fields to Rust and use the native token plan result for Map keys or candidate projection. Rust must own trimming, `relay`/`direct` entry-kind defaulting, and valid continuation-owner selection.
- Anti-pattern: keeping local helpers such as `readScopeToken()`, `normalizeEntryKind()`, or `normalizeContinuationOwner()` in the store after operation-specific plans exist; this recreates TS continuation isolation policy under the name of Map maintenance.
- Evidence target: Rust unit covers trimmed scope token, invalid token rejection, entry-kind defaulting, and owner normalization; rebuild native hotpath and llmswitch-core checked outputs; focused store/residue Jest, responses history protocol contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-34 Bridge policy action descriptor parsing belongs to Rust

- Trigger: `native-hub-bridge-policy-semantics.ts` locally parses Rust-returned bridge policy/action JSON into policy phases or action descriptors.
- Rule: Rust `hub_bridge_policies.rs` owns policy shape, stage-to-phase/action selection, action descriptor name/options, and unknown policy/stage null projection. TS policy wrappers may only invoke native capabilities, use shared native JSON parse/fail-fast helpers, and return native-owned shapes.
- Anti-pattern: reintroducing TS helpers such as `parseActionDescriptor()`, `parseActionDescriptors()`, `parsePhase()`, `parsePolicy()`, local `JSON_PARSE_FAILED`, or a wrapper-local non-blocking parse logger; this duplicates Rust bridge action parsing semantics under native facade code.
- Evidence target: Rust `hub_bridge_policies` unit tests pass; residue audit bans local TS bridge policy parsers; rebuild native hotpath and llmswitch-core checked outputs; rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-35 Responses response_outbound bridge pipeline must not use TS wrapper

- Trigger: `responses-openai-bridge/response-payload.ts` uses `createBridgeActionState()` / `runBridgeActionPipeline()` or catches/logs bridge action pipeline failures during `response_outbound`.
- Rule: Responses response payload code may resolve policy actions and call `runBridgeActionPipelineWithNative()` directly, then apply the native-returned message. Bridge action execution and error behavior belong to Rust plus required native fail-fast wrappers.
- Anti-pattern: wrapping native bridge execution in the old TS `bridge-actions.ts` mutation shell, or catching bridge action errors and continuing with a partially normalized message.
- Evidence target: residue audit bans TS wrapper and swallowed-error strings in `response-payload.ts`; focused residue Jest, llmswitch-core build, native hotpath rebuild, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-36 Responses request-side bridge action filtering belongs to Rust

- Trigger: `responses-openai-bridge.ts` locally drops `reasoning.extract`, scans messages for tool signals, or filters `tools.normalize-call-ids` / `tools.ensure-placeholders` before request-side bridge action execution.
- Rule: Rust `hub_bridge_policies.rs` must own request-side action filtering through a dedicated plan such as `planResponsesBridgePolicyActionsJson`. TS may resolve the base policy, pass `stage/actions/messages` to the native plan, and then execute the returned actions; it must not keep local action-filter helpers.
- Anti-pattern: treating request-side action filtering as harmless preflight glue in `responses-openai-bridge.ts`, or reintroducing stage-specific TS branches because the final execution still happens in Rust.
- Evidence target: Rust unit covers no-tool-signal filtering and tool-signal keep behavior; required export + TS native facade are fail-fast wired; residue audit bans the old TS helper names and direct action-name filters; native hotpath rebuild, llmswitch-core build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-37 Responses response payload reasoning normalization must not be TS best-effort

- Trigger: `responses-openai-bridge/response-payload.ts` calls `normalizeMessageReasoningTools()` through the shared TS wrapper or catches reasoning normalization failures as best-effort cleanup.
- Rule: response payload projection may call `normalizeMessageReasoningToolsWithNative()` directly and apply the native-returned message object, but reasoning/tool-call extraction and normalization failure behavior belong to Rust. Do not keep a TS wrapper hop plus local swallow path inside response projection.
- Anti-pattern: leaving `try/catch` around reasoning normalization because projection should be "best effort", or reusing the shared wrapper when the runtime path already sits at the native boundary.
- Evidence target: residue audit bans the wrapper import and swallow comment/pattern inside `response-payload.ts`; llmswitch-core build, focused residue + reasoning-normalizer Jest, native hotpath rebuild, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-38 Delete dead TS bridge wrapper layers once direct native consumers are in place

- Trigger: wrapper files like `conversion/bridge-policies.ts` or `conversion/bridge-actions.ts` have zero live production consumers, and the remaining callers can import native bridge facades directly.
- Rule: once the live bridge files already sit at the native boundary, delete the intermediate TS wrapper layer instead of keeping a pass-through file in `src/conversion/`. Add residue gates that assert physical deletion and ban imports from the deleted wrapper path.
- Anti-pattern: keeping a "harmless" TS wrapper because it is thin; thin dead wrappers still preserve duplicate public surfaces and make TS ownership look live.
- Evidence target: repository search shows only deletable self-references, live callers import native bridge facades directly, residue audit bans the deleted files and import paths, llmswitch-core build, native hotpath rebuild, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-39 Delete zero-consumer shared TS native wrappers and update tests to native-return truth

- Trigger: a `conversion/shared/*` wrapper has no production consumers and tests are the only importers, such as `reasoning-tool-normalizer.ts`.
- Rule: delete the wrapper and update tests to call the native facade directly. If the wrapper previously mutated input objects, tests must switch to asserting the native returned object instead of preserving wrapper mutation semantics.
- Anti-pattern: keeping a shared TS wrapper only because a unit test imports it; this makes a deleted runtime surface look alive and can preserve TS behavior such as in-place mutation that native does not promise.
- Evidence target: `rg` shows no production imports, wrapper file is physically deleted, residue/red tests assert deletion, llmswitch-core build, native hotpath rebuild, rustification audit, function-map, mainline, focused Jest, and touched-file diff check pass.

## L93-40 Thin native wrapper is not enough if consumer count is zero

- Trigger: `conversion/shared/*` contains a tiny native wrapper with no production/test imports, for example old `anthropic-message-utils-openai-response.ts`, `chat-output-normalizer.ts`, or `output-content-normalizer.ts`.
- Rule: physically delete zero-consumer wrapper files and update residue gates to require deletion. Keep a TS shell only when it has a current owner-specific runtime consumer or unavoidable IO/type role.
- Anti-pattern: preserving a wrapper because it is already native-backed; dead TS surfaces still count as ownership surface and can be revived as semantic owner later.
- Evidence target: repository search proves zero consumers, llmswitch-core build and focused residue/red tests pass after deletion, native hotpath rebuild, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-41 Zero-consumer native router facades should be deleted, but required Rust exports need separate proof

- Trigger: `sharedmodule/llmswitch-core/src/native/router-hotpath/native-*.ts` facade has no production/test consumers except checked dist, docs/audit baselines, or residue gates.
- Rule: delete the zero-consumer TS facade and add a residue deletion gate. Keep underlying Rust/NAPI exports unless a separate search proves they are absent from required exports, docs/contracts, tests, and runtime bridge paths.
- Anti-pattern: deleting by filename alone. `native-failure-policy.ts` looked low-consumer by path, but is live through `src/modules/llmswitch/bridge/native-exports.ts`, provider failure policy, and executor retry execution.
- Evidence target: `rg` proves no live facade imports/symbol calls; llmswitch-core build, focused residue/red tests, native hotpath rebuild, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-42 Responses captured input sanitize belongs to Rust bridge action plan

- Trigger: `responses-openai-bridge/utils.ts` scans captured Responses input to collect accepted call ids, sanitize `function_call.name`, or drop orphan `function_call_output`.
- Rule: put this in a Rust `hub_bridge_actions` plan such as `sanitizeCapturedResponsesInputJson`; TS bridge utils may only pass input to native and return the native output.
- Anti-pattern: leaving `acceptedCallIds`, `sawFunctionCalls`, `callIdCandidates`, or direct `sanitizeResponsesFunctionName` calls in TS bridge utils because they look like local cleanup.
- Evidence target: Rust unit covers name sanitize, invalid function_call drop, orphan output drop, and no-call output preservation; rebuild native hotpath; llmswitch-core build, residue Jest, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-44 Responses bridge utility policy belongs to Rust bridge action plans

- Trigger: `responses-openai-bridge/utils.ts` contains request/tool passthrough allowlists, generic object picking, metadata extra-field extraction, tool-control field deletion, `data` unwrap loops, or retained request-parameter merge loops.
- Rule: these bridge utility policies belong to Rust/native `hub_bridge_actions` plans. TS may expose owner-specific wrappers that call `pickResponsesRequestParametersWithNative`, `pickResponsesToolPassthroughFieldsWithNative`, `pickResponsesBridgeDecisionMetadataWithNative`, `extractResponsesMetadataExtraFieldsWithNative`, `buildSlimResponsesBridgeContextWithNative`, `stripResponsesToolControlFieldsWithNative`, `unwrapResponsesDataWithNative`, and `mergeRetainedResponsesRequestParametersWithNative`.
- Anti-pattern: treating allowlists or small `delete tool_choice` / unwrap loops as harmless glue in TS; these are request/metadata policy and recreate a second Responses bridge owner.
- Evidence target: residue audit bans TS allowlists, local field picker, tool-control deletion, unwrap loop, captured call-id scanner, and retained-parameter merge loop; Rust `hub_bridge_actions` tests, native hotpath rebuild, llmswitch-core build, responses history contract, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-45 OpenAI chat request normalization should be a single Rust request plan

- Trigger: `openai-message-normalize.ts` maps `messages` or `tools` in TS, calls per-message/per-tool native wrappers, then calls a second message-array native wrapper.
- Rule: move the full request traversal into Rust, e.g. `normalizeOpenaiChatRequestJson`, so TS `normalizeChatRequest()` is a single native facade plus unavoidable env/IO input. Keep native error-object handling outside JSON parse catches so `fail()` is not remapped to `invalid payload`.
- Anti-pattern: calling native for small pieces while TS still owns package-level ordering, array traversal, and validation composition; this preserves TS as the request-normalization owner.
- Evidence target: Rust unit covers normal message/tool normalization and dangling/orphan/synthetic history failure; residue audit bans TS `messages.map`, `tools.map`, and per-piece native wrapper orchestration; rebuild native hotpath before focused Jest so required export availability is real.

## L93-19 Direct Responses data-only SSE needs event-name normalization at provider wire edge

- Trigger: live `/v1/responses` samples contain `data:` JSON frames with `type: "response.*"` and tool data such as `apply_patch` / `function_call`, but the SSE block has no `event:` line; Codex then mis-parses tool-call events even though payload text is present.
- Rule: normalize at the direct provider wire passthrough edge by prefixing `event: <payload.type>` only for data-only JSON blocks whose `type` starts with `response.`. Preserve original `data:` bytes and frame boundaries.
- Anti-pattern: fixing this in `handler-response-sse.ts`, adding `request_id`, inventing terminal events, injecting `[DONE]`, inferring `required_action`, or changing continuation/tool history. Handler/SSE remains transport-only.
- Evidence target: red/green provider direct passthrough test covers data-only `response.output_item.added`, `response.function_call_arguments.done`, and `apply_patch`; release pack/install/global version gates pass; live 5520 client-response sample contains named `response.*` events and no new post-restart `Responses SSE event sequence missing request_id` logs.

## L93-43 Materialized v2 config must not re-enter deleted Virtual Router builder

- Trigger: global `rcc start --snap` logs `virtual-router-builder deleted. Use bootstrapVirtualRouterConfig.` after `RouteCodexHttpServer` has already registered ports.
- Rule: classify this as a materialized-config bootstrap caller bug. `loadRouteCodexConfig()` may already have put provider-file truth into `userConfig.virtualrouter.providers`; server bootstrap must call `compileRouteCodexRuntimeConfigManifest()`, and that Rust manifest compiler must treat materialized `virtualrouter.providers` as the provider config source instead of reloading provider files or calling a stale guard.
- Anti-pattern: making `buildVirtualRouterInputFromUserConfig()` work again, adding a fallback path in `virtual-router-types.ts`, or only installing `rcc` while leaving `routecodex` stale.
- Evidence target: focused runtime provider-merge red reproduces the stale guard, then config/runtime/auth Jest, function-map, mainline, `build:base`, `pack:rcc`, `verify:rcc-release-install`, synchronized global install of `routecodex` and `rcc`, and live `/health` + `/daemon/auth/status` all pass.

## L93-46 Rustification LOC threshold is not total completion

- Trigger: Hub/VR/Chat Process rustification audit reaches a numeric threshold such as `47 files / 6999 LOC`, but remaining non-native TS files have not all been classified and closed.
- Rule: report numeric threshold as a gate/baseline only. Total rustification remains open until every remaining scoped TS file is evidenced as `rust_ssot`, `native_shell_ok`, or `ts_io_shell_ok`, and every `ts_semantic_debt` is moved through an owner-scoped L2 slice.
- Anti-pattern: saying “rustification complete” after an audit threshold or several slice commits without a fresh L1 classification, full closeout gates, and runtime replay evidence when runtime closure is claimed.
- Evidence target: fresh `verify:llmswitch-rustification-audit` file list classified in `docs/loops/rustification/loop-run-log.md`; each semantic debt has owner/function-map/mainline/verification evidence; closeout gates pass; live managed restart/replay is included before runtime completion claims.

## L93-47 Responses store native facade must not choose continuation input source

- Trigger: `responses-conversation-store-native.ts` restore/materialize facade computes `continuationInput` from `entry.input` vs `entry.releasedInputPrefix`, or branches on `entry.continuationOwner === 'direct'`.
- Rule: restore/materialize facades may only pass the complete entry snapshot to native and project returned payload/meta. Rust `shared_responses_conversation_utils.rs` owns input source selection, released-prefix side-channel behavior, direct/relay differences, pending tool output replay, and delta construction.
- Anti-pattern: treating input-source selection as harmless TS wrapper glue because it sits near the native call; it is continuation policy and recreates a second owner.
- Evidence target: residue audit scopes restore/materialize blocks and bans local `continuationInput`, `useReleasedPrefixSideChannelOnly`, direct-owner branching, and `entry.input`/`releasedInputPrefix` selection; focused Responses store + residue Jest, Responses history contract, native build, llmswitch-core build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-48 Native response parser facades are parse-only

- Trigger: `native-hub-pipeline-resp-semantics-parsers.ts` trims alias maps, floors token counts/status codes, validates SSE descriptor enums, validates Responses SSE projection state, normalizes Anthropic stop reasons, filters provider tool names, or checks client protocol enums.
- Rule: TS parser facades may only parse JSON, preserve null/object/array boundary contracts, and type-project Rust-owned native output. Response semantic validation and normalization belong in Rust response owners.
- Anti-pattern: keeping field-level checks in TS because the file is "only a parser"; these checks define protocol semantics and create a second owner beside Rust.
- Evidence target: residue audit bans representative semantic-validation patterns in the parser facade; parser observability still proves JSON parse failures are logged; llmswitch-core build, native build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-49 Req outbound parser facade must not rebuild Rust compat output

- Trigger: `native-hub-pipeline-req-outbound-semantics-parsers.ts` validates `payload`, trims `appliedProfile`, checks `nativeApplied`, or rebuilds the compat output object after Rust returns JSON.
- Rule: req_outbound parser facade may parse JSON and type-project the Rust output. Compat output shape and field semantics belong to Rust req_outbound compat owners.
- Anti-pattern: keeping a small TS output validator/rebuilder because it looks like type safety; this silently defines a second compat contract and can diverge from Rust.
- Evidence target: residue audit bans `payloadRaw`, `appliedProfileRaw.trim()`, `nativeAppliedRaw` boolean validation, local output rebuild, and zero-runtime `parseBoolean()`; parser observability uses a live parser surface; llmswitch-core build, native build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-50 Req inbound parser facade must not validate Rust tool-output snapshot shape

- Trigger: `native-hub-pipeline-req-inbound-semantics-parsers.ts` validates nested `snapshot` / `payload` objects or rebuilds `parseToolOutputSnapshotBuildResult()` output after Rust returns JSON.
- Rule: req_inbound parser facade may parse the native JSON result as a top-level object and type-project it. The `ToolOutputSnapshotBuildResult { snapshot, payload }` contract belongs to Rust `hub_req_inbound_tool_output_snapshot.rs`.
- Anti-pattern: treating nested object/array checks in a parser facade as harmless type safety; they redefine Rust req_inbound snapshot semantics in TS.
- Evidence target: residue audit bans local snapshot/payload validation and rebuild; parser observability and residue Jest pass; llmswitch-core build, native build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-51 Generic system tool guidance text must be native-owned

- Trigger: `guidance/index.ts::buildSystemToolGuidance()` constructs OpenAI tool_calls guidance text with local `lines.push` / `join` logic.
- Rule: move the text owner to Rust, expose a small native function such as `buildSystemToolGuidanceJson`, and keep TS as a fail-fast native facade. Generic guidance must stay apply_patch-policy-free; apply_patch guidance belongs to its dedicated Chat Process/apply_patch owner.
- Anti-pattern: leaving "small static text" in TS because it looks harmless. Tool guidance text affects model behavior and is part of tool governance semantics.
- Evidence target: Rust focused test checks expected tool_calls guidance and absence of apply_patch legacy text; residue audit bans local TS text construction; required native export subtest, native build, llmswitch-core build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-52 MemPalace artifact exclusion must be scanner-audited, not root-config based

- Trigger: checking whether RouteCodex MemoryPalace excludes `dist`, `target`, `node_modules`, `.local-index`, or other generated/local paths.
- Rule: do not add root `mempalace.yaml`; root governance forbids root MemPalace files. Audit the real scanner with MemPalace's own `scan_project(..., respect_gitignore=True)` and keep `scripts/ci/mempalace-scan-artifact-audit.mjs` green.
- Anti-pattern: assuming `.local-index/mempalace/mempalace.yaml` is used by `mempalace mine .`; dry-run shows repo-root mining ignores it and uses auto-detected defaults plus `.gitignore`/built-in skips.
- Evidence target: `npm run verify:mempalace-scan-artifacts` reports `artifactHits=0`; `git check-ignore -v` confirms representative build paths are ignored; historical wing cleanliness should be checked through Chroma `embedding_metadata` by filtering `wing=routecodex` and counting generated/local patterns in `source_file`.

## L93-53 Generic tool guidance augmentation belongs to Rust

- Trigger: `guidance/index.ts` mutates tool descriptions or schema for `shell`, `exec_command`, `update_plan`, `view_image`, MCP resource tools, or Anthropic `input_schema`.
- Rule: keep TS guidance functions as fail-fast native facades. Rust should own the OpenAI/Anthropic generic tool guidance array plans through exports like `augmentOpenAIToolsJson` and `augmentAnthropicToolsJson`.
- Anti-pattern: treating marker strings, `appendOnce`, or `ensureObjectSchema` as harmless TS glue; those strings and schema changes alter model/tool behavior and are Hub/tool governance semantics.
- Evidence target: Rust focused test covers OpenAI and Anthropic tool augmentation while keeping apply_patch untouched; residue audit bans local guidance markers and `.parameters` / `.input_schema` mutation in TS; focused tool-guidance Jest, coverage script, required-export subtest, native build, llmswitch-core build, rustification audit, function-map, mainline, and touched-file diff check pass.

## L93-53 Dangerous exec_command guard rules belong in Rust

- Trigger: `exec-command/validator.ts` or `tool-registry.ts` contains hardcoded destructive-command regex, shell tokenizers, checkout-scope evaluators, shell write-redirection detectors, or reason strings such as `forbidden_git_reset_hard` / `forbidden_git_checkout_scope`.
- Rule: dangerous command policy belongs to Rust `resp_process_stage1_tool_governance_blocks/exec_command_guard.rs` and must be exposed through a fail-fast native facade such as `validateExecCommandGuardJson`; TS may only normalize arguments, read policy files as IO, pass raw policy JSON, and call the native guard.
- Anti-pattern: treating TS regex/tokenizer checks as harmless preflight. They duplicate tool governance semantics, drift from Rust, and can misclassify quoted code operators like `=>` or `1 << 4` as shell writes.
- Evidence target: Rust tests cover blocked reset/checkout/shell writes, policy-file regex matching, shell-wrapper shape/repair, and allowed `/dev/null`, fd merge, heredoc stdin, quoted operators, `sudo`/`ssh`/`scp`/`rsync`; residue audit bans retired TS regex/tokenizer/write-detector/policy-loader/shell-wrapper markers; native build, llmswitch-core tsc, focused tool-registry/exec-command/servertool/residue Jest, rustification audit, function-map, mainline, servertool rust-only, `build:base`, and diff check pass.

## L93-54 Router direct must not preflight Responses provider wire shape

- Trigger: live `/v1/responses` direct request fails before provider dispatch with `evaluateResponsesDirectRouteDecisionJson JSON stringify failed` or `Maximum call stack size exceeded`.
- Rule: `direct-passthrough-payload.ts` is a payload-object boundary only. Same-protocol router direct keeps the current request body as provider wire and uses Virtual Router only for target selection; it must not call native direct-route decision helpers as provider-wire preflight.
- Anti-pattern: validating historical `function_call_output.content`, sanitizing/rebuilding request bodies, replaying raw metadata, or forcing relay from `http-server/index.ts` / direct helper because a Responses body looks provider-wire-invalid.
- Evidence target: route-level red/green proves historical Responses tool-output shape is passed unchanged and HubPipeline is not executed; direct shape gates forbid `findResponsesDirectFunctionCallOutputContentViolation`, `buildDirectPayloadContractError`, and `evaluateResponsesDirectRouteDecisionNative` in live direct helper/server preflight; release proof includes build/pack/install/restart plus live same-shape smoke and no new stack-overflow logs.

## L93-55 Tool argument JSON artifact repair belongs to Rust tool governance

- Trigger: `tools/args-json.ts` contains `<arg_key>/<arg_value>` regex, XML tag stripping, recursive key/value repair, primitive coercion, or parse-failure warning fallback.
- Rule: put tool argument JSON artifact repair in Rust `resp_process_stage1_tool_governance_blocks::json_args` and expose a native facade such as `parseToolArgsJsonWithArtifactRepairJson`. TS `args-json.ts` may only call native and return the parsed native value.
- Anti-pattern: treating small JSON/key repair helpers as shared TS utility code. These helpers decide tool-call arguments and therefore belong to Chat Process tool governance, not a TS convenience layer.
- Evidence target: Rust test covers raw malformed JSON repair, key artifact normalization, injected arg pair extraction, primitive coercion, and invalid/non-string empty-object behavior; residue audit bans TS arg artifact regex/recursive repair; focused tool-registry/residue Jest, native build, llmswitch-core build, rustification audit, function-map, mainline, servertool rust-only, and diff check pass.

## L93-56 Exec command argument normalization belongs to Rust tool governance

- Trigger: `tools/exec-command/normalize.ts` unwraps nested `input` / `arguments`, maps aliases like `command` / `toon` / `script`, repairs command arrays, maps option aliases, or converts `with_escalated_permissions`.
- Rule: put exec_command argument normalization in Rust `resp_process_stage1_tool_governance_blocks::exec_command_args` and expose a native facade such as `normalizeExecCommandArgsJson`. TS `normalize.ts` may only call native and return the native result.
- Anti-pattern: treating alias/compat normalization as harmless TS validation glue. These branches decide the actual tool command and execution options, so they belong to response Chat Process tool governance.
- Evidence target: Rust test covers compat nested unwrap, canonical `cmd` behavior, command aliases/arrays, option aliases, sandbox escalation mapping, metadata repair, and legacy `toon` removal; residue audit bans TS alias/compat normalization; focused exec-command/tool-registry/residue Jest, required native export subtest, native build, llmswitch-core build, rustification audit, function-map, mainline, servertool rust-only, and diff check pass.

## L93-57 Virtual Router hit-log formatting belongs to Rust

- Trigger: `runtime/virtual-router-hit-log.ts` formats `[virtual-router-hit]` lines, summarizes stopMessage state, parses provider keys, assigns route/session colors, truncates continuation scope, builds hit reasons, or maps hit records to telemetry events.
- Rule: put hit-log semantics in Rust `virtual_router_hit_log.rs` and expose required native exports for record creation, event projection, line formatting, provider/key/color helpers, continuation scope, and hit reason. TS may only adapt input shape, call native, parse native JSON, and preserve types.
- Anti-pattern: treating logging as harmless TS diagnostics. Hit logs carry VR route/provider/session/stopMessage truth and can drift from Rust VR decisions if TS owns summarization or display policy.
- Evidence target: Rust unit covers record normalization, stopMessage summary, formatted line omit/color behavior, telemetry event projection, continuation truncation, provider key parsing, and context-aware hit reason; focused hit-log Jest, required-export subtest, residue audit, native build, llmswitch-core build, rustification audit, function-map, mainline, and diff check pass.

## L93-58 Virtual Router contracts must stay type-only

- Trigger: `virtual-router-contracts.ts` exports runtime route constants, route priority arrays, or `VirtualRouterError` values while also serving as the TS bridge type surface.
- Rule: keep `virtual-router-contracts.ts` as type/interface surface only. Route/default/priority semantics belong to Rust Virtual Router; small TS runtime error shells belong in an existing native-linked shell such as `native-router-hotpath-policy.ts`.
- Anti-pattern: leaving unused TS constants in contracts because no caller imports them. Unused exported constants still create a second public semantic owner and keep the file in non-native runtime debt.
- Evidence target: source-only search shows no production consumer of retired TS constants; residue audit bans constants and error value exports in contracts; llmswitch-core build, native build, focused routing/error/residue Jest, rustification audit, function-map, mainline, and diff check pass.

## L93-59 Responses store preflight guards belong to Rust, TS projects errors only

- Trigger: `responses-conversation-store.ts` locally checks missing `requestId`, missing `response.id`, missing `responseId`, missing payload, or missing `tool_outputs` before calling native store plans.
- Rule: put capture/record/resume preflight decisions in Rust `shared_responses_conversation_utils.rs` and expose a native plan such as `planResponsesConversationPreflightJson`. TS store may execute Map/FS/global singleton/timer IO, non-blocking logging, and `ProviderProtocolError` projection only.
- Anti-pattern: classifying basic guard branches as harmless IO shell. These guards decide legal Responses continuation save/restore entry behavior and must share the same Rust owner as the rest of the continuation store plans.
- Evidence target: Rust focused preflight test covers positive/negative capture, record, and resume branches; required native export subtest, focused responses store/residue Jest, responses history protocol contract, native build, llmswitch-core build, rustification audit, function-map, mainline, and diff check pass.

## L93-60 Responses store allowContinuation decision belongs to Rust

- Trigger: `responses-conversation-store.ts::recordResponse()` locally flips `entry.allowContinuation` after appending assistant blocks by scanning pending tool calls or keeping extra TS scope-based continuation branches alive.
- Rule: move the record-time continuation decision into Rust `shared_responses_conversation_utils.rs` and expose a native plan such as `planResponsesRecordContinuationFlagJson`. TS may collect canonical `pendingToolCallIds` from `entry.input` and project the returned boolean only.
- Anti-pattern: treating `hasPendingToolCalls` or "already true so keep true" branches as harmless wrapper logic. This is continuation policy on the `rct-06` save edge and creates a second owner beside Rust.
- Evidence target: Rust focused test covers pending ids => allow, existing true => keep true, and no-pending => false; focused responses store/residue Jest, responses history protocol contract, required native export subtest, native build, llmswitch-core build, rustification audit, function-map, mainline, and diff check pass.

## L93-61 Responses store capture entry construction belongs to Rust

- Trigger: `responses-conversation-store.ts::captureRequestContext()` locally builds `ConversationEntry` fields from prepared payload/context, token plans, scope keys, timestamps, and `shouldAllowContinuation`.
- Rule: put capture-time entry construction in Rust `shared_responses_conversation_utils.rs` behind a plan such as `planResponsesCapturedEntryJson`. TS may compute current scope candidates, execute cleanup plans, write the returned entry to Maps, and flush persistence.
- Anti-pattern: leaving `basePayload/input/tools/allowContinuation/provider/session/conversation` assignment in TS because the individual helpers are native. The assignment itself is the saved continuation truth on `rct-06`.
- Evidence target: Rust focused test covers full payload/context entry and minimal fallback entry; focused store/residue Jest, required native export subtest, responses history contract, native build, llmswitch-core build, rustification audit, function-map, mainline, and diff check pass.

## L93-62 Responses store dead wrapper cleanup stops at TS facade/export boundary unless map/wiki also move

- Trigger: source-only search shows a `conversion.responses.store` wrapper/native facade/required export trio has zero active production consumer, but the underlying Rust NAPI symbol still appears in `lib.rs`, mainline docs, or feature-map-facing public surfaces.
- Rule: split the cleanup. First delete the dead TS wrapper, TS native facade helper, and `native-router-hotpath-loader.ts` entry after source-only consumer proof. Only delete the underlying Rust NAPI shell in a later slice that also updates `lib.rs`, function/mainline/wiki surfaces, and any public export contract that still names it.
- Anti-pattern: treating every dead TS facade cleanup as permission to delete the matching Rust NAPI symbol immediately. That mixes a safe dead-surface slice with a broader contract/doc migration slice and needlessly expands blast radius.
- Evidence target: source-only `rg` excluding generated artifacts shows no active consumer; native build, llmswitch-core build, focused responses store/residue Jest, required export subtest, responses history contract, rustification audit, function-map gate, mainline gate, and diff check pass. If the Rust NAPI shell is kept, report the remaining `lib.rs` / wiki / mainline surface explicitly as boundary debt.

## L93-63 Responses store IO-shell closeout needs a residue gate, not only a written classification

- Trigger: `responses-conversation-store.ts` still has many branches after native plans are introduced, but the branches appear to be Map/FS/timer/logging/index mutation rather than continuation semantics.
- Rule: close this as `ts_io_shell_ok/native_plan_io_shell_ok` only when a residue gate requires all store semantics to go through native plans and explicitly bans semantic drift markers: manual continuation owner comparisons, manual scope-key string builders, direct `allowContinuation=true/false`, retired `pick/prepare/shouldAllow` wrappers, local response output-to-input conversion, and history repair/released-prefix fallback.
- Anti-pattern: saying "it is IO-only" after visual inspection. The file is large enough that IO-shell classification must be machine-locked, otherwise TS continuation policy can quietly return.
- Evidence target: focused residue Jest for `responses conversation store TS surface must stay a native-plan IO shell`, responses history protocol contract, native build, llmswitch-core build, rustification audit, function-map gate, mainline gate, and touched-file diff check pass. Runtime closure still requires managed restart and same-entry replay.

## L93-64 Responses handler must consume request metadata only through the bridge

- Trigger: `responses-handler.ts` imports `readRequestBodyMetadata()` or derives session/conversation request context directly from the HTTP payload.
- Rule: put body metadata read/merge, session/conversation derivation, and payload metadata stripping behind `responses-request-bridge.ts` (`prepareResponsesHandlerRuntimeForHttp`). The handler may consume the bridge-returned `requestBodyMetadata` for request-start logging/projection only.
- Anti-pattern: moving body metadata into `MetadataCenter`, reading it twice in handler and bridge, or bypassing the bridge because the handler needs logs. Request body `metadata` is protocol data-plane input; the bridge owns preprocessing for this entry.
- Evidence target: `verify:responses-handler-single-bridge-surface`, focused Responses bridge/handler Jest, and tsc pass; the request-context normalization spec proves body `metadata.session_id/conversation_id` becomes bridge request context while prepared provider payload metadata is stripped.

## L93-65 ProviderProtocol boundary conflict must not exclude VR providers

- Trigger: retry/execution planning sees `ERR_PROVIDER_PROTOCOL_MISMATCH` or a message/reason containing `runtime_control.providerProtocol conflict`.
- Rule: classify it as a protocol boundary failure. Project/fail fast without retry/reroute, without `retrySwitchPlan`, and with `excludedCurrentProvider: false`; provider route pool availability must stay unchanged.
- Anti-pattern: treating a protocol-boundary mismatch as provider failure, excluding the selected provider, or mutating Virtual Router route-hit state to recover. This creates false provider unavailability from a request/protocol contract error.
- Evidence target: `verify:route-metadata-preselected-route-owner` plus focused `request-executor-provider-failure-plan.spec.ts` regression `protocol boundary conflicts never exclude providers from VR route hits`.

## L93-66 Function-map required_tests entries must be files

- Trigger: `verify:function-map-test-coverage-integrity` fails with `EISDIR` or crashes while reading a `required_tests` entry.
- Rule: `required_tests` must point to concrete test/script files. Directories belong in `allowed_paths`, docs, or owner notes, not the required-test list consumed by integrity gates.
- Anti-pattern: adding a source directory as a coverage anchor to prove ownership; it may look descriptive but breaks test coverage integrity and makes the feature gate unreadable.
- Evidence target: `verify:function-map-test-coverage-integrity`, `verify:function-map-required-tests-bidir`, and `verify:function-map-compile-gate` pass.

## L93-67 Install-global build tier must stay build:min

- Trigger: `verify:build-script-tiering` complains that `scripts/install-global.sh` runs `npm run build:dev`.
- Rule: global install's default build tier is `npm run build:min`; skip-build/help messages must name the same tier.
- Anti-pattern: using `build:dev` as a faster install default. It diverges from the release/install tier gate and can install artifacts that did not pass the intended minimum build path.
- Evidence target: `npm run verify:build-script-tiering` passes before architecture-ci is claimed.

## L93-68 Superseded: managed restart is not a single-port lifecycle action

- Trigger: `routecodex restart --port <target>` reports success but live `/health.version` stays old, or the command fails because a sibling port in the same config group is stale/missing.
- Superseded: the old "version lag -> spawn `start --restart --port <target>` adoption" rule is wrong because it creates a new start/session takeover instead of restarting inside the original managed session. Use L93-120 for current restart semantics.
- Superseded on 2026-07-13 by L93-141. The old single-port target rule is invalid for the aggregate server runtime; a port is now only an aggregate instance locator.
- Anti-pattern:
  - treating configured aggregate member ports as unrelated standalone servers;
  - spawning `start --restart` from `restart.ts` to adopt a lagging runtime;
  - rewriting shims back to global-package-first order after install, which can leave `routecodex` and `rcc` on different binaries.
- Evidence target: restart focused Jest + probe-host Jest pass; root `tsc` and `build:min` pass; `install-release-snapshot.mjs` updates `~/.rcc/install/current`; snapshot-first shims make both `routecodex --version` and `rcc --version` match; managed `routecodex restart --port 5555` moves live `/health.version` to the current version; same-entry `/v1/responses` live probe completes with stopless contract intact.

## L93-120 Restart command must restart inside the original session

- Trigger: `rcc restart` or `routecodex restart --port <port>` stops the originally started server/session and launches a new `start --restart` takeover path.
- Rule: restart is one request to the existing aggregate process/session. Use `/daemon/restart-process` when authorized, otherwise send `SIGUSR2` once to the aggregate listener PID identity. If a managed start parent exists, the child exits with code 75 and the original parent supervisor re-resolves the current install entry and restarts the child in the same session.
- Anti-pattern: comparing live `/health.version` in `restart.ts` and spawning `start --restart` from the restart command, or treating restart as stop-then-start/new-session adoption.
- Evidence target: restart focused Jest must prove a lagging live version still requests SIGUSR2/in-session restart and does not require spawn/start context; `tests/cli/restart-command.probe-host.spec.ts`, `tsc`, runtime lifecycle/function-map gates, installed dist scan for deleted adoption strings, and authorized live restart/health evidence.
- Live evidence pattern: after global install, run installed `rcc restart --port <locator-port> --host <host>` once, record the original `start --snap` parent PID before/after, prove the new server child is still under the same parent, and verify every configured member port. Do not report this class of fix without global install/version/health evidence.
- Guard: after any concurrent or later install, re-run the installed dist scan and version/health checks. A prior good install is not final evidence if `~/.rcc/install/current` changes before reporting.

## L93-121 Lifecycle logs must serialize nested errors recursively

- Trigger: `~/.rcc/logs/process-lifecycle.jsonl` shows lifecycle failure records such as `port_http_shutdown result=failed` with `details.error={}`.
- Rule: fix `src/utils/process-lifecycle-logger.ts` recursive serialization, not each caller. `details.error`, nested `cause`, and extra fields such as `code` must be preserved in JSONL.
- Anti-pattern: adding caller-specific `message` fields in `port-utils` or treating `{}` as enough because a later host succeeds. That leaves other lifecycle failures silent.
- Evidence target: red/green `tests/utils/process-lifecycle-logger.spec.ts`, focused caller test such as `tests/cli/port-utils.spec.ts`, TypeScript, runtime lifecycle/function-map/build gates, global install, and an installed-dist logger probe proving `details.error.message/cause/code` are present.

## L93-69 Minimal TS surface needs a manifest gate

- Trigger: Hub/VR rustification review says remaining TypeScript is minimal or cannot shrink further.
- Rule: do not rely on chat text, note prose, or aggregate LOC thresholds. Maintain a source/doc-only manifest for every current non-native production TS file with classification, owner feature, minimum TS role, forbidden semantics, and a hard `cannotShrinkFurtherBecause` reason. A verifier must compare the manifest against the live `llmswitch-rustification-audit` file set and fail on unclassified files, stale manifest entries, invalid classifications, or weak/empty reasons.
- Anti-pattern: saying "type shell" or "IO shell" without a machine-readable owner/reason, or saying "impossible" without naming the concrete blocker such as Node host IO, public TS declaration surface, JSON-string NAPI ABI, process-global singleton, filesystem/timer state, or diagnostics sink.
- Evidence target: `npm run verify:llmswitch-minimal-ts-surface`, `npm run verify:llmswitch-rustification-audit`, function-map/mainline gates, and architecture-ci longtail wiring pass.

## L93-70 VR contract type surface must be facade-scoped

- Trigger: Hub pipeline, host-effects, hit-log, tests, or server/runtime code imports `native/router-hotpath/virtual-router-contracts.ts` directly for types.
- Rule: upper layers import VR types from the adjacent native facade that owns the boundary: runtime/route types from `native-virtual-router-runtime.ts`, bootstrap/config types from `native-virtual-router-bootstrap-config.ts`, provider error/success types from `native-provider-runtime-ingress.ts`. Direct contracts imports are allowed only inside same-layer `native/router-hotpath` facades and root package `export type *`.
- Anti-pattern: creating a new TS barrel to hide contracts. That keeps the TS surface alive without a real owner boundary. Use existing native facades or wait for Rust-generated TS declarations.
- Evidence target: residue audit blocks upper-layer direct imports; `verify:llmswitch-core-tsc`, llmswitch-core build, minimal TS surface gate, rustification audit, function-map/mainline gates, `verify:vr-no-ts-runtime`, and `verify:vr-no-fallback-semantics` pass.

## L93-71 Config JSON removal means no migration/fallback surface remains

- Trigger: config loader rustification or provider config closeout mentions `config.json`, `config.v1.json`, `config.v2.json`, TOML shadow, or JSON migration.
- Rule: root runtime config is `config.toml`; provider runtime config is `provider/<id>/config.v2.toml`. JSON config paths are removed legacy paths: reject user `config.json` fail-fast before any init/load/write action, reject retired provider JSON in v2 loader, and do not keep migration commands that copy JSON into `~/.rcc`.
- Rule: `rcc init --config <path>` must call the user config codec path detector before profile validation, provider-source validation, provider listing, config-state inspection, or writes. Missing `.json` files are still rejected; never write TOML content to a `.json` path.
- Anti-pattern: keeping `migrate-user-config`, JSON-to-TOML shadow parity tests, provider path fallback `config.v2.toml || config.v2.json`, normal init tests using `config.json`, or admin/provider-update code that says `config.v2.toml/config.v2.json`. These preserve JSON support even if the main loader prefers TOML.
- Evidence target: production `src` and llmswitch-core source scans have no `config.json`/`config.v1.json`/`config.v2.json` runtime refs; focused config/provider-update Jest passes; root TS and Rust runtime materialization cargo test pass; `~/.rcc` cleanup scan confirms no old JSON config files remain.

## L93-72 Provider-update must not be a v1 config generator

- Trigger: `provider update` or provider maintenance touches model sync, provider config paths, or `virtualrouter.providers`.
- Rule: provider update reads provider configs by `providerId` plus provider root through `loadProviderConfigsV2()` and writes the canonical `provider/<id>/config.v2.toml` with `writeProviderConfigFile()`. The only JSON it may read/write is non-runtime cache/control data such as `models-latest.json` or `blacklist.json`.
- Anti-pattern: keeping a `config-builder` that emits `version: 1.0.0` / `virtualrouter.providers`, accepting provider input JSON as runtime config, accepting arbitrary provider `--config <file>` paths, or seeding models from stale input after upstream fetch failure.
- Evidence target: grep gate blocks production `virtualrouter.providers` outside materialization validation and blocks old config filenames/migration/shadow refs; provider-update focused Jest and root TS compile pass.

## L93-73 HubPipeline config must consume the full Rust runtime manifest

- Trigger: server runtime calls a VR-only bootstrap helper and passes only `virtualRouterBootstrapInput` to HubPipeline while Rust also emits `pipelineRuntimeConfig`.
- Rule: compile RouteCodex runtime config once through Rust `RouteCodexRuntimeManifest`, carry the exact manifest from bootstrap into setup through a non-enumerable runtime carrier, and pass `manifest.pipelineRuntimeConfig` into `HubPipelineConfig`. Missing manifest is a fail-fast runtime error.
- Anti-pattern: recompiling config in setup as a convenience, using a TS fallback to rebuild pipeline config, or letting server runtime consume VR-only bootstrap input after Rust manifest materialization exists.
- Evidence target: runtime setup test asserts HubPipeline receives Rust `pipelineRuntimeConfig`; grep gate bans server runtime imports of VR-only bootstrap builders; root/llmswitch TS compile, Rust materialization cargo test, native build, function-map/mainline gates, VR no-TS-runtime, minimal TS surface, rustification audit, architecture longtail, managed restart, `/health`, and same-entry live probe pass before claiming runtime path closeout.

## L93-74 Config TOML codec rustification must split semantic owner from IO shells

- Trigger: config rustification touches `toml-basic.ts`, `user-config-codec.ts`, `provider-config-codec.ts`, or writer/loader tests.
- Rule: create a dedicated Rust `config.toml_codec` owner for TOML parse/serialize semantics. Keep user/provider codecs as TS shells only for file IO, TOML-only path rejection, and provider coercion; they must delegate TOML data semantics through the native TOML shell.
- Anti-pattern: marking `user_config_codec` or `provider_config_codec` as Rust owner while their canonical TS IO/format functions still live in TS; function-map gates will catch missing canonical builder definitions or duplicate owners.
- Evidence target: Rust TOML codec tests, config blackbox/Jest, `verify:config-ssot`, required native exports, root and llmswitch-core TS compile, native hotpath build, function-map gate, minimal TS surface, rustification audit, and diff check pass.

## L93-75 Comment-preserving TOML patch belongs with the TOML Rust owner

- Trigger: config writer rustification touches `toml-comment-preserving.ts`, `updateUserConfigStringScalar`, or admin/CLI config scalar updates.
- Rule: move async real-file config writes and user scalar patch read/write into Rust once pre-wire TS/native blackbox proves equivalence. Keep only injected-host boundaries in TS, such as `writeProviderConfigFileSync(fsImpl)`, until the caller no longer requires a supplied synchronous filesystem.
- Anti-pattern: treating comment-preserving patching as writer-owned TS because it preserves layout. Table scanning, key matching, scalar escaping, comment spacing, missing-table creation, and insertion order are TOML semantics and must not be duplicated in TS.
- Evidence target: pre-wire writer parity tests must compare Rust writer output with old TS writer output using temp config files; post-wire `config-writer`/`toml-comment-preserving`/grep Jest, Rust `config_file_codec` and `config_toml_codec` tests, native hotpath build with required exports, root TS compile, function-map gate, minimal TS surface, and diff check pass.

## L93-76 Provider config coercion is separate from provider config file IO

- Trigger: config rustification touches `provider-config-codec.ts`, `provider-v2-loader.ts`, or `coerceProviderConfigV2FromParsed`.
- Rule: keep provider config file read/path rejection/TOML parse shell in TS, but move parsed-record to `ProviderConfigV2` coercion into Rust with its own feature id such as `config.provider_config_coercion`. The TS function may keep the public name but must delegate to native.
- Anti-pattern: marking the whole provider config codec as Rust-owned while TS still owns `detectProviderConfigFormat`, `parseProviderConfigText`, or `decodeProviderConfigFile`; this breaks canonical-builder owner gates. Split the coercion feature instead.
- Evidence target: Rust provider codec tests cover root `providerId`, provider-node `id`, fallback id, default version, and null cases; provider loader blackbox passes through native; `verify:config-ssot`, native hotpath build, required native exports, function-map gate, root/llmswitch TS compile, minimal TS surface, rustification audit, diff check, and cargo fmt check pass.

## L93-77 Responses record save must prefer the active request label

- Trigger: `/v1/responses` fails with `RESPONSES_STORE_MISSING_REQUEST_CONTEXT` after VR routes `router-*` to a provider-labeled request such as `orangeai.key1.*`.
- Rule: request capture/rebind uses the current provider request label; response save must pass that same active label into `publishResponsesRecordPlanJson` and the Rust planner must prefer it over stale `requestTruth.requestId`. `requestTruth` may provide scope fields, not override the save key after rebind.
- Anti-pattern: reintroducing tests or code that prefer `requestTruth.requestId` for `recordArgs.requestId`; this records under the old router id after the store was re-bound and recreates missing context.
- Evidence target: `verify:responses-history-protocol-contract` statically requires `publish_responses_record_plan_uses_current_request_id_before_stale_request_truth` and forbids the old requestTruth-first test names; provider-response focused Jest must assert active provider id is recorded and old router id is not.

## L93-78 Provider v2 loader planning belongs with provider config Rust owner

- Trigger: config rustification touches `provider-v2-loader.ts`, provider directory scanning, `config.v2*.toml` ordering, or provider id resolution for base/suffixed config files.
- Rule: Rust `config_provider_codec.rs` owns provider root loading end-to-end: provider directory scan, `config.v2*.toml` file selection, file read, TOML parse bridge, coercion, duplicate provider id detection, and base/suffixed identity validation. TS `provider-v2-loader.ts` may only resolve the root path and call the native bridge.
- Provider-update CLI/tooling/admin/init reads (`provider update`, `sync-models`, `probe-context`, `inspect`, `doctor`, `change`, config admin provider listing/detail views, `rcc init --list-current-providers`, init maintenance, and init provider merge reads) must consume `loadProviderConfigsV2()` root-loader output by providerId/root. Direct provider `--config <file>` decode shells and init-local provider file decoders are removed and must not return.
- Anti-pattern: leaving `fs/promises`, `readdir`, `readFile`, local `loadProviderConfigV2`, duplicate accumulation, `isProviderConfigV2FileName`, local sort planning, `config.v2.<suffix>.toml` explicit-id enforcement, base-file directory id injection, or providerId CLI direct decode in TS because the loader/CLI is "mostly IO". Those are provider config contract semantics and must be Rust-owned for VR/pipeline config decoupling.
- Evidence target: Rust provider codec tests cover root loading, retired JSON rejection, duplicate provider ids, file planning, and base/suffixed identity rules; focused `provider-v2-loader` and provider-update Jest prove providerId reads pass through the native root loader; grep gate blocks root-loading and CLI direct-decode semantics from returning to TS; required native exports, `build:native-hotpath`, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, and diff check pass.

## L93-79 User config validation belongs with Rust runtime materialization

- Trigger: config rustification touches `user-config-loader.ts`, `collectV2ConfigSourceErrors`, `validateV2ConfigSources`, or primary `routingPolicyGroup` resolution.
- Rule: keep file/provider/profile orchestration in TS, but move v2 source-layout validation and primary routingPolicyGroup resolution into Rust `runtime_config_materialization.rs`. TS may clone/normalize, load provider configs, call the Rust manifest compiler, and build provider profiles.
- Anti-pattern: leaving allowed-field sets, default route explicit-target checks, implicit-v2 detection, router-port precedence, active-group fallback, or single-group fallback in TS because the loader already calls the Rust manifest compiler. Those choices determine config contract truth and must share the Rust materialization owner.
- Evidence target: Rust runtime materialization tests cover invalid v2 source layout and routingPolicyGroup precedence/fallback; focused routecodex loader/runtime materialization/grep Jest passes through native; required native exports, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and crate-local cargo fmt pass.

## L93-80 Materialized provider extraction belongs with Rust runtime materialization

- Trigger: config rustification touches `user-config-loader.ts`, materialized `virtualrouter.providers`, or provider config selection before `compileRouteCodexRuntimeConfigManifest`.
- Rule: keep provider file IO in TS, but move materialized provider extraction into Rust `runtime_config_materialization.rs`. TS may choose Rust-extracted materialized providers or call `loadProviderConfigsV2`; it must not build `ProviderConfigV2` records from `virtualrouter.providers` itself.
- Anti-pattern: leaving provider id trimming, provider object validation, provider.id mismatch checks, id injection, or materialized provider record construction in TS because `virtualrouter.providers` is already a materialized runtime copy. Those checks are runtime materialization contract semantics.
- Evidence target: Rust runtime materialization tests cover materialized provider extraction and provider.id mismatch; focused routecodex loader/runtime materialization/grep Jest passes through native; required native exports, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and crate-local cargo fmt pass.

## L93-81 Implicit v2 normalization belongs with Rust runtime materialization

- Trigger: config rustification touches implicit v2 config detection, `virtualrouterMode`, or `user-config-loader.ts` source normalization.
- Rule: normalize implicit v2 runtime source in Rust `runtime_config_materialization.rs` via `normalize_routecodex_v2_runtime_source_json`; TS may only call the native bridge before validation/materialization.
- Anti-pattern: reintroducing TS `isImplicitV2Config`, local `virtualrouterMode` mutation, or pre-validation shape mutation in `user-config-loader.ts`. That makes Rust manifest compilation consume a TS-normalized truth and preserves duplicate config semantics.
- Evidence target: Rust runtime materialization tests cover implicit v2 normalization; focused loader/runtime/grep Jest passes through native; required native exports, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and crate-local cargo fmt pass.

## L93-82 Manifest-to-userConfig materialization belongs with Rust runtime materialization

- Trigger: config rustification touches `materializeRouteCodexConfig`, `manifest.virtualRouterBootstrapInput`, or final `userConfig.virtualrouter` runtime projection.
- Rule: apply the Rust runtime manifest back to user config through `materialize_routecodex_user_config_from_manifest_json`. TS may call the native bridge and then build provider profiles; it must not reconstruct `providers`, `routing`, `forwarders`, or `applyPatch` locally.
- Anti-pattern: reintroducing `userConfig.virtualrouter = { ...vrBase, providers: v2Input.providers, routing: v2Input.routing }`, no-op materializers, or private TS primary-group resolvers in loader/bootstrap code. These preserve duplicate config materialization semantics after Rust manifest compilation.
- Evidence target: Rust runtime materialization test covers manifest application; config grep gate blocks local rebuild; required native exports, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and crate-local cargo fmt pass.

## L93-83 Provider profile projection belongs with Rust runtime materialization

- Trigger: config loader closeout touches `buildProviderProfiles`, provider protocol aliases, provider auth projection, provider transport projection, compatibility field rejection, or provider metadata extraction.
- Rule: project provider profiles from materialized runtime config in Rust via `build_routecodex_provider_profiles_json`. TS `provider-profile-loader.ts` may keep public type shell and `extractApiKeyEntries()` for already-built auth configs only.
- Anti-pattern: restoring TS `resolveProtocol`, `protocolAliases`, `extractTransport`, `extractAuth`, `normalizeAuthType`, `extractCompatProfile`, `extractMetadata`, or `collectProviderNodes` because provider profiles are "just loader output". Those are provider/runtime config semantics and must share the Rust materialization owner.
- Evidence target: Rust runtime materialization tests cover provider profile projection and rejected shapes; provider-profile Jest and config loader Jest pass through native; config grep gate blocks old helpers; required native exports, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and crate-local cargo fmt pass.

## L93-84 Forwarder profile projection belongs with Rust runtime materialization

- Trigger: config/profile closeout touches `buildForwarderProfiles`, forwarder id/protocol/model/strategy/stickyKey/targets/weights parsing, or unknown provider target validation.
- Rule: project forwarder profiles from config in Rust via `build_routecodex_forwarder_profiles_json`. TS `provider-profile-loader.ts` may only expose `buildForwarderProfiles()` as a native shell; `forwarder-types.ts` may keep schema types and the id-prefix helper.
- Anti-pattern: restoring TS `collectForwarderNodes`, `parseForwarderTargets`, `parseForwarderWeights`, `pickString`, `pickNumber`, `pickPositiveInt`, or inline error strings like `transportOverride is not supported` because forwarder profiles are "just adapter output". These are config/runtime projection semantics and must stay with the Rust materialization owner.
- Evidence target: Rust runtime materialization tests and `tests/providers/forwarder-selection.spec.ts` pass through native; config grep gate blocks TS helper resurrection; required native exports, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and crate-local cargo fmt pass.

## L93-85 Dead config guard files must be physically deleted after callers are gone

- Trigger: config rustification leaves a TS file whose only runtime behavior is throwing stale-caller errors, or leaves a shadow compare/helper file after JSON/v1/shadow support has been removed.
- Rule: if `rg` shows no production/test imports and the only remaining refs are maps or historical docs, delete the file and remove its function/verification map owner. Guard files without callers are dead compatibility surface, not safety.
- Anti-pattern: keeping `virtual-router-types.ts` as a fail-fast guard after all callers use Rust manifest compilation, or keeping `config-semantic-compare.ts` because old TOML shadow plans mentioned it. This invites future resurrection of deleted JSON/shadow/TS materialization paths.
- Evidence target: live source/test/architecture import grep is zero; root `tsc`, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, diff check, and note/MEMORY updates pass.

## L93-86 Config path/auth shells must fail fast and expose no compatibility helper surface

- Trigger: config closeout touches `user-data-paths.ts`, `unified-config-paths.ts`, `auth-file-resolver.ts`, or config template/materializer helper files.
- Rule: keep Node path/env/fs IO in TS only where Node owns it, but delete public `ForRead`, legacy `.routecodex`, `llmsShadow`, directory-scan metadata, batch auth fallback, no-caller template, and re-export helper surfaces. Retired `.routecodex` env roots and retired provider JSON files must fail fast; do not silently redirect or ignore them.
- Anti-pattern: treating old env/config names as harmless compatibility redirects, keeping default TOML template files with no caller, or preserving re-export files "for import stability" after all callers can import the real owner.
- Evidence target: source/test grep shows no public legacy helper/shadow/template imports; focused path/provider loader tests, Rust provider config codec tests, native hotpath build, root `tsc`, `verify:config-ssot`, function-map gate, minimal TS surface, rustification audit, and diff check pass.

## L93-87 Responses apply_patch gates must cover the full Hub response path

- Trigger: `/v1/responses` live sample shows provider `output[].type=custom_tool_call` with `name=apply_patch`, but RouteCodex client projection emits empty `output` or `responses_empty_output`.
- Rule: do not rely only on helper/unit filters such as `build_chat_response_from_responses`. The required gate must run a full Rust Hub response-path fixture from provider Responses payload through `RespChatProcess` and `RespOutbound`, with `requestSemantics.tools.clientToolsRaw` present, and assert non-empty client-visible `custom_tool_call`.
- Anti-pattern: adding a regression test in Rust but not wiring it into `verify:hub-response-responses-chat-projection`, or documenting it only in notes while `function-map` / `verification-map` still list only helper-level tests.
- Evidence target: `npm run verify:hub-response-responses-chat-projection` runs both `build_chat_response_from_responses` and `response_path_preserves_existing_responses_custom_tool_call`; `verify:function-map-compile-gate` passes with `hub_pipeline_lib/tests.rs` listed for both Responses chat projection and Responses client projection features.

## L93-89 apply_patch live replay must prove VR route and SSE projection together

- Trigger: apply_patch appears broken even after Rust Hub response-path tests pass, especially when live `/v1/responses` returns JSON-wrapped `{"patch":"..."}` in `response.created`, `response.in_progress`, `response.completed`, or `response.done`.
- Rule: closeout must prove both halves on the global release: Rust Virtual Router routes explicit `tool_choice:{type:"custom",name:"apply_patch"}` to `tools` with reason `tools:apply_patch-tool-choice`, and client-visible Responses SSE contains raw patch text in `custom_tool_call.input` with zero JSON-wrapped `arguments`/delta leaks.
- Anti-pattern: declaring apply_patch fixed from route evidence only, from helper projection tests only, from repo-local build only, or without global install + health version match + live replay.
- Evidence target: global release version and all configured ports `/health.version` match; `scripts/tests/apply-patch-freeform-10000-online.mjs` passes against the target `/v1/responses` endpoint with `functionArgumentPatchLeakCount=0`; live log contains `tools:apply_patch-tool-choice` for the replay request id.

## L93-90 RCC user path resolution needs explicit env snapshot and Node join parity

- Trigger: rustifying `src/config/user-data-paths.ts`, `resolveRccUserDir`, `resolveRccPath`, or path-resolution NAPI exports.
- Rule: Rust owns RCC user dir env precedence, retired `~/.routecodex` rejection, default `~/.rcc`, and RCC subpath joining. TS may pass current `process.env` into NAPI and publish env values, but must not reimplement user-dir/subpath resolution.
- Rule: NAPI must accept explicit `RCC_HOME`, `ROUTECODEX_USER_DIR`, and `ROUTECODEX_HOME` snapshots; Jest/VM `process.env` mutation is not a reliable source for Rust `std::env`.
- Rule: Rust subpath joining must match old Node `path.join()` lexical behavior for `..` and absolute-looking segment strings. `PathBuf::push` alone is not equivalent because absolute segments replace the base.
- Anti-pattern: importing aggregate `bridge.js` from `user-data-paths.ts` and creating an ESM cycle, relying on Rust process env for JS-mutated env, or silently redirecting retired `.routecodex` user roots to `.rcc`.
- Evidence target: Rust `resolve_rcc` tests cover default, retired env fail-fast, normal subpath, and Node-like normalization; focused `tests/config/user-data-paths.spec.ts` passes through rebuilt native binding; function/verification maps point `config.path_resolution_surface` at Rust and list `verify:config-path-resolution-rust`.

## L93-91 Config file path resolution must be blackbox-compared before native shell wiring

- Trigger: rustifying `UnifiedConfigPathResolver`, `resolveRouteCodexConfigPath`, config path env precedence, config directory scan, or TOML-only path rejection.
- Rule: first expose an unconnected native resolver and add blackbox tests that compare current TS output/errors with native output/errors on temp fixtures; only after that passes may `unified-config-paths.ts` be collapsed to a native shell.
- Rule: Rust owns `ROUTECODEX_CONFIG_PATH` / `ROUTECODEX_CONFIG` precedence, preferred path, `config.toml` default, cwd/base candidates, `~/.rcc/config.toml`, `~/.rcc/config/`, TOML-only rejection, and directory scan. TS must not rebuild the candidate list or scan directories.
- Anti-pattern: wiring native resolver before old/new comparison, treating real `~/.rcc/config.toml` edits as compatibility fixes, or leaving a TS candidate-builder "for readability" after native wiring.
- Evidence target: `tests/config/unified-config-paths.spec.ts` includes native comparison cases; `verify:config-path-resolution-rust` runs both `resolve_rcc` and `resolve_routecodex_config_path`; focused config loader/grep tests, function-map gate, native build, TS compile, minimal TS surface, rustification audit, and diff check pass.

## L93-88 Config loader wiring requires blackbox compatibility before live startup

- Trigger: config loader, provider loader, VR materialization, Hub Pipeline config, or runtime manifest rustification is about to be connected to runtime/startup.
- Rule: before wiring, run module blackbox tests against fixtures matching existing `~/.rcc/config.toml` and provider `config.v2.toml` shapes and compare old/new materialized output. Existing user config is a compatibility sample; do not edit it to fit new code.
- Rule: blackbox incomplete means not wired. Do not start/restart live server, do not claim runtime closure, and do not modify real `~/.rcc` config files unless Jason explicitly authorizes a config migration/cleanup.
- Anti-pattern: making code pass by changing Jason's real config, connecting a Rust loader because unit tests compile, then discovering startup failure in live. Fix the loader/materializer owner first.
- Evidence target: fixture blackbox comparison passes, focused config gates pass, then connection diff is made, then managed `routecodex restart --port <port>` + `/health` + same-entry probe only after blackbox is green and live verification is in scope.

## L93-83 Responses store missing-context debug must use configured providers and preserve raw request body

- Trigger: `/v1/responses` fails with `RESPONSES_STORE_MISSING_REQUEST_CONTEXT`, especially after provider request-id relabeling or provider reroute.
- Rule: verify protocol/provider behavior against configured RouteCodex providers such as `cc` / `asxs` using keys from the login shell environment; do not use `api.openai.com` as a substitute for configured provider behavior. Treat provider behavior and local store failures as separate evidence streams.
- Rule: executor metadata must preserve the original `/v1/responses` request body in `__raw_request_body` before Hub rewrites it to provider wire shape. Chat Process request-context capture must use that raw entry payload plus the active provider request id, so response save can find the same request-map entry.
- Anti-pattern: inventing a standard `previous_response_id` missing-context client error, using OpenAI.com probes when the configured provider is `cc`/`asxs`, or letting capture read only provider wire payload after Hub rewrite.
- Evidence target: configured provider probe records status/body; `request-executor.metadata-center.contract.spec.ts` proves raw request preservation and active provider request-id capture; `verify:responses-history-protocol-contract`, function-map gate, mainline call-map gate, mainline manifest sync, and root/llmswitch TS compile pass before claiming code-level closure.

## L93-92 Config single-file decode must be pre-wire compared before native shell wiring

- Trigger: rustifying `src/config/user-config-codec.ts`, `src/config/provider-config-codec.ts`, `parseUserConfigText`, `parseProviderConfigText`, or single-file decode helpers.
- Rule: expose Rust file/text decode first and blackbox-compare current TS output/errors with native output/errors on temp TOML/retired JSON fixtures before replacing TS callers.
- Rule: after wiring, TS config codec files may keep TOML path rejection, Node async file IO, injected `fsImpl` sync support, types, and return shape only. TOML text decode must call `decodeRouteCodexUserConfigTextSync` / `decodeRouteCodexProviderConfigTextSync`; no TS `parseTomlRecord` + `isRecord` fallback is allowed.
- Anti-pattern: wiring a file-only native decoder while existing callers use raw text parse APIs, or bypassing injected `fsImpl` sync callers by forcing native file reads. That breaks test/CLI IO boundaries and leaves duplicate TS parse semantics.
- Evidence target: `tests/config/config-file-codec-rust.spec.ts` covers file and text parity plus JSON rejection; Rust `config_file_codec` tests, native required exports, focused config codec/loader/writer Jest, root TS compile, function-map gate, minimal TS surface, and diff check pass before committing.

## L93-93 RouteCodex config loader native wiring requires full loader parity and env snapshot roots

- Trigger: rustifying `src/config/routecodex-config-loader.ts`, `loadRouteCodexConfig`, provider-root selection inside runtime config load, or connecting a native config loader to startup/runtime.
- Rule: expose the native loader first and compare old TS vs native output/errors on temp fixtures before wiring. Required cases: explicit TOML config, auto-resolved config path with changed `RCC_HOME`, `ROUTECODEX_PROVIDER_DIR` / `RCC_PROVIDER_DIR` override, legacy v1 field rejection, and retired JSON rejection.
- Rule: native loader must receive JS env snapshots for `cwd`, `HOME`, `ROUTECODEX_CONFIG_PATH`, `ROUTECODEX_CONFIG`, `RCC_HOME`, `ROUTECODEX_USER_DIR`, and `ROUTECODEX_HOME`. Default provider root is `<resolved rcc user dir>/provider`; `plan_provider_config_root_for_host(None)` is not a default-root resolver.
- Anti-pattern: compiling Rust and immediately wiring `loadRouteCodexConfig`, relying on Rust `std::env` to see Jest/JS env mutations, reading relative `""` as provider root, or editing Jason's real `~/.rcc` config/provider files to compensate for loader incompatibility.
- Evidence target: `tests/config/routecodex-config-loader-rust.spec.ts` passes before wiring and after wiring; focused loader/provider/materialization/config-codec gates, Rust `config_file_codec` and `runtime_config_materialization`, native hotpath build, root TS compile, function-map/mainline map gates, minimal TS surface, rustification audit, and diff check pass. Live restart is separate and only after offline parity is green and live verification is in scope.

## L93-94 Hub Pipeline literal zero TS requires reference-lock shrink, not thin-shell acceptance

- Trigger: a Hub Pipeline rustification audit reports no `ts_semantic_debt`, but remaining files are `type_shell_ok`, `ts_io_shell_ok`, `diagnostic_io_ok`, or native facades and Jason asks for pure Rust / zero TS.
- Rule: first audit and close public/runtime reference locks: package barrels, ambient `.d.ts`, required dist outputs, bridge dynamic loads, mainline caller/callee edges, direct source/test imports, and docs/gates that still name the TS module. Only after those locks move to Rust/generated/server-boundary owners can the TS file be deleted.
- Anti-pattern: declaring "zero TS" because semantics are Rust-owned while `src/index.ts`, `src/types/llmswitch-core.d.ts`, `scripts/lib/build-core-utils.mjs`, `response-converter.ts`, `state-integrations.ts`, or mainline edges still publish/load the TS file.
- Evidence target: narrow source/test/script import audit excludes samples/docs/generated outputs; `verify:llmswitch-minimal-ts-surface`, `verify:llmswitch-rustification-audit`, `verify:function-map-compile-gate`, and a red import-graph gate for the deletion candidate pass before removing the file.

## L93-95 VR/Hub config runtime artifacts must come from Rust manifest

- Trigger: runtime code needs router-port provider allowlists, ErrorErr05 route tiers, primary-exhausted/default-pool tiers, or Hub Pipeline config after `RouteCodexRuntimeManifest` exists.
- Rule: emit runtime policy inputs from Rust `runtime_config_materialization.rs` into `pipelineRuntimeConfig` and make server runtime consume those artifacts. `virtualRouterBootstrapInput` is for VR bootstrap; `pipelineRuntimeConfig.routingProviderIds` and `pipelineRuntimeConfig.routingTiersByRoute` are for Hub/runtime policy helpers.
- Rule: `fwd.*` route targets must not be parsed as provider id `fwd`; Rust must expand referenced forwarder targets to real provider ids while preserving route tiers with the original forwarder target strings.
- Anti-pattern: reading `server.userConfig.virtualrouter.routingPolicyGroups` in runtime request/error paths to rebuild allowlists or tiers, or keeping a startup check over old `virtualrouter.routing` after v2 Rust validation exists.
- Evidence target: Rust runtime materialization tests prove `routingProviderIds` and `routingTiersByRoute`; http-server tests consume only `pipelineRuntimeConfig`; grep gate blocks old TS helper names and old startup `virtualrouter.routing` validation; function/mainline/verification maps bind config -> VR and config -> Hub artifact edges.

## L93-96 Servertool/Hub boundary closeout must remove direct TS type/carrier imports

- Trigger: rustifying the overlap between `sharedmodule/llmswitch-core/src/servertool/**` and `sharedmodule/llmswitch-core/src/conversion/hub/**`.
- Rule: servertool must not import Hub TS type files, Hub timing helpers, Hub stage recorders, or Hub MetadataCenter writer. Keep local servertool boundary types and servertool-local MetadataCenter IO; semantic decisions must come from Rust/native plans.
- Rule: Hub must read bound MetadataCenter through Hub `metadata-center-runtime-control-writer.ts`, not through `servertool/metadata-center-carrier.ts`. The only tolerated Hub -> servertool import is the Rust-planned response-stage IO entrypoint, and the gate must name that exception explicitly.
- Anti-pattern: "thin shell" imports from Hub type/timing modules, or moving the coupling into another TS facade without a red gate. That leaves the modules unable to shrink independently toward pure Rust.
- Evidence target: source grep shows zero servertool `../conversion/hub/**` imports; Hub grep shows no `servertool/metadata-center-carrier`; `verify:servertool-rust-only` has a boundary check; focused Rust servertool tests, focused response-stage/servertool audit tests, llmswitch/root TS compile, function-map/mainline gates, minimal TS surface, rustification audit, and diff check pass.

## L93-97 Routing instruction state TS bridge must not own persistence predicates

- Trigger: rustifying servertool/VR routing instruction state, especially `native-virtual-router-routing-state.ts` persistence key checks, empty-state checks, or sync-vs-async save decisions.
- Rule: Rust `routing_state_store.rs` owns persistent-key, empty-state, and sync-save predicates; TS may call native NAPI and execute store IO only. Add the native capability to required exports before running Jest that depends on it.
- Rule: if routing instruction parsing uses JS env overrides in tests, pass explicit env snapshots into Rust NAPI; do not rely on Rust process env seeing Jest/VM `process.env` mutations.
- Anti-pattern: leaving `key.startsWith('session:') || key.startsWith('tmux:')`, `allowedProviders.size === 0`, or stopMessage/preCommand empty checks in TS because the wrapper is "thin". That keeps state truth split between Rust and TS.
- Evidence target: Rust focused tests for `routing_state_store` and stopMessage instruction parse/apply, rebuilt native hotpath, routing-instructions Jest, `verify:servertool-rust-only` with `servertool-routing-instruction-state-native-only`, llmswitch/root TS compile, function-map/mainline gates, minimal TS surface, rustification audit, and diff check pass.

## L93-98 Request log color must use request session identity before tmux/route keys

- Trigger: one tmux/Codex session shows different colors across `▶ started`, `[virtual-router-hit]`, `[usage]`, or response logs, especially when routes differ (`longcontext`, `thinking`, `search`) or `logSessionColorKey` was synthesized.
- Rule: resolve log color from current request session identity first: `sessionId` / `session_id`, then `conversationId` / `conversation_id`, then `logSessionColorKey`, and only then tmux aliases (`clientTmuxSessionId` / `tmuxSessionId` / `rccSessionClientTmuxSessionId`). Route color may format route internals before host recolor, but must not survive as the outer request/session color.
- Rule: error/failure response lines are still response logs. If the request id has a registered session color, `❌ failed` must use that same session color; red is only allowed when no request/session color exists.
- Anti-pattern: letting tmux alias, request id, route name, or Rust route palette decide the outer color when a real request `sessionId` exists. That makes one session id appear as multiple sessions and breaks log correlation.
- Gate rule: tests must not keep legacy tmux-first expectations. Request start, usage, VR hit, completed, and failed logs all need explicit negative assertions against tmux/route/red colors when a session color is registered.
- Evidence target: red test with `{ sessionId, clientTmuxSessionId }` proves native returns `sessionId`; focused request-start/request-log/usage logger/VR hit-log tests, root TS compile, function-map/mainline gates, VR no-TS gate, llmswitch rustification audit, Rust `virtual_router_hit_log`, release/global install, `/health.version`, and installed-runtime smoke pass before claiming fixed.

## L93-99 Codex Responses color session truth is top-level client_metadata

- Trigger: request-start color is correct but `[virtual-router-hit]`, `[usage]`, or response lines still use route/tmux colors for the same visible Codex session.
- Rule: `/v1/responses` session truth must be read from `body.client_metadata.session_id` / `body.client_metadata.thread_id` at both handler and executor metadata entrypoints. `turn_id` is per-turn and must never become the session color key.
- Anti-pattern: only teaching handler request-start about `client_metadata`, or extracting session color from `body.metadata` / tmux scope later in executor/VR/usage. That creates a split where the first log line is session-colored but downstream runtime logs are not.
- Evidence target: executor metadata regression proves `metadata.sessionId`, `conversationId`, `logSessionColorKey`, and MetadataCenter request truth use `client_metadata.session_id` and reject `turn_id`; installed-runtime smoke after global install proves request, failed response, and `[virtual-router-hit]` share the same ANSI color.

## L93-100 Servertool engine shell helper exports are dead bridge surface

- Trigger: shrinking `engine-orchestration-shell.ts` after Rust owns preflight, match, stopless, and postflight decisions.
- Rule: helper functions such as `runEnginePreflight`, `recordServertoolEngineMatchSkipped`, `recordServertoolEngineMatchHit`, and `runServertoolEnginePostflight` must be private when production callers only need the response-stage/main orchestration entry. Rewrite tests to exercise them through the public mainline shell and mock Rust/native plans, then add a rust-only gate forbidding public helper exports.
- Anti-pattern: keeping helper exports only because tests import them directly. That freezes TS bridge surface and makes internal shell code look like runtime API.
- Evidence target: source grep shows only intended runtime exports remain; focused engine preflight/observation/response-stage Jest, root TS compile, `verify:servertool-rust-only`, minimal TS surface, rustification audit, function-map gate, and diff check pass.

## L93-101 Servertool queue registry helper exports are dead bridge surface

- Trigger: shrinking `execution-queue-shell.ts` after Rust/native owns registry lookup, dispatch planning, and execution-loop decisions.
- Rule: `getServerToolHandler` and queue-local native type aliases are internal queue details, not public servertool API. Keep them private and exercise registry behavior through `runServertoolIoExecutionQueue` or native/Rust tests; add `verify:servertool-rust-only` and source assertions forbidding `export const getServerToolHandler` plus queue-local type re-exports.
- Anti-pattern: preserving a public helper or type re-export so registry/casing/tests can call it directly. That turns a test seam into a stable TS bridge and blocks later shell collapse.
- Evidence target: red focused Jest/gate fail on `export const getServerToolHandler`, then focused queue/registry Jest, root TS compile, `verify:servertool-rust-only`, minimal TS surface, rustification audit, function-map gate, `build:base`, and diff check pass.

## L93-102 CLI-owned servertools must not stay in the server-side skeleton registry

- Trigger: stopmessage/web_search/vision_auto are CLI-owned or planned CLI-owned, and the server-side skeleton still exposes default registry/profile/noop entries.
- Rule: default `servertool_skeleton_config.rs` must keep `internalTools`, primary auto-hook order, progress flow map, and followup profiles empty. Missing followup profile means `outcomeMode: "skip"` + `noFollowup: true`; it must not infer reenter/client-inject behavior from retired flow ids.
- Rule: `continue_execution` noop dispatch/outcome/effect bridge is dead business. Physically remove the Rust/TS bridge and make `verify:servertool-rust-only` forbid `planServertoolNoop*`, `noopResult`, `noopEffectPlan`, and default skeleton registry/profile resurrection markers.
- Anti-pattern: keeping server-side default registration because the CLI path still exists. CLI projection truth belongs to `servertool-core` CLI/outcome contracts; server-side registry defaults must not shadow it.
- Evidence target: Rust `servertool_skeleton_config`, `chat_servertool_orchestration`, and `execution_loop_effect_contract` focused tests pass; native hotpath build passes; focused servertool Jest passes; `verify:servertool-rust-only`, root TS compile, function-map/mainline gates, and diff check pass before committing.

## L93-103 Server-side servertool runtime actions are retired

- Trigger: stopmessage is CLI-owned, web_search/vision are moving CLI, and remaining server-side servertool execution/followup business is unwanted.
- Rule: delete the server-side runtime-action bridge instead of migrating dead business. `provider-response.ts` may only fail fast on non-empty Rust `servertoolRuntimeActions`; CLI-owned tool projection must already be represented by Rust client projection before TS provider-response sees it.
- Rule: Rust/NAPI planner and projection exports for provider-response servertool runtime actions must stay absent, including `planProviderResponseServertoolRuntimeActionsJson`, `resolveProviderResponsePostServertoolEffectJson`, and `projectPostServertoolHubRespOutbound04ClientSemanticJson`.
- Anti-pattern: keeping a TS engine/queue/stage/progress shell or post-servertool projection helper as a "thin" compatibility path. That preserves a second runtime owner after server-side tools were intentionally removed.
- Evidence target: `verify:servertool-rust-only` checks deleted paths, active runtime refs, provider-response fail-fast markers, and retired native exports; then run sharedmodule/root TypeScript, focused provider-response/servertool Jest, Rust hub pipeline/resp outbound tests, native build, function/mainline/manifest/wiki gates, minimal TS surface, rustification audit, and diff check.

## L93-104 llmswitch-core zero-TS closeout must clean scripts and package gates

- Trigger: closing a `sharedmodule/llmswitch-core/src` minimal TS surface entry to literal zero TS, especially Hub Pipeline, provider-response, responses-store, servertool, or VR facade/type shells.
- Rule: scan and update package-level scripts/tests/docs in addition to `src`. Old scripts that import deleted `dist/conversion/hub/**`, `dist/router/virtual-router/**`, or deleted TS facade paths are dead gates once native/host boundaries own the flow; remove them or point the gate at the native wrapper/blackbox owner.
- Rule: VR closeout scripts must use native engine APIs plus explicit `metadataCenterSnapshot`. Do not preserve JS `quotaView`, TS dependency injection, or weights-only synthesized-pool assertions as compatibility behavior; those are old bridge semantics, not Rust truth.
- Rule: zero-TS completion requires a dedicated gate proving manifest entries are zero, no non-native production TS files remain, rustification audit non-native counts are zero, and the 12 target paths cannot reappear as hand-authored production TS.
- Anti-pattern: declaring zero TS after `src` deletion while package scripts, docs, tests, or generated host `.js/.d.ts` still publish/import old TS names; accepting stale tests by reintroducing TS facades or fallback behavior.
- Evidence target: `verify-llmswitch-zero-ts-closeout`, `verify:llmswitch-minimal-ts-surface`, `verify:llmswitch-rustification-audit`, `verify:servertool-rust-only`, sharedmodule/root TypeScript, architecture gates, Hub module blackbox, VR direct/health/pool scripts, focused provider-response/responses-store/hub residue Jest, native hotpath build, build:base, diff check, and targeted stale import scan all pass before committing.

## L93-105 Native HubPipeline handles must not be object facades

- Trigger: shrinking server runtime HubPipeline/Virtual Router bridge after llmswitch-core zero-TS closeout, especially `hub-pipeline-handle.ts`, router-direct, and diagnostics routes.
- Rule: server runtime HubPipeline values are native string handles only. Do not restore object facades with `.handle`, `.getVirtualRouter()`, `.route()`, `.getStatus()`, or `.diagnoseRoute()`. Call Rust native route/status/diagnose APIs directly from the string handle.
- Rule: tests that install a native handle must also install the matching `pipelineRuntimeConfigByRoutingPolicyGroup` artifact, because router-port `allowedProviders` comes from the Rust materialized runtime config, not from the deleted facade object.
- Anti-pattern: making tests pass by accepting object-shaped handles or by dropping `allowedProviders` assertions. That reintroduces TS bridge semantics or weakens the router-port metadata contract.
- Evidence target: zero-TS gate npm script, minimal TS surface, rustification audit, root TypeScript, focused direct-passthrough native-handle/routing metadata tests, direct server contract tests, function-map/mainline gates, and diff check pass before committing.

## L93-106 Zero-prod TS wrappers still need direct-native contract proof before deletion

- Trigger: deleting llmswitch-core TS wrappers that have `prodImportRefs=0`, especially native event-payload or bootstrap wrappers kept alive by tests/docs.
- Rule: zero production import is only a deletion candidate signal. Before physical deletion, move tests/maps to direct Rust/NAPI evidence and run the focused contract test. If direct NAPI does not satisfy the existing regression contract, revert the deletion attempt and leave the wrapper until the Rust contract is reconciled.
- Rule: after deleting a wrapper, add a residue gate that both requires the source path to stay absent and scans source roots for old wrapper path/name revival.
- Anti-pattern: weakening or rewriting a regression to match current direct NAPI output just to delete the TS shell. That converts reference closeout into behavioral drift.
- Evidence target: exact refs split by prod/host/package/test/docs, focused direct-native Jest/Rust contract tests, function/verification map updates, residue deleted-path gate, llmswitch zero-ts/minimal/rustification gates, sharedmodule/root TypeScript, and diff check.

## L93-107 Request-side stopless injection requires request truth session

- Trigger: request-stage shell deletion or direct-native request-stage tests expect provider payload stopless guidance from a real entry payload containing `<**stopless:on**>`.
- Rule: Rust request governance injects stopless guidance only when `runtimeControl.stopMessageEnabled=true` and `requestTruth.sessionId` exists. Test the real entry path by putting session truth in `body.metadata.sessionId` or the corresponding request-truth source; do not hand-write nested `runtimeControl.stopMessage` to compensate for missing session truth.
- Anti-pattern: treating missing stopless guidance as a reason to restore `hub-pipeline-execute-request-stage.ts` or add a TS bridge patch. First prove the MetadataCenter snapshot includes request truth and runtime control.
- Evidence target: focused request-stage/provider payload Jest passes through direct native helper; strict shell audit and exact source/package scan show the retired request-stage shell remains absent.

## L93-108 Native split facade deletion must move active gates to aggregate owners

- Trigger: deleting llmswitch-core native split wrapper files such as `native-shared-conversion-semantics-*.ts`, `native-hub-bridge-action-semantics-tools-*.ts`, or `native-virtual-router-engine-proxy.ts` after the aggregate wrapper already owns native-call glue.
- Rule: active tests, function-map allowed paths, required dist import tests, and architecture denylist entries must point at the aggregate owner (`native-shared-conversion-semantics.ts`, `native-hub-bridge-action-semantics.ts`, or `native-virtual-router-runtime.ts`) or assert the old split path is absent. Do not keep active imports/readFile checks for deleted split files.
- Rule: if local `git diff --check` is intercepted by the rtk diff wrapper and exits 2 with no findings, do not report it as passed. Record the wrapper limitation and run an equivalent targeted trailing-whitespace scan on changed files.
- Anti-pattern: making a deleted split facade pass by relying on stale `dist/native-*-split.js`, historical goal docs, or backup function-map references. Those are not current runtime/source truth.
- Evidence target: strict TS shell audit drops shell count, rustification audit remains `nonNativeFileCount=0`, minimal TS surface stays zero, function-map compile gate passes, focused residue/import tests pass, native required export tests import aggregate dist owner, stale active reference scan excludes generated/history paths, and targeted whitespace scan has zero matches when `git diff --check` is unavailable.

## L93-109 Single-import conversion facades should collapse into the importing bridge

- Trigger: strict shell audit shows a conversion helper such as `responses-tool-utils.ts` has exactly one production importer and no host text references.
- Rule: move only local type aliases and native-call/mutation glue into the importing bridge, then delete the facade and any coverage-only script that imported its dist path. The importing bridge must call the Rust/NAPI owner directly and residue gates must assert the old facade and script stay physically absent.

## L93-110 Aggregate native wrapper deletion must retarget scripts to host native exports

- Trigger: deleting a production aggregate native wrapper such as `native-shared-conversion-semantics.ts` after its split facades are already gone.
- Rule: do not create a replacement production aggregate or compatibility subpath. Move test-only direct native glue under `tests/sharedmodule/helpers/`; move script/runtime needs to `src/modules/llmswitch/bridge/native-exports.ts/js/d.ts` or direct `.node` binding checks. Required-export tests should assert the packaged `.node` symbol or an existing live aggregate owner, not the retired dist barrel.
- Anti-pattern: keeping `sharedmodule/llmswitch-core/dist/native/router-hotpath/native-shared-conversion-semantics.js` as a required build output or Jest mock target after the source wrapper is gone.
- Evidence target: strict shell audit shell count drops, rustification baseline drops, active import/require scan has no retired aggregate subpath outside absent locks/test-only helper comments, `build:base` passes, and focused scripts/Jest import the host native owner.
- Anti-pattern: preserving a separate TS facade because a coverage script or red test imports it. That keeps a second TS bridge surface alive after Rust already owns the semantics.
- Evidence target: focused red/residue Jest, sharedmodule TypeScript, strict TS shell audit count drop, rustification audit `nonNativeFileCount=0`, minimal TS surface, function-map gate, stale active import scan, and targeted whitespace scan pass before commit.

## L93-110 Local shutdown requires caller provenance, not only localhost

- Trigger: managed multi-port RouteCodex process stops unexpectedly and logs show `shutdown_route result=accepted` followed by `signal_received`, especially with empty `callerPid` / `callerCmd`.
- Rule: `/shutdown` is lifecycle control-plane, not a public local convenience endpoint. The route must reject localhost requests that lack `x-routecodex-stop-caller-*` provenance headers, and exception paths must fail visibly instead of ACKing then self-terminating.
- Rule: legitimate callers (`stop`, explicit restart/port takeover, start supervisor cleanup, install/verify scripts that own a child runtime) must attach caller pid/timestamp/cwd/cmd headers. Tests must prove anonymous local POST does not call `process.kill` and CLI callers include headers.
- Anti-pattern: accepting any localhost POST because apikey middleware exempts `/shutdown`, or using a bare `curl -X POST /shutdown` in verification scripts. That makes unrelated local tools able to stop all listeners in a single multi-port process.
- Evidence target: focused `/shutdown` provenance Jest, `port-utils` and `stop-command` header assertions, runtime lifecycle gate, TypeScript compile, and live `/health` read-only check after tests.

## L93-111 Root re-export can hide a dead production TS chain

- Trigger: strict TS shell audit reports a production importer only because `src/index.ts` re-exports a function, while repository host/runtime source has no call site for that export.
- Rule: treat the root re-export as a reference lock, not proof of runtime use. Verify host/runtime consumers, package subpath exports, scripts, and tests separately; if only coverage/dev scripts and facade-specific tests remain, remove the re-export, facade, dependent dead helper, and those dead verification surfaces together.
- Rule: replace facade-content tests with residue gates that require the deleted paths and retired scripts to stay absent. Keep direct Rust/native owner tests as the behavior evidence.
- Anti-pattern: preserving hundreds of TS LOC because `index.ts` imports them, or keeping coverage scripts whose only purpose is to exercise an otherwise unused facade.
- Evidence target: `build:base`, root/core TypeScript, focused residue Jest, strict TS shell audit count drop, rustification audit with `nonNativeFileCount=0`, minimal TS surface zero, function-map gate, active reference scan, and diff check.

## L93-112 Req-inbound aggregate deletion must move map edges first

- Trigger: deleting `native-hub-pipeline-req-inbound-semantics.ts` after production import refs drop to zero.
- Rule: move function-map/mainline-call-map/verification-map edges to the real host bridge (`src/modules/llmswitch/bridge/native-exports.ts`) and Rust owner (`hub_req_inbound_context_capture.rs`) before deleting the file; keep direct evidence in `tests/sharedmodule/helpers/`.
- Anti-pattern: deleting the file while mainline edges still point at `captureReqInboundResponsesContextSnapshotWithNative`; architecture gates fail even if source imports are gone.
- Evidence target: strict shell audit count drops, function-map compile gate passes, focused req_inbound/native-required/residue Jest passes, active import scan has no retired aggregate imports, and `build:base` passes.

## L93-113 Co-located required-export lists need scoped parsing

- Trigger: deleting a standalone required-export TS shell and moving `REQUIRED_NATIVE_HOTPATH_EXPORTS` into a loader that also contains runtime helper code.
- Rule: build scripts must parse only the declared export array block, not all quoted strings in the file. Full-file string scanning turns ordinary loader literals such as `"string"` or stack parsing fragments into fake required native exports.
- Anti-pattern: fixing native export validation by weakening the required export list or ignoring missing entries. The contract must remain strict; only the source extraction scope changes.
- Evidence target: `node sharedmodule/llmswitch-core/scripts/build-native-hotpath.mjs`, `npm run build:base`, strict TS shell audit, rustification audit, and focused native-required/residue tests pass after the standalone required-export shell is deleted.

## L93-114 Package-export facades require full external reference contraction

- Trigger: deleting a llmswitch-core TS facade that is still exposed through `sharedmodule/llmswitch-core/package.json` exports or `src/types/rcc-llmswitch-core.d.ts`, such as `runtime/virtual-router-hit-log.ts`.
- Rule: contract refs must be closed in this order: production imports, host package subpath imports, ambient declarations, tests, package exports, function/mainline/verification maps, residue gates, then physical delete. Mainline edges must point at real Rust/NAPI bridge symbols, not deleted facade API names.
- Anti-pattern: moving tests but leaving package exports or ambient declarations; this keeps external consumers able to recreate the stale TS shell surface even after source deletion.
- Evidence target: source-tracked exact ref scan shows old subpath only in forbidden/doc/history gates, strict TS shell audit count drops, rustification baseline drops, package JSON has no retired export, focused direct-native tests pass, and `build:base` passes.

## L93-115 Native helper shell deletion should collapse into the loader, not another production facade

- Trigger: a production TS native helper shell has only another llmswitch-core TS shell as importer, and the helper only wraps NAPI binding/stringify/parse/fail-fast plumbing.
- Rule: move the minimal binding helper exports into the existing loader owner (`native-router-hotpath-loader.ts`), update the caller to import from the loader, and move helper-shaped test APIs to `tests/sharedmodule/helpers/*-direct-native.ts`.
- Rule: legacy script probes must import the loader/dist binding directly; do not keep a production shell just so a script can call old facade functions.
- Anti-pattern: replacing one deleted shell with a new production facade or preserving old dist import paths in scripts after source deletion.
- Evidence target: source tracked scan has no active import of the retired helper shell outside absent-file locks, strict shell audit count drops, rustification baseline drops, focused Jest passes, script probes pass after `build:base`, and `git diff --check` passes.

## L93-116 Native exports wrapper fan-out must shrink after Rust ownership is proven

- Trigger: `src/modules/llmswitch/bridge/native-exports.ts` contains a large hand-written `*WithNative` block whose symbols have no production callers after Rust/NAPI owns servertool semantics.
- Rule: classify every wrapper by git-tracked caller. If callers are only generated package wrapper, tests, scripts, docs, or stale JS mirror, first migrate tests/scripts to direct NAPI or active servertool-core wrappers, then delete the production export block and generated declaration surface.
- Rule: if an internal active bridge still needs the underlying Rust capability, keep only a private non-exported helper with a non-public name. Do not restore the retired public `*WithNative` symbol just to satisfy a local call.
- Rule: build must regenerate tracked JS mirrors before exact scan. Residue gates should check both `native-exports.ts` and `native-exports.js` for the old block marker and representative deleted export names.
- Supersedes old June lessons that required preserving `runServertoolResponseStageWithNative` as a marker; after server-side runtime actions are retired, that name is a deleted surface and should appear only in history or forbidden-token tests.
- Anti-pattern: leaving generated `servertool-wrapper.d.ts` declarations for functions no longer exported at runtime, or making release verification require deleted package exports.
- Evidence target: `build:base`, `verify:servertool-rust-only`, strict llmswitch shell audit, rustification/minimal TS audits, focused servertool/residue Jest, function-map/mainline/deleted-path/thin-wrapper/fallback gates, exact source scan, and diff check pass before commit.

## L93-117 Host bridge JS mirror deletion must distinguish ESM specifiers from files

- Trigger: deleting tracked `src/modules/llmswitch/**/*.js` side-by-side emit mirrors while canonical host bridge source remains `.ts` and imports use ESM `.js` specifiers.
- Rule: first prove every mirror has a same-name `.ts` owner and Jest/build resolve `.js` specifiers to TS or `dist` output. Then delete the checked-in mirror files and make residue gates assert no tracked-and-existing host bridge `.js` source artifact under `src/modules/llmswitch`.
- Rule: update gates that read mirror file contents to read `.ts` canonical source or assert mirror absence. Do not keep a source mirror just for Jest mocks, absolute mock paths, or syntax checks.
- Anti-pattern: treating `import './bridge/foo.js'` in TS or tests as proof that `src/modules/llmswitch/bridge/foo.js` must stay checked in. That specifier is the ESM output contract; the checked-in source mirror is a separate stale artifact.
- Evidence target: focused residue Jest, focused tests that import/mock deleted mirror paths, strict shell reference audit, architecture deleted-path/thin-wrapper/function-map gates, root `tsc`, `build:base`, and `git diff --check` pass.

## L93-118 Provider runtime ingress must update HubPipeline handles

- Trigger: provider error/success reports mutate a standalone `VirtualRouterEngineProxy` but live routing uses Rust `HubPipelineEngine` handles, causing provider health/failure windows not to affect the active HubPipeline route state.
- Rule: provider runtime ingress must dispatch normalized provider error/success events to both the registered VR proxy registry and the HubPipeline handle registry. Match by `routecodexRoutingPolicyGroup` when present, then by provider ownership; do not add TS polling, bridge-side health mirrors, or executor/provider local reroute state.
- Rule: provider success may clear only the exact provider key that succeeded. Do not also clear `runtimeKey` or provider-key aliases; backup success with `runtimeKey` pointing at the primary must not erase the primary failure window.
- Anti-pattern: fixing repeated primary hits by changing config, persisting cooldown, clearing all providers on success, or adding a TS retry/cooldown compatibility path.
- Evidence target: Rust provider runtime ingress/HubPipeline handle tests, focused `provider-runtime-ingress` and `hub-pipeline-runtime-ingress` Jest, error-pipeline gates, provider-failure blackbox, native build, llmswitch zero-TS audits, function/mainline gates, root/core TypeScript, `build:base`, and diff check pass before commit.

## L93-119 WebUI routing display must read pool targets, not route names

- Trigger: WebUI multi-port routing view looks correct by route label but selects or displays targets from the route name instead of configured `routing.<route>[][].targets[]`.
- Rule: WebUI is a config editor surface only. Target summaries must parse `pool.targets[]` (`providerId` / `provider` / `target` / string) and show live-apply evidence from `portApply` / `selfReload`; non-active config edits must say saved-only.
- Anti-pattern: route-name-derived target IDs such as `default.default`, "restart required" success text after a live apply response, or frontend fixtures using deleted `config.json` user config truth.
- Evidence target: frontend target-chain tests, daemon-admin live port apply test, `test:webui`, `verify:config-ssot`, function/runtime lifecycle gates, `build:webui`, and a mock-browser visual smoke for hierarchy/background/radius.

## L93-120 Responses direct SSE dry-run carrier leak is not an SSE transport bug

- Trigger: Codex `/v1/responses` SSE reports `stream closed before response.completed` or `SSE stream missing from pipeline result`, while provider samples finish in ~5-20ms with `routecodex.pipeline_dry_run` and `stoppedBeforeProviderSend=true`.
- Rule: first compare provider-request truth and metadata truth. If providerRequest is current Codex SSE but metadata contains an older curl/sample dry-run `requestId`, `clientHeaders.x-routecodex-dry-run`, or `__rccDryRunSerialized`, fix provider runtime metadata isolation; do not patch handler-response-sse or synthesize terminal frames.
- Fix owner: `ResponsesProvider.processIncomingDirect()` must build context from the current request runtime carrier. It must not read provider instance `getCurrentRuntimeMetadata()` before current direct request metadata is applied.
- Evidence target: red/green direct passthrough test proving a dry-run turn does not make the next non-dry-run direct SSE skip upstream, plus pipeline-dry-run tests, TS compile, function/mainline gates, `build:base`, and live installed same-entry replay before claiming production closure.

## L93-121 Dry-run closure must prove both final request and black-box response conversion

- Trigger: request-side or response-side provider bugs are fixed but evidence only shows config, source code, or a provider/log snippet.
- Rule: request bugs close only when provider-request dry-run returns `object=routecodex.pipeline_dry_run`, `kind=provider_request`, `evidence.stoppedBeforeProviderSend=true`, and final `providerRequest.body/endpoint/headers`. Response bugs close only when `npm run dry-run:codex-response -- --sample <provider-response.json>` writes `response-dry-run.json` through `convertProviderResponseIfNeeded`.
- Protocol rule: for same-protocol direct chat entries backed by Responses providers, infer response dry-run protocol from provider payload truth (`object=response`) before path/entry fallback. A sample under `openai-chat` can still require `openai-responses` conversion.
- Anti-pattern: treating ordinary upstream response content from a provider-request dry-run as success; using serialized live `sseStream` without `bodyText/raw/text/sseBodyText` as an offline response dry-run sample; manually overriding protocol forever instead of fixing inference from sample truth.
- Evidence target: installed `routecodex/rcc` version, live `/health`, replayed `client-request.json` with `--dry-run provider-request`, and response dry-run on an existing materialized `provider-response.json`.

## L93-122 Response bridge facade deletion must leave only handler IO plus native calls

- Trigger: `responses-response-bridge.ts` has no active source/test/script import after handler JSON dispatch moves to direct Rust/NAPI calls.
- Rule: physically delete the duplicate facade and make residue gates assert absence. Keep request log context, snapshot sanitize, and HTTP dispatch orchestration in `handler-response-utils.ts`; keep client-visible response projection and JSON dispatch policy behind `native-exports.ts` Rust/NAPI calls.
- Rule: update function map, mainline call map, verification map, wiki markdown, and rendered wiki HTML in the same slice. Mainline edges should point at `handler-response-utils.ts -> native-exports.ts`, not the deleted facade.
- Anti-pattern: restoring `responses-response-bridge.js`/`.ts` for Jest mocks, keeping architecture scripts that read the deleted file, or moving projection semantics into handler code instead of calling native.
- Evidence target: exact active ref scan has no imports/mocks of `responses-response-bridge`, focused handler/residue/red Jest passes, response/SSE architecture gates pass, strict llmswitch shell audit passes, function/mainline/deleted-path/thin-wrapper gates pass, `tsc`, `build:base`, and diff check pass.

## L93-123 Single-import loader shells should collapse into their native loader

- Trigger: a standalone llmswitch host loader shell has only one production importer and only docs/residue references, such as `core-loader.ts` after all dist/core dynamic-load paths are gone.
- Rule: move path/package resolution into the actual native binding loader as private plumbing, then physically delete the standalone shell. Add both residue and architecture deleted-path guards.
- Anti-pattern: preserving a one-function loader shell or source-side `.js` mirror because older docs/tests mention it. Docs/tests should point to the native loader surface, not resurrect a shell.
- Evidence target: exact active ref scan shows no imports/mocks of the deleted shell, root TypeScript passes, focused residue Jest passes, strict shell audit passes, deleted-path/thin-wrapper/function-map gates pass, `build:base` and diff check pass.

## L93-124 Release snapshot startup failures require dependency closure evidence

- Trigger: installed release crashes on startup with `ERR_MODULE_NOT_FOUND` from `~/.rcc/install/current/dist/**`, while repo `dist` import works.
- Rule: compare installed snapshot `package.json.dependencies` against `install/current/node_modules/*/package.json`, then import the failing installed dist module directly. The fix belongs in release install/snapshot dependency materialization, not in live config, ports, or manual `node_modules` copying.
- Rule: `install-release.sh` must not treat a present `node_modules` tree or symlink as complete; it must verify the production dependency closure before reuse. `install-release-snapshot.mjs` must verify dependency closure and key runtime imports before switching `install/current`.
- Anti-pattern: manually copying a missing package into `~/.rcc/install/current`, accepting repo-local import as proof, or declaring startup fixed without global install plus live `/health`.
- Evidence target: red missing-dependency snapshot test, install dependency-closure test, installed dependency scan with `missing=[]`, direct installed import probe, global `routecodex/rcc --version`, and live health for the affected port group.

## L93-125 Release snapshot copy EINTR must retry visibly, not finalize manually

- Trigger: `scripts/install-release-snapshot.mjs` fails while copying large `node_modules` trees with `EINTR, Interrupted system call`, often under nested package paths such as `zod-to-json-schema/.../node_modules/zod`.
- Rule: handle only interrupted filesystem copy as an IO retry: remove the partial target, retry a small fixed number of times, and print a visible retry warning. All non-`EINTR` copy errors must fail immediately, and exhausted retries must still fail the install.
- Rule: dependency closure verification and runtime import verification must still run before `install/current` is switched. Do not manually finalize a staging snapshot after a copy interruption unless a separate explicit recovery task validates the full snapshot.
- Anti-pattern: treating an `EINTR` copy failure as ignorable because build gates already passed, or copying missing files into an installed snapshot by hand.
- Evidence target: focused install snapshot/dependency Jest, `npx tsc --noEmit --pretty false`, full `scripts/install-release.sh`, global `routecodex/rcc --version`, installed host/file probe if relevant, and live `/health` on managed ports.

## L93-126 Runtime lifecycle closeout must validate status transitions in Rust

- Trigger: `runtime.lifecycle` is called Rust-owned but `instance.json` status names or transitions are still accepted as TS-provided strings.
- Rule: Rust/NAPI must own both record shape and lifecycle transition policy. TS may pass an observed event/status request, but Rust must reject unknown statuses and invalid backward transitions before TS writes `instance.json`.
- Rule: completion proof must include global install plus live in-session restart evidence: listener child pid changes, parent supervisor/session pid stays the same, and `process-lifecycle.jsonl` shows `SIGUSR2 -> restart_delegate_parent_supervisor -> exitCode 75`.
- Anti-pattern: accepting focused Jest/build evidence as enough for restart semantics, using repo-local `node dist/...`, or using `start --restart`/`/shutdown` to adopt a live runtime.
- Evidence target: Rust lifecycle unit tests for invalid status/transition, direct-native lifecycle Jest, `verify:runtime-lifecycle-pid-rebase`, function/mainline/resource gates, `build:native-hotpath`, `build:base`, `install:release`, global version checks, live `/health`, and explicit global `routecodex restart --port <port>`.

## L93-127 Start command must be launch-only unless explicitly exclusive

- Trigger: `restart` is fixed in-session but `rcc start` still defaults to takeover or `start --restart` can stop an existing runtime.
- Rule: default `rcc start` and explicit `rcc start --restart` are not restart transports. Rust `plan_runtime_start_restart_takeover_guard_json` must refuse occupied non-exclusive starts before stop-intent, `/shutdown`, or `ensurePortAvailable`; only explicit `--exclusive` may enter the destructive takeover path.
- Rule: live negative proof should compare listener pid/health and stop-intent mtime before/after a global `rcc start --restart --port <port>` refusal. An old stop-intent file is not a failure if installed dist cannot write its retired source string and its mtime stays unchanged.
- Anti-pattern: leaving `options.restart !== false`, using health-probe stubs as a second pre-start runtime truth in tests, or keeping TS-owned stop-intent TTL defaults after Rust owns the consume plan.
- Evidence target: direct-native guard tests for default and explicit refusal, start-command tests for default launch-only/occupied refusal/exclusive takeover, `build:base`, `install:release`, global version checks, live `/health`, explicit global `routecodex restart`, and live `rcc start --restart` refusal.
# L93: cyclic metadata must cross the Rust safe-projection boundary before observers

- Trigger: a direct-route request with image/request mirrors fails in `JSON.stringify` before the intended VR/provider projector runs.
- Judgment: trace every earlier observer; the first JSON-only consumer, not the final route call, defines the effective boundary.
- Action: generate the owner-specific route-safe carrier in Rust first, then let log/session/debug observers consume only that projection; lock TS key lists and control projectors out with residue red fixtures.
- Boundary: cycle-aware TS serialization is transport mechanics only. Do not add a TS semantic allowlist, try/catch fallback, or second projection path.
# L93: direct canonical model projection is a directional dual-track contract

- Trigger: a direct-route test expects the client alias on the provider send payload, or a canonical provider response leaks the wire model back to the client.
- Judgment: verify live/provider model truth first. Provider request uses configured canonical `modelId`; client JSON/SSE restores the original inbound alias. Do not flatten both directions into one model expectation.
- Action: keep request model/thinking planning and bounded response envelope projection in one Rust owner. Return `payloadChanged` explicitly so TS can preserve unchanged request object identity without rederiving semantics.
- Boundary: rewrite only recognized top-level/`response`/`body`/`data` model envelopes. Never recursively rewrite nested diagnostics, trim complete SSE frames, or add provider-specific branches/fallbacks.
- Evidence target: positive canonical-wire/client-alias tests, negative nested-model/malformed-SSE/whitespace/`[DONE]` tests, TS semantic-revival red fixtures, focused direct integration, native/reference/rustification/build gates.
# L93: returned provider response failures must enter ErrorErr from a Rust plan

- Trigger: a direct provider resolves `{status, body}` instead of throwing, and router-direct locally lists retryable HTTP statuses or synthesizes `HTTP_<status>`.
- Action: pass only extracted status into a Rust owner that returns `shouldRaise` plus the ErrorErr-compatible descriptor. TS may construct the JS Error, attach the raw response reference, call the existing provider-error hook, and rethrow.
- Positive/negative lock: auth/quota/429/5xx raise; missing/200/ordinary 400/404/499 do not. This owner does not decide retry/reroute or client projection.
- Anti-pattern: duplicating status lists in TS, sending the entire possibly-streaming response through JSON, or using returned-response detection as a second retry policy.
# L93: direct eligibility must be an action plan, not scattered TS guards

- Trigger: router-direct checks port mode, defaults `sameProtocolBehavior`, resolves provider availability, and compares protocols through separate TS `if` branches.
- Action: Rust first emits `skip` or `resolve_provider`; after TS performs provider/protocol IO, Rust emits final `skip` or `execute_direct`. Public eligibility/behavior queries consume the same planner.
- Boundary: TS may resolve the provider handle and entry protocol, but cannot own skip reasons/default behavior/protocol-match semantics. Unknown planner actions fail-fast; never default to direct or relay.
- Evidence target: provider-mode/relay/missing-provider/mismatch negatives, default/explicit-direct matching positives, TS guard-revival red fixtures, native/reference/function/base gates.
# L93: audit allowlists are semantic projections, not TS logging glue

- Trigger: a TS direct/executor path contains an observable-field array and loops over business payload keys into audit/snapshot context.
- Action: move field allowlist, order, absence/null behavior, and nested-field exclusion into a Rust diagnostic projector. TS may preserve the original payload reference and assign the projection.
- Boundary: diagnostic projection never mutates provider/client payload and never becomes metadata/control truth. Do not treat a field allowlist as harmless logging IO.
- Evidence target: ordered positive values, explicit-null/absent/nested negatives, TS list/loop revival red fixtures, focused integration and architecture/native/base gates.
# L93: opaque stream references are TS facts, response action remains Rust truth

- Trigger: a router-direct response can contain a live `sseStream` that cannot cross JSON/NAPI, while TS branches between passthrough, JSON model projection, header projection, and SSE frame projection.
- Action: TS may report only `responseIsRecord` / `hasSseStream` facts and preserve the opaque reference. Rust returns the closed response action; TS executes the selected JSON/SSE/header IO.
- Boundary: stream buffering, `Transform`, pipe, and error propagation are Node IO. Selecting JSON vs SSE vs passthrough, normalizing the client model, or inventing a default action is semantic ownership and must stay Rust-only.
- Evidence target: positive JSON/SSE/header/passthrough cases, impossible/unknown-action negatives, TS branch-revival red fixtures, focused integration, native/reference/rustification, and base build gates.
# L93: MetadataCenter writes may be host IO, but their write plan is Rust semantics

- Trigger: TS decides whether a direct model override should write observations and hardcodes MetadataCenter family/key/value/reason tuples.
- Action: Rust emits a closed effect list. TS may check whether the request-local carrier exists and execute each write, but cannot synthesize missing writes or duplicate them onto a cloned payload carrier.
- Boundary: both client alias and provider wire model must exist before emitting the paired writes. A clone that is only attached/written and never returned or read is dead semantics and must be physically removed.
- Evidence target: paired-write positive, missing-half no-write negatives, payload isolation, TS key/reason/clone revival red fixtures, focused Rust/Jest, metadata/function/resource/native/base gates.
# L93: opaque runtime carriers stay TS IO while attach/dry-run decisions stay Rust

- Trigger: router-direct checks a symbol carrier, creates its metadata record, propagates provider-request dry-run, and attaches the carrier under a TS `if`.
- Action: send only carrier-exists plus cycle-safe pipeline metadata facts to Rust. Rust returns `skip` or `attach` and an optional validated dry-run control; TS preserves opaque carrier fields and performs the symbol/hidden-property writes.
- Boundary: never serialize the whole opaque carrier through Rust merely to choose the action; that can drop non-JSON runtime references. Never default an unknown action to attach.
- Evidence target: carrier+valid-dry-run positive, missing-carrier and invalid/disabled-dry-run negatives, unknown-action fail-fast, TS propagation/if/fallback revival fixtures, focused Rust/Jest, native/base/live gates.
## L93-33 Provider-request dry-run is terminal before provider postprocess

- Trigger: direct provider-request dry-run returns HTTP 200, but relay selection sends `routecodex.pipeline_dry_run` into Hub response parsing and fails a normal provider contract such as missing OpenAI `choices`.
- Judgment: inspect the provider transport return boundary before changing response converters. A marked dry-run result is a terminal diagnostic result, not `ProviderRespInbound01Raw`.
- Action: Rust emits a closed terminal/continue action immediately after provider transport; TS only observes the opaque internal marker and executes return/continue. Unknown actions fail-fast.
- Anti-pattern: add `routecodex.pipeline_dry_run` payload-shape checks to TS, provider postprocessors, or Hub response parsers; retry live requests until a direct provider happens to win.
- Evidence: positive test proves marked dry-run skips postprocess, negative test proves normal responses still enter the normal path, and live replay must force or uniquely select a relay provider and return HTTP 200 without the previous response-contract error.
## L93-34 Provider-response MetadataCenter synchronization is a Rust effect plan

- Trigger: post-conversion TS compares bridge/outer MetadataCenter identity, chooses sync keys/reasons, and conditionally copies stopless, stop-message, or Hub-stage state.
- Action: Rust returns exactly one closed action: `no_op`, `bind_bridge_center`, or `apply_writes`, including the complete family/key/value/reason/writer tuples. TS may observe center existence/identity, read request-local bridge snapshots, and perform the specified bind/write IO.
- Boundary: allow only internal `runtime_control.stopless`, `runtime_control.stopMessageCompareContext`, and `debug_snapshot.hubStageTop`; never project these into provider/client payloads. Unknown actions or write targets fail-fast; do not default to no-op or reconstruct a write in TS.
- Evidence target: bind/no-op/write positives, absent-center and unknown-action negatives, TS key/reason/action revival red test, owner-specific host mocks, focused Rust/Jest, architecture/native/base/release gates, and a real relay response with sample leakage scan.
## L93-35 Retired effect rejection still needs a Rust action plan

- Trigger: a TS bridge treats a runtime action as retired but still validates its array, extracts control fields, chooses MetadataCenter writes, and owns the rejection text.
- Action: Rust emits a closed `continue` / `reject_legacy_actions` plan containing any legal write tuple plus the error message. TS only performs the returned write, unchanged return, or explicit throw; unknown action fails fast.
- Boundary: “fail-fast only” does not make TS parsing semantic-free. Retirement policy, malformed classification, field selection, writer/reason, and user-visible/internal error text remain Rust truth. Do not preserve them in TS comments or residue gates as fake owner evidence.
- Evidence target: empty continue, malformed input, non-empty reject with/without control write, TS array/field/string revival red gate, focused Rust/Jest, servertool/native/base/release gates, and real relay response leakage scan.

## L93-36 Effect planners must consume the producer's serialized shape

- Trigger: a new Rust consumer planner fails live/focused cases because it reuses a generic projector whose input wrapper differs from the actual Rust effect payload.
- Judgment: trace the producer enum/struct serialization before adapting the consumer. `StoplessMetadataCenterWrite` carries `StoplessMetadataCenterWritePlan` directly; a projector expecting `{ plan: ... }` is a different contract, even if field names inside look similar.
- Action: consume and validate the producer's canonical direct shape in the Rust owner, project only declared runtime-control fields, and fail fast on obsolete wrappers or unknown fields. Update owner-specific mocks to mirror that exact shape.
- Anti-pattern: add wrapper compatibility, dual-shape parsing, or TS preprocessing to make the wrong projector accept the payload.
- Evidence target: direct-shape positive, learned-note-only/no-value no-op, obsolete-wrapper/unknown-field negatives, TS wrapper/key/reason revival red gate, focused Rust/Jest, native/base/release, and live leakage scan.

## L93-37 Stream IO does not make stream effect validation TS-owned

- Trigger: TS receives a Rust `streamPipe` effect and locally checks/normalizes codec, requestId, and payload before constructing Node SSE IO.
- Action: Rust emits a closed `no_pipe` / `use_pipe` plan and owns all serializable field validation plus malformed errors. TS only consumes the validated pipe and performs non-serializable Node stream operations.
- Boundary: `Readable`, backpressure, piping, and stream error propagation remain host IO; codec selection, identifier normalization, payload shape, absence behavior, and malformed policy are Rust semantics. Unknown actions fail fast.
- Evidence target: canonical pipe positive, absent no-pipe, missing/empty/non-object negatives, TS field/error revival red gate, focused Rust/Jest, native/base/release, and real SSE completed/done replay.

## L93-38 Complete Rust effect arguments must reach host IO unchanged

- Trigger: Rust already emits a complete store/effect argument object, but TS rebuilds it, filters optional fields by truthiness, or repeats fixed owner/default values.
- Action: type the full Rust contract at the narrow native host and pass the object directly to the IO function. Keep normalization, omission policy, owner, and defaults in the Rust planner.
- Boundary: TS may execute filesystem, stream, HTTP, or store IO; it must not reshape serializable effect arguments. Unknown or malformed plans fail fast in Rust rather than gaining a compatibility path in TS.
- Evidence target: direct-object positive, fixed and optional fields preserved, TS constant/spread/rebuild revival red gate, focused Rust/bridge tests, architecture/native/base/release gates, and live payload-isolation replay.

## L93-39 Malformed Rust effect shapes must fail in the Rust consumer

- Trigger: TS calls `asRecord` or another shape helper before a Rust effect consumer, silently converting arrays/scalars into null or absence.
- Action: pass the producer effect value unchanged through the NAPI JSON shell. The Rust consumer must parse fail-fast and explicitly accept only its canonical object/null contract.
- Boundary: `undefined -> null` at JSON transport is allowed absence projection; malformed-to-null coercion is semantic fallback and forbidden. Do not add compatibility parsing for obsolete shapes.
- Evidence target: canonical object/null positives, array/scalar/invalid-JSON negatives in Rust, TS coercion-revival residue gate, focused bridge tests, architecture/native/base/release, and live payload-isolation replay.

## L93-134 Diagnostic console output still requires a Rust effect plan

- Trigger: TS filters response diagnostics, reads an `alarm`, serializes details, or maintains primary/fallback console message formats.
- Action: Rust emits a closed `no_op` / `emit` plan containing complete message strings. TS only performs `console.warn(message)` and rejects unknown actions.
- Boundary: managed Responses HTTP entry creates request-local session truth when an explicit session is absent; do not change live config or add a bypass to manufacture `stopless_missing_session_id`. Lock emit through the real native-binding integration path, then use live normal/no-op replay for runtime and payload-isolation evidence.
- Evidence target: alarm/no-alarm positives, malformed diagnostics negative, TS filtering/stringify/try-catch revival red gate, focused Rust and native integration, release/live no-op, sample leakage scan.

## L93-135 Materialize total native plans before host effect IO

- Trigger: TS reads nested `payload`, `requestId`, `diagnostics`, or `effectPlan.effects`, validates them, normalizes effects, then caches the native plan before host IO.
- Action: one Rust materializer consumes the total native plan, validates every serializable boundary, and returns the closed payload, diagnostic input, and runtime-effect projection. TS passes the total plan once, then performs console/store/stream/MetadataCenter IO only.
- Boundary: success checking may remain at the host call boundary, but payload/effect/diagnostic validation and runtime projection must not be split between host and Rust. Delete dead native-plan caches and old narrow normalize exports; do not retain compatibility aliases.
- Evidence target: total-plan positive, malformed payload/requestId/diagnostics/effects negatives, old export/cache/nested-read residue gate, focused Rust/Jest, function/native/base/release gates, and a cross-protocol live response with internal-carrier leakage scan.

## L93-136 Separate compiled config from request-scoped policy truth

- Trigger: a top-down design lets config validation and runtime classification both write the same request-scoped control resource.
- Action: config compilation writes only the deterministic provider profile projection. After final real-target selection, one runtime resolver uniquely creates the request-scoped policy consumed by paired request/response projectors.
- Boundary: response projection reads the shared resolved contract directly; it must not depend on request projector output, payload deltas, MetadataCenter, or a forwarder-owned policy cache.
- Evidence target: resource map allows one runtime writer, call-map config edge cannot produce the request policy, resolver edge must produce it, pending edges use machine-readable `binding_pending: true`, and generated manifest counts match.

## L93-137 Route pool names are not display truth

- Trigger: config authoring contains per-pool `id` / `poolId`, and logs or rollups print `route/pool-id`.
- Action: Rust config materialization strips authoring pool names from public routing config. Hit-log and telemetry project route classification only, and rollup grouping ignores `poolId`.
- Boundary: route isolation uses only `routeParams.routePolicyGroup`; do not infer group from pool ID. Internal synthetic tier IDs may exist for execution/error bookkeeping but must not be user-visible labels.
- Evidence target: config tests reject compiled `id/poolId`, hit-log tests reject `route/pool-id`, rollup tests prove `formatRoutePool(route, pool)` returns `route` and aggregation keys ignore pool ID, plus focused Rust/Jest gates.

## L93-141 Aggregate server restart is one process-level lifecycle action

- Trigger: one RouteCodex process listens on multiple configured ports, but operators or install scripts run `restart --port` once per port.
- Action: treat `--port` only as a locator. Resolve configured/listening members to one canonical listener PID set, issue one HTTP/SIGUSR2 restart, then verify every member port is healthy and still shares one identity.
- Boundary: different non-empty PID sets are different aggregate instances and must fail before restart IO. Missing configured members may be restored by the one restart, but success requires all of them to return. Never implement aggregate restart as a per-port loop.
- Evidence target: positive Jest proves same-PID members receive one signal and all health endpoints are probed; negative Jest proves mixed PID identities send no signal; release install uses one locator restart and live verification checks all configured ports.

## L93-138 Direct route thinking must stay outside routeParams

- Trigger: a direct semantic fixture or runtime resolver reads route thinking from `routeParams`, or a test moves top-level route-tier `thinking` under `routeParams` to obtain a green projection result.
- Judgment: route policy metadata and route thinking are different resources. The only route-thinking chain is `RoutePoolTier.thinking -> SelectionResult.route_thinking -> target.routeThinking -> VrDirect03ResolvedSemantics.route_thinking`.
- Action: author tests with the real top-level tier `thinking` field, carry it as the dedicated selected-target `routeThinking`, and let the Rust resolver/projectors consume only that field. Physically remove any `routeParams.thinking` compatibility read.
- Anti-pattern: accepting both locations, defaulting from one to the other, or calling an invalid `routeParams` fixture live-equivalent evidence. This creates a false green test and a second semantic source.
- Boundary: forwarder still selects only the real target; it does not own or cache route thinking or direct semantic policy. Managed-live passthrough evidence requires an explicitly configured provider model and cannot be replaced by module blackbox evidence or an unapproved real-config edit.
- Evidence target: Rust negative proves `routeParams.thinking` has no effect; runtime residue gate forbids production reads; module HTTP blackbox uses top-level tier thinking; installed same-entry provider-request dry-run proves canonical model plus route effort override; JSON/SSE live responses prove client model restoration and internal-policy isolation.

## L93-142 Managed-live direct policy reclassification needs one failing passthrough turn

- Trigger: source/module tests prove mixed direct policies, but managed-live still lacks evidence that retry resolves policy from the new real target instead of reusing the first target's classification.
- Action: after explicit authorization, temporarily configure one real model as `passthrough` for the bounded validation window. First prove its request with provider-request dry-run and its JSON/SSE response parity. Then send a client model that the passthrough upstream explicitly rejects, allowing the normal ErrorErr reroute to select a routing target. Remove the temporary policy after evidence capture unless Jason separately authorizes rollout.
- Judgment: the first provider request must preserve client model/effort; the rerouted routing target must show canonical provider model plus route thinking, and the client response must apply routing response projection. This single positive failure/recovery sample proves both real-target policy selection and reclassification after retry.
- Boundary: validation authorization is not rollout authorization. Do not leave `passthrough` in the real provider config or treat it as the new production default. Do not mutate provider health, credentials, or routing pools to manufacture selection. Do not use module blackbox as managed-live evidence. SSE transport keepalive may differ at the client boundary; after removing only that transport prefix, provider/client event bytes must remain equal.
- Evidence target: authorized temporary config diff, config validate, one aggregate restart, all member health/version checks, provider-request dry-run, JSON/SSE live samples, provider-switch log with first/second provider keys, raw provider/client response comparison, internal-carrier leakage scan, config withdrawal, second aggregate restart, and a final provider-request dry-run proving default routing is active again.

## L93-143 Payload copy reduction is an ownership contract, not payload trimming

- Trigger: large requests show RSS amplification across retry seed, JS/Rust JSON boundaries, req_inbound capture, standardized request, handler context, and debug/snapshot surfaces.
- Action: first remove eager copies on success paths. Borrow request objects for retry seed, clone only at actual retry/reentry restore, in Rust borrow before mutation then move raw ownership into the normalized owner, and let Hub standardization consume owned payload parts instead of cloning payload out of a temporary wrapper.
- Boundary: never reduce memory by trimming or summarizing real request/response payload semantics. Only debug snapshots, observability samples, and retry materialization timing may be optimized without changing client/provider payload truth.
- Anti-pattern: adding a smaller parallel payload, fallback snapshot path, provider-specific special case, or `additional_tools` special handling before the canonical tool semantics are explicitly owned.
- Evidence target: tests must prove no eager `JSON.stringify` / `structuredClone` on first attempt, restore still materializes an independent retry payload, Rust context capture preserves Responses tool semantics, owned standardizer output matches wrapper output, and source/native/build gates pass. Live closure additionally requires release install, aggregate restart, and same-entry large-payload replay.

## L93-144 Continuation store writer uniqueness is a Rust effect contract

- Trigger: a `/v1/responses` relay response has both Rust `publishResponsesRecordPlanJson` output and handler/request-bridge code that records or finalizes the continuation after the pipeline.
- Action: Rust must emit the complete ordered store effect list at Chat Process response save, and TS may only pass each effect unchanged to the conversation store host IO. `record_response` must precede `finalize_retention`.
- Boundary: handler, SSE, resp_outbound, and request bridge may transport/project finalized response truth, but they must not infer response ids, seed tool-call responses, finalize retention, or re-own relay save defaults after Chat Process save.
- Anti-pattern: restoring `finalizeResponsesPipelineResultForHttp`, `seedResponsesToolCallResponseForHttp`, `recordResponsesResponseForHttp`, or local response-id readers to make continuation state convenient for HTTP closeout.
- Evidence target: positive gate proves `continuationStoreEffects` are executed by the provider-response effect host; negative residue gate forbids handler/request-bridge writer helpers; source closure needs focused Rust/Jest, function/native/rustification, architecture mainline/wiki/manifest, base build, and diff check. Live closure still requires authorized install/restart/replay.

## L93-145 Host IO can read prompt/config, but payload mutation and writer identity stay Rust-planned

- Trigger: request bridge code calls a TS helper that reads env/FS and immediately mutates payload, or branches on a returned `family` string to choose a MetadataCenter writer.
- Action: keep only the non-serializable or host-local read in TS. Pass the host-read value into a Rust total plan, and have Rust emit the complete payload mutation result or complete write descriptor including writer identity.
- Boundary: reading files/env, attaching MetadataCenter, and executing `writeMetadataCenterSlot` are host IO. Deciding whether `/v1/responses.instructions` changes, how prompts merge, which MetadataCenter stage wrote a slot, and whether a family is legal are serializable semantics and must be locked by Rust/native wrapper fail-fast validation.
- Anti-pattern: `applySystemPromptOverride(...)` inside `responses-request-bridge.ts`, local `RESPONSES_PIPELINE_*_WRITER` constants, `write.family === ... ? writerA : writerB`, or treating "TS only calls a helper" as thin when the helper mutates protocol payload.
- Evidence target: residue gate forbids local prompt apply and writer branches; Rust positive proves instruction merge and writer descriptors; native wrapper rejects malformed writer shape; bridge Jest proves host-read prompt reaches native finalize; source closure needs TypeScript, native/base build, function/server-boundary/native-reference/rustification gates, and live closure still requires authorization.

## L93-146 Errorsample classification is semantics, errorsample write is host IO

- Trigger: request/handler TS catches an error, checks code/message/details, and decides whether to write an errorsample.
- Action: Rust must return a closed `none` / `write_errorsample` plan with group, kind, and payload. TS may only serialize the thrown error, add wall-clock timestamp if the artifact needs current time, and execute the file write.
- Boundary: `MALFORMED_REQUEST`, message substrings such as `Tool history contract violated`, and detail flags such as `toolHistoryContractViolation` are semantic classification inputs and must not be branched on in TS bridge/handler code.
- Evidence target: residue gate forbids TS-local code/message/detail checks; Rust focused tests prove positive and negative classification; bridge Jest proves only planned writes call `writeErrorsampleJson`; native/base/function gates prove export and owner surface wiring.

## L93-147 Resume-error projection is a total Rust plan

- Trigger: request bridge catches an error, separately checks `origin`, then builds a client HTTP error body with TS status/code/message defaults.
- Action: Rust emits one closed `rethrow` / `client_error` plan. TS serializes the thrown error, calls the plan once, returns the exact descriptor, or rethrows the original object.
- Boundary: host code may preserve the original thrown value for rethrow and may execute HTTP response transport, but it must not own client-error defaults, projectability checks, status/code/message fallback, or split builder/projectability helpers.
- Anti-pattern: `shouldProjectResponsesResumeClientErrorForHttp*`, `buildResponsesResumeClientErrorForHttp*`, local `origin === "client"`, or hardcoded `responses_resume_failed` / `Unable to resume Responses conversation` inside request bridge or host wrappers.
- Evidence target: residue gate forbids split helpers and local defaults; Rust positive/negative tests lock client vs non-client; bridge tests prove exact descriptor/rethrow behavior; native wrapper validates unknown action and malformed descriptor fail-fast.

## L93-148 Stopless limits interception, not client stop delivery

- Trigger: the stopless CLI shows a fourth repeat, or the third consecutive missing/invalid-schema stop becomes a synthetic budget-exhausted terminal response.
- Judgment: `maxRepeats=3` limits consecutive interception attempts. Rounds 1 and 2 project CLI; round 3 is the original provider stop and must pass through to the client.
- Action: keep the streak in the Rust stop-schema state owner. Reset it on non-stop progress, ordinary tool calls, valid terminal schema, `simple_question=true`, or session change. Reject CLI/manual `repeatCount >= maxRepeats` instead of clamping or producing a success result.
- Continuation boundary: during relay submit, `MetadataCenter.continuation_context.responsesResume` means capture the already materialized Chat Process payload. Raw HTTP `{tool_outputs}` cannot replace its model/base payload.
- Anti-pattern: projecting a third/fourth CLI, building a neutral/budget-exhausted response, accumulating non-consecutive stops, or fixing a missing model in outbound with a fallback default.
- Evidence target: paired Rust positive/reset/session tests, CLI at-limit fail-fast blackbox, and a real HTTP three-round relay blackbox proving exactly two CLI projections plus third-round original stop passthrough.

## L93-149 Stopless live probes require runtime activation evidence

- Trigger: a live prompt asks the model to print stop schema, but the response contains no injected `reasoningStop` tool and returns the schema as ordinary text.
- Judgment: prompt content does not activate stopless. The sample is valid only when request-local runtime evidence shows `stopless.active=true`, a session id, the managed route/provider, and the expected repeat count.
- Action: first classify the sample from canonical `ports/<port>/<requestId>/__runtime.json` plus route log. Reject direct/no-control samples as `invalid_direct_or_no_stopless_path`. For at-limit verification, prove `repeatCount=3` and scan the client response for absence of `reasoningStop`, `exec_command`, synthetic budget-exhausted projection, and a fourth CLI.
- Boundary: do not mutate live config or add probe-only activation headers to manufacture stopless. A source HTTP blackbox proves the full deterministic three-round sequence; a managed live at-limit sample proves the installed runtime executes the third-stop passthrough contract.
- Evidence target: command/install/current/health version alignment, exactly one aggregate restart signal, canonical runtime sample with `stopless.active=true`, managed provider route evidence, and client response completion without stopless CLI projection.

## L93-150 Responses request continuation effects need closed Rust plans

- Trigger: `responses-request-bridge.ts` switches on direct/relay/scope/attach and locally fills response ids, endpoints, owner classes, resume metadata, or materialized payload input after a store lookup.
- Action: Rust `planResponsesContinuationRequestActionJson` must return only `execute_effect` with complete IO args or `complete` with the final host descriptor. TS may call the indicated host IO and must pass `{ operation, result, resultPlanInput }` back unchanged; `resultPlanInput.stage` must match the operation.
- Boundary: host IO includes lookup, relay resume, direct materialize, and scope materialize. Semantics include response-id selection, owner validation, direct `previous_response_id` mutation, relay/scope options, endpoint selection, resumeMeta, final payload merge, missing/not-found/unknown-owner client errors, and malformed-result fail-fast.
- Anti-pattern: treating `null` lookup as relay, defaulting unknown owner, locally adding `response_id` to relay payload, parsing `resumeMeta`, merging materialized input in TS, or keeping relay-only context builders such as `buildCapturedRelayResumeRequestContextForHttp`.
- Evidence target: Rust continuation action positive/negative tests, native wrapper unknown/malformed validation, bridge Jest exact IO calls, handler E2E explicit owner fixtures, residue gate `verify:responses-request-bridge-total-plan-shrink`, mainline call-map binding for real symbols, architecture review/wiki sync, native/base build, and live replay only after explicit authorization.

## L93-151 Provider-response hosts preserve raw errors, ErrorErr classifies

- Trigger: provider-response host code reads SSE wrapper/message/context/provider config and writes `code`, `status`, `statusCode`, `retryable`, `upstreamCode`, or provider failure stage before throwing.
- Action: preserve raw wrapper evidence under `response/details` and throw it unchanged. Error classification, recoverability, route policy, execution decision, and client projection must continue through Rust `ErrorErr02 -> ErrorErr06`.
- Boundary: Node may read body/stream, capture raw message/code/status, log diagnostics, and rethrow. It may not run rate/context/network/provider-configured/SSE remappers or decide retry from normalized fields.
- Anti-pattern: deleting classification in `provider-response-converter.ts` while leaving `request-executor-provider-send-failure.ts` to call the same TS remapper. That closes one host slice, not the ErrorErr path.
- Evidence target: positive source gate plus negative revival fixtures, raw-evidence converter tests, ErrorErr contract/function-map gates, and a separate follow-up red/green slice for executor-side remap/retry removal.

## L93-152 Dead TS remappers need deletion locks, not just unused imports

- Trigger: a TS remapper/helper has no production caller after Rust/ErrorErr takes ownership, but the helper and its old unit test still exist.
- Action: physically delete the helper and the old assertion test, then extend the owner verifier to fail if either file path reappears. Keep semantic coverage in Rust/owner gates and focused host tests, not in a dead TS helper test.
- Boundary: historical goal docs may keep old commands as past evidence, but canonical function map, verification map, and wiki anchors must not list deleted tests as required gates.
- Anti-pattern: leaving `provider-response-sse-error-normalizer.ts` idle because no caller imports it, or retaining `provider-response-converter-empty-sse.spec.ts` as a "safety" test for behavior that is now explicitly forbidden in TS.
- Evidence target: `rg` proves no production caller, verifier red fixture fails on dead file revival, function/verification maps compile without the deleted test, target diff check passes, and base/architecture blockers are reported separately instead of hidden.

## L93-153 Call-map symbol drift must follow current source truth, not old symbol names

- Trigger: an architecture gate fails because a mainline edge references a deleted or renamed callee, while current source has a nearby real owner for the same lifecycle step.
- Action: first verify the current source owner and whether the old semantic moved to a different lifecycle edge. Update only the call-map/wiki/generated artifacts to the real adjacent caller/callee when the runtime source is already correct; do not revive the old symbol or edit runtime just to satisfy a stale map.
- Boundary: keep distinct lifecycles separate. For servertool, request-side tool injection maps to `maybe_apply_servertool_orchestration`; stopless schema instruction injection is a separate `stopless.session.mainline` edge and must not be folded into the servertool hook skeleton edge.
- Anti-pattern: treating a missing symbol as proof runtime is wrong, or adding compatibility wrappers such as a new `inject_reasoning_stop_tool` just to make architecture review green.
- Evidence target: `verify:architecture-mainline-call-map`, `verify:architecture-wiki-sync`, `verify:architecture-mainline-manifest-sync`, `verify:architecture-wiki-html-sync`, `verify:architecture-review-surface-light`, and `build:base`.

## L93-154 VR selection dry-run does not prove ErrorErr05 singleton policy

- Trigger: a default-pool last-provider fix is tested by placing every default provider in `metadataCenterSnapshot.excludedProviderKeys` and calling VR route dry-run.
- Judgment: that probe exercises initial route selection and may correctly return `PROVIDER_NOT_AVAILABLE`; it does not execute post-provider-failure ErrorErr05 availability or exclusion-release policy.
- Action: first use the installed release's `resolveErrorErr05RouteAvailabilityDecisionNative` and `resolveErrorErr05ExecutionDecisionNative` with a configured default singleton, then run the executor singleton replay blackbox. Use managed live replay only when the real route is already singleton; never mutate production routing config to manufacture the branch.
- Evidence target: availability reports `defaultPoolAvailable=true`, `defaultPoolSingletonProvider=true`, `policyExhausted=false`, `mayProject=false`; execution reports same-provider retry, no current-provider exclusion, and an empty exclusion list; executor logs wait completion before exclusion clear/replay.

## L93-155 Retired host effects must not preserve a semantic result union

- Trigger: a provider-response host awaits a Rust-planned retired effect, then returns `unchanged` or a Hub stage name and uses that result to choose between payloads.
- Action: prove the legal retired-action set is empty or fail-fast, change the host executor to `Promise<void>`, and consume the Rust materializer's canonical body/stream payload directly. Remove the stage-result union and every host branch built around it.
- Boundary: a Rust projector can remain the semantic owner even after its zero-production-caller TS wrapper is deleted. Do not keep a root wrapper/export that converts malformed native output into `{}` as compatibility; malformed native results must remain explicit failures.
- Anti-pattern: calling a branch unreachable while leaving its union, wrapper, export, or fallback in production source. Dead semantic surfaces are revival points even when current callers do not select them.
- Evidence target: pre-fix verifier names the live host branch, result union, wrapper, and fallback; negative fixtures revive each form; focused provider-response contracts plus TypeScript, function-map, thin-wrapper, rustification, architecture, native, base build, and diff gates pass.

## L93-156 Effects must not duplicate their parent node payload

- Trigger: a Hub output contains a complete response at the top level while an effect plan clones the same full object only so host IO can consume it.
- Judgment: effects describe operations, not a second semantic truth. Keep the complete payload in the canonical typed node and carry only effect-specific metadata when host IO can reuse the existing top-level object reference.
- Action: remove the effect payload clone, make the effect metadata-only, reject legacy full-payload fields in the Rust planner, and let the host borrow the canonical materialized response for transport.
- Boundary: this is not a fallback from missing effect payload. The contract itself has one owner; malformed legacy `payload/body` must fail fast. TS may perform Node stream IO but cannot validate codec/request identity or rebuild response semantics.
- Anti-pattern: retaining both `rawPayload` and `streamPipe.payload`, renaming the duplicate to `body`, or preserving both contracts for compatibility.
- Evidence target: red source gate for the clone, Rust positive/negative planner tests, host integration proving the same top-level semantic object is used for SSE, architecture maps naming the sole owner, native build, and authorized live RSS replay before claiming memory reduction.

## L93-157 Stopless repeat truth survives relay endpoint materialization

- Trigger: the first missing/invalid stop projects CLI correctly, but a relay submit round starts again at `repeatCount=1`, or provider history reveals the automatic CLI call/result.
- Judgment: after relay submit materialization the internal endpoint may already be `/v1/responses`; endpoint substrings are not continuation truth. The current `MetadataCenter.continuation_context.responsesResume.toolOutputsDetailed` output is the private current-turn evidence.
- Action: restore repeat state from current resume output plus the stopless snapshot, remove the automatic CLI call/result from provider-facing history, and emit only the fixed transparent user prompt or exact valid `next_step`.
- Boundary: the transparent prompt is presentation, never state. Stop schema guidance belongs only in the system instruction. Do not expose `reasoningStop`, servertool/hook identity, repeat counters, schema feedback, missing fields, or CLI output to the provider.
- Evidence target: request dry-run must inspect final `providerRequest.body`, require `routecodex.pipeline_dry_run/provider_request`, `stoppedBeforeProviderSend=true`, complete schema contract/examples, the expected ordinary user prompt, no internal markers, and no second upstream send. Sequence tests separately prove `1 -> 2 -> third original stop passthrough` and reset on a new user turn or non-stop progress.

## L93-158 Provider-origin auth and quota failures are availability events

- Trigger: Rust ErrorErr03 classifies provider-origin 401/402/403/404 or auth/quota/account/model codes as unrecoverable, so ErrorErr05 projects them before a configured backup/default provider can run.
- Judgment: provider-origin availability failure and local/client contract invalidity are different classes. Auth, quota, account, and model rejection from one provider says that provider target failed; it does not prove the request is invalid for every route/default target.
- Action: delete provider-origin auth/quota/account unrecoverable status/code/message predicates from the Rust failure-policy owner. Classify them recoverable, then let ErrorErr05 use authoritative route-pool plus default-pool availability; project only when both are exhausted.
- Boundary: keep `MALFORMED_REQUEST`, `CLIENT_TOOL_ARGS_INVALID`, provider runtime request contract, and local response contract unrecoverable. Keep `client_disconnect` health-neutral. Node/TS may preserve raw evidence and execute IO but cannot restore status/code/message classifiers or provider-response remappers.
- Anti-pattern: reintroducing `INVALID_API_KEY`/401/403/quota/account checks in provider-response converter, host, RequestExecutor, HTTP error mapper, or provider runtime; treating a provider catalog label as an ErrorErr05 terminal decision.
- Evidence target: pre-fix Rust positives must be red; Rust failure-policy full suite and local-contract/client-disconnect negatives must pass; focused executor tests must prove default-pool availability reroutes and simultaneous route/default exhaustion projects; blackbox 401/403/quota primary failures must hit backup and return HTTP 200.

## L93-159 Installed blackbox scripts must prove their runtime import root

- Trigger: a release snapshot does not contain a test script, or a repo-local gate passes after global installation.
- Action: locate the packaged script under the actual global npm package, execute its absolute path, and prove relative imports resolve under that package's `dist`. The repo may supply a tracked fixture through `cwd`, but must not supply runtime modules.
- Boundary: `~/.rcc/install/current` and the global npm package have different packaging surfaces. A missing script in the release snapshot is not a blackbox failure when the npm package intentionally carries `scripts/`.
- Anti-pattern: running the repo script after install and calling it installed-artifact evidence, or treating a failed `test -f` precondition as a runtime gate failure.
- Evidence target: package/script path, script hash or packaged-content identity, resolved import path under global `dist`, gate result, CLI/install/current version, and all aggregate member health versions.

## L93-160 Isolated installers must carry governance source, not collaboration state

- Trigger: `install:global` passes source build but its isolated build fails an architecture/resource gate because tracked maps or protocols are absent.
- Action: compare the global installer allowlist with the authoritative release installer and copy the smallest tracked governance source set required by build gates.
- Boundary: `.agent-collab/PROTOCOL.md`, schema, and examples are authoring contracts; runs, claims, heartbeats, evidence, merge queues, and kill-switch state are runtime collaboration data and must not enter the package.
- Anti-pattern: disabling the failing architecture gate, copying all of `.agent-collab`, or adding a second installer-specific validation path.
- Evidence target: isolation red test, green allowlist test, shell syntax check, the originally failing architecture gate, full global install, one aggregate restart, and member health/version alignment.

## L93-161 Stopless reset must cover every collapse owner

- Trigger: ReqChatProcess correctly resets on a real user turn, but inbound normalization, continuation-history collapse, or the final Responses-to-chat codec still replaces that user text with automatic guidance or leaks an internal stopless tool declaration.
- Action: scan backward only within the current turn. The latest real user is a hard boundary: delete older stopless pairs, preserve the user text verbatim, clear private streak state, and do not synthesize guidance. Before provider projection, filter internal stopless names from embedded `toolsNormalized` while preserving ordinary client tools.
- Boundary: structured current-turn CLI output plus request-scoped runtime control are state truth. User text, including literal `继续执行`, is presentation or real input and must never reconstruct stopless state.
- Evidence target: paired Rust tests for stopless evidence after user versus user after stopless evidence; Responses codec test proving real user preservation, no internal tool/history marker, and retained `exec_command`; final provider-request dry-run plus installed-artifact replay.

## L93-162 Zero-copy audits must distinguish ownership transfer from mutation isolation

- Trigger: a debug replay harness deep-clones a captured payload immediately before provider context/preprocess/postprocess.
- Judgment: do not remove the copy merely because snapshot file reads currently deserialize a fresh object. A public harness input remains caller-owned unless every caller and store contract proves unique ownership transfer; provider hooks may mutate it.
- Action: keep exactly one named execution-copy owner, test caller/nested/runtime-metadata isolation for request and response paths, and remove only redundant/fallback clone mechanisms. Under Node.js >=20, use the single `structuredClone` contract and reject JSON round-trip cloning.
- Boundary: this diagnostic execution graph cannot become live provider/client payload truth or MetadataCenter state. Remove it only after a typed unique-ownership transfer contract is verifiable across direct harness and replay-runner callers.
- Evidence target: source red for fallback/residue, positive provider mutation visibility, negative captured-input pollution, function/resource/mainline bindings, inventory release point, TypeScript, architecture gates, and base build.

## L93-163 Debug comparison scripts should borrow unless they mutate

- Trigger: a debug/shadow comparison script deep-clones complete baseline and candidate payloads before a recursive diff, but the diff function only reads both objects.
- Action: remove the comparison clone and pass the original wrappers by reference into the read-only diff. Keep full debug artifact persistence only on real diffs, so audit fidelity remains unchanged.
- Boundary: this applies only to diagnostic comparison state. Do not use this rule to remove the single mutation-isolation copy from provider replay harnesses, retry materialization, continuation snapshots, or live payload owners.
- Anti-pattern: adding `cloneJsonSafe`, `JSON.parse(JSON.stringify(...))`, or fallback-to-original clone helpers around a read-only diff. If serialization fails and fallback returns the original object, later path deletion may silently mutate caller state.
- Evidence target: a red source-residue test for the clone, green focused script gate, inventory/resource/function bindings, source residue scan, TypeScript, architecture review, base build, and no RSS claim without installed-runtime replay.

## L93-164 Ordered action pipelines need borrow/take/write-back contracts

- Trigger: a configurable Rust action loop clones the same raw request/response or captured state into every action so later actions can still read it.
- Action: give read-only actions borrowed cores; for mutating actions use `Option::take()`, execute the unique semantic owner, and write the returned value back to the same state slot before advancing.
- Boundary: preserve `Some([])`, non-object values, and no-match owners explicitly. Internal owner-return fields must be serde-skipped and absent from external JSON; owned N-API wrappers delegate to the same core.
- Anti-pattern: clone-per-action, dropping state when an action returns `None`, or adding a second borrowed implementation with divergent semantics.
- Evidence target: source residue gate, ordered positive/negative multi-action tests, external JSON shape test, full owner suite, adjacent request/response gates, native/base build, and RSS claims only after installed large-payload replay.

## L93-165 Provider-wire compatibility sanitizers should consume owned branches

- Trigger: a Rust N-API compatibility wrapper parses provider-wire JSON into an owned `Value`, then clones the full payload or full object branches before editing only a few compatibility fields.
- Action: make the sanitizer take the owned `Value`, `remove()` branch owners, mutate maps in place, and write them back before serialization. Non-object or unsupported branches must be restored unchanged instead of dropped.
- Boundary: keep the wrapper as parse-owned-JSON -> owned sanitizer -> serialize. Do not add a TS fallback, provider-config patch, MetadataCenter carrier, routing hook, or second semantic sanitizer.
- Anti-pattern: `payload_obj.clone()`, `tool_obj.clone()`, `message_obj.clone()`, `tool_call_obj.clone()`, or cloning a full branch just to update a name/schema field.
- Evidence target: source-residue red test for full-branch clone patterns, positive semantic tests for the compatibility edits, resource/function/verification map bindings, native/base build, and no RSS claim without installed large-payload replay.

## L93-166 Debug replay scripts should shallow-own top-level mutations

- Trigger: a debug replay script deep-clones a captured request/chat payload only to set `model`, replace system messages, or pass the value into a native/provider builder that serializes its input but does not mutate it.
- Action: create a named shallow top-level replay owner, mutate only that owner, and preserve nested `messages`, `tools`, metadata, and extension references unless the script actually rewrites that branch.
- Boundary: this applies to script-local diagnostic replay preparation. It does not remove the single provider harness execution copy required for preprocess/postprocess mutation isolation, and it cannot become live provider/client payload truth, route truth, or MetadataCenter state.
- Anti-pattern: generic `deepClone`, `JSON.parse(JSON.stringify(...))`, or clone-before-native-call in debug scripts when the next function is read-only/serialization-only.
- Evidence target: source red for clone residue, positive top-level mutation isolation with nested identity preservation, import-without-provider-IO test, function/resource/verification bindings, inventory row, TypeScript, and no RSS claim without installed-runtime replay.

## L93-167 Compatibility probe variants must be lazy

- Trigger: a debug capture/probe script tries several provider compatibility request shapes and builds every variant with JSON round-trip clones before attempting the first request.
- Action: expose one named variant projection owner that returns an iterator. Each yielded variant owns only the branch it rewrites; unchanged `input`, `tools`, content, metadata, and schema branches stay borrowed until normal JSON/provider IO.
- Boundary: explicit sample mode is different from generated-probe mode. Missing or invalid explicit samples must fail fast; do not fall back to generated variants because that changes diagnostic truth.
- Anti-pattern: eager `tries = [...]` full-payload variants, `JSON.parse(JSON.stringify(payload))`, `structuredClone`, `deepClone`, or catch-and-switch from explicit sample to generated probes.
- Evidence target: red source-residue test, lazy projection test that proves later variants are not materialized early, nested identity assertions, explicit sample fail-fast, function/resource/mainline bindings, fallback-diff gate, TypeScript, and no RSS claim without installed-runtime replay.

## L93-168 Capture-only top-level overrides need a shallow owner

- Trigger: a debug provider capture script deep-clones a complete request before changing only top-level model, instructions, tool choice, or stream fields.
- Action: create one named shallow capture owner and apply top-level writes/deletes there. Preserve nested input, tools, schemas, metadata, and extension references unless the capture script actually rewrites that branch.
- Boundary: importing the script for source/identity tests must not read provider config, initialize a provider, call the network, or write artifacts. Direct CLI execution remains the only provider-capture entry.
- Anti-pattern: JSON round-trip, `structuredClone`, or recursive clone for top-level-only capture overrides; mutating the loaded source request; treating a debug capture owner as provider/client/MetadataCenter truth.
- Evidence target: source residue red/green, nested identity and source-isolation tests, import guard, function/resource/mainline bindings, TypeScript, and no live/RSS claim without authorized provider capture or installed replay.

## L93-169 Necessary provider-mutation copies must fail fast

- Trigger: a regression/replay path needs an independent provider execution graph, but its clone helper falls back from `structuredClone` to JSON cloning or returns the original object.
- Judgment: do not remove the one copy when provider conversion/send may mutate input. Remove only incompatible fallback paths and name the copy owner/lifetime explicitly.
- Action: use one `structuredClone` immediately before provider conversion/send. Let clone errors propagate; release the graph with the attempt. Add a direct CLI guard so source tests cannot trigger live regression execution.
- Anti-pattern: JSON compatibility clone, catch-and-return-original, a second clone before conversion, or assuming a parsed sample grants unique ownership when the built request is reused across providers.
- Evidence target: circular/BigInt/undefined preservation, provider-mutation isolation, source fallback residue gate, import guard, function/resource/mainline bindings, TypeScript, and no live/RSS claim without authorized provider execution.

## L93-170 Completed debug event projections should borrow

- Trigger: a debug/golden-roundtrip utility receives a complete `response.completed.response` object and immediately passes it to a read-only encoder, but first JSON-clones the full response graph.
- Action: return the completed event object by reference and lock nested identity in a focused test. Keep synthesized fallback responses independent only for event streams that lack a completed response.
- Boundary: this applies to diagnostic completed-event projection only. Do not use it to remove mutation-isolation copies, retry snapshots, continuation stores, provider runtime inputs, or live payload owners unless unique ownership and no mutation are proven.
- Anti-pattern: keeping a second `response.completed` branch inside an aggregator that is only called after the completed-event pre-scan fails, or using `JSON.parse(JSON.stringify(...))` to create a debug projection for a read-only encoder.
- Evidence target: source-residue red test, object/nested identity positive test, partial-stream synthesis regression, function/resource/mainline bindings, wiki/html sync, TypeScript, and no RSS claim without installed-runtime replay.

## L93-171 Replay request preparation should use path-local owners

- Trigger: a debug replay script deep-clones a complete captured request body only to strip replay-only metadata keys or derive a provider-request sample into a replayable client request.
- Action: shallow-copy only the top-level owner and metadata object when stripping keys. For read-only provider-request projection, create a new replay envelope and borrow typed content blocks, tools, metadata, and stream intent from the captured sample.
- Boundary: this applies to diagnostic replay preparation. It does not remove provider mutation-isolation copies, retry snapshots, continuation stores, or live request owners. Explicit live replay remains separate and requires authorization/evidence before RSS claims.
- Anti-pattern: `JSON.parse(JSON.stringify(body))`, `structuredClone`, or `deepClone` around replay request prep when the helper only deletes known metadata keys or reads fields to build a new envelope.
- Evidence target: source-residue red test, nested identity positive tests, source sample mutation isolation, function/resource/mainline bindings, no-fallback diff, TypeScript, and no RSS claim without installed-runtime replay.

## L93-172 Diagnostic canonicalizers should own only rewritten paths

- Trigger: a debug/parity canonicalizer deep-clones a whole chat payload before normalizing a small set of top-level, metadata, message, or tool-call fields.
- Action: create a new top-level projection object, then shallow-own only branches the canonicalizer mutates. Preserve unchanged schema/tool/content branches by reference and prove the source sample is not mutated.
- Boundary: this applies to diagnostic canonical projections only. Do not remove mutation-isolation copies from provider runtimes, harness execution, retry snapshots, continuation stores, or live payload owners without unique ownership and no-mutation proof.
- Anti-pattern: `JSON.parse(JSON.stringify(chat || {}))`, cloning function tool `parameters`, or adding generic `structuredClone` / `deepClone` helpers around read-mostly parity normalization.
- Evidence target: source-residue red test, source-isolation positive test, unchanged nested reference identity, function/resource/mainline bindings, TypeScript, and no RSS claim without installed-runtime replay.

## L93-173 Debug parity helpers should not JSON-clone before equality

- Trigger: a debug/coverage parity helper serializes and parses both actual and expected payloads before `assert.deepEqual`.
- Action: compare already materialized outputs directly unless a proven JSON-semantics mismatch needs a small explicit comparator. Do not create full comparison object graphs only to remove undefineds or normalize JSON-shaped data.
- Boundary: native or provider call boundaries that already serialize JSON are separate contract limits. This rule only removes extra diagnostic comparison clones after outputs are already materialized.
- Anti-pattern: `stableJson(value) { return JSON.parse(JSON.stringify(value)); }` or clone-before-assert helpers in coverage scripts.
- Evidence target: red source-residue test, green focused coverage test, function/resource/mainline bindings, TypeScript, and no RSS claim without built artifact/live replay evidence.

## L93-174 Debug reports should share one summary owner

- Trigger: a debug/report script JSON serializes and parses the same summary before assignment, persistence, or display.
- Action: generate and assign one authoritative summary object, then borrow it for readers. Keep only the final serialization required by artifact IO.
- Boundary: this applies to diagnostic report state. It does not authorize sharing mutable live request/response payloads across runtime stages or removing required retry/continuation isolation.
- Anti-pattern: nested `JSON.parse(JSON.stringify(...))` chains around summary assignment, report writes, or console display.
- Evidence target: source-residue red/green, summary-owner assertion, parseability, function/resource/mainline bindings, TypeScript, and no live/RSS claim without installed runtime evidence.
## L93-175 Temporary config projections should own only rewritten paths

- Trigger: a debug capture script deep-clones a complete loaded config and provider config before replacing only one provider wrapper, routing target, and temporary HTTP host/port.
- Action: create path-local owners for the top-level config, virtual router, providers map, selected provider wrapper, routing, and HTTP server. Borrow unchanged model catalogs, auth/header/extensions, and unrelated config branches until the temporary artifact serialization boundary.
- Boundary: importing the capture module for tests must not scan provider configuration, write artifacts, spawn a server, or call provider IO. This rule does not authorize live provider config changes, `config.toml`, `~/.rcc`, capture execution, or normalization-semantic changes.
- Anti-pattern: `JSON.parse(JSON.stringify(baseDoc))`, cloning the selected provider graph, or using generic structured/recursive clones for a local temporary config projection.
- Evidence target: red source residue, base/provider source immutability, rewritten path independence, unchanged nested identity, import guard, resource/function/mainline bindings, TypeScript, and no RSS claim without authorized installed-runtime replay.
## L93-176 Bidirectional parity helpers should share the no-clone comparison rule

- Trigger: one direction of a TS/native parity pair compares materialized outputs directly while the reverse helper still JSON serializes/parses both full outputs before equality.
- Action: audit both conversion directions and every fixture in the helper. Compare already materialized outputs directly and delete the generic JSON normalization helper unless a proven semantic difference requires a narrow comparator.
- Boundary: the native call's existing JSON-string transport is a separate contract limit. Missing built `dist` artifacts block direct CLI parity execution but do not justify restoring comparison clones or claiming runtime evidence.
- Anti-pattern: fixing only chat-to-standardized while leaving standardized-to-chat `stableJson`, or treating serialization-based equality normalization as part of conversion semantics without a failing counterexample.
- Evidence target: source-residue red/green, node parse check, both full/minimal parity assertions, resource/function/mainline bindings, and built-artifact blackbox only when the required dist exists.
## L93-177 Equivalence sanitizers should strip debug fields with path-local owners

- Trigger: a diagnostic equivalence script deep-clones complete protocol payloads only to delete debug-only comparison fields before diffing.
- Action: export the sanitizer for focused tests, add an import guard, and shallow-own only the top-level object and metadata object when those fields are actually removed. Borrow unchanged messages, tools, schemas, content blocks, and extension branches.
- Boundary: direct equivalence CLI still depends on built dist modules and local samples. Missing samples are a blackbox fixture gap, not permission to restore full clones or claim runtime/RSS evidence.
- Anti-pattern: `JSON.parse(JSON.stringify(payload))` around a comparison sanitizer, top-level dist imports that run during unit-test import, or treating debug `__rcc_*` stripping as protocol conversion truth.
- Evidence target: source-residue red/green, source mutation isolation, unchanged nested reference identity, import-without-dist execution, map bindings, TypeScript, and direct CLI only when required samples exist.
## L93-178 Compatibility simulation should own only projected paths

- Trigger: a debug compatibility script deep-clones a complete request before creating a top-level parameters projection and rewriting only tool choice, token aliases, or tool wrappers.
- Action: shallow-own the top-level request and parameters object, allocate wrappers only for normalized tools/functions, and borrow unchanged messages, tools, parameter schemas, content, and extension branches until explicit debug IO.
- Boundary: this applies to diagnostic simulation, not live provider compatibility truth. Importing the helper must not call localhost/provider IO or write reports; direct network execution remains explicit and separately authorized/evidenced.
- Anti-pattern: `JSON.parse(JSON.stringify(request))`, generic structured/recursive clone, or moving the debug compatibility projector into Hub/VR/provider live semantics.
- Evidence target: red source residue, caller immutability, top-level/parameters ownership, normalized wrapper ownership, unchanged nested schema/message identity, map bindings, TypeScript, and no live/RSS claim without explicit execution.
## L93-179 Regression scripts should bind the native owner, not dead TS projections

- Trigger: a regression/debug script imports a removed TS conversion runtime and deep-clones a read-only sample before checking provider-to-Hub response semantics.
- Action: create one named debug builder in the script, point it at the exact compiled native artifact, serialize once at the required JS/Rust boundary, and call the Rust native semantic owner. Parse native envelopes fail-fast and keep direct CLI execution behind an import guard.
- Boundary: this applies to local regression scripts. It does not authorize reimplementing provider response semantics in TS, changing provider config, installing/restarting RouteCodex, or claiming RSS/live-provider equivalence without installed-runtime replay.
- Anti-pattern: importing physically deleted dist conversion files, `structuredClone(sample.payload)` before a read-only native projection, dist path fallback helpers, or letting module import execute regression IO.
- Evidence target: source-residue red/green, import-without-execution, direct local native CLI replay, resource/function/mainline bindings, TypeScript, no-fallback diff, and explicit no-RSS/no-live-provider boundary.
## L93-180 Artifact generators should serialize their authoritative projection directly

- Trigger: an architecture/debug artifact generator builds a complete JSON-safe projection, JSON serializes/parses it into a second graph, then immediately serializes that clone to YAML or disk.
- Action: name one projection builder, pass its returned object directly to the final serializer, and make module import side-effect free. Keep direct CLI generation as the only write entry.
- Boundary: preserve the complete artifact schema and run the canonical generated-artifact sync gate. This rule does not apply when a proven mutation-isolation owner or protocol boundary requires an independent graph.
- Anti-pattern: `manifestClean = JSON.parse(JSON.stringify(manifest))`, comments claiming JSON round-trip strips nulls, or import-time artifact writes that block focused builder tests.
- Evidence target: red source residue, builder shape test, import-without-IO, direct generator execution, artifact sync, map bindings, TypeScript, and no runtime/RSS claim for architecture-only changes.
## L93-181 Retry restore must fail explicitly when clone materialization fails

- Trigger: a retry seed is correctly borrowed on first attempt, but restore falls back to full JSON serialization/parsing or shallow object spread when `structuredClone` cannot create the independent retry graph.
- Action: delete the compensation path. Restore returns a real owned clone or `undefined`; do not reintroduce serialized retry seed variants unless there is a new explicit owner contract and red/green lifecycle test.
- Boundary: keep the independent clone for actual retry/reentry while provider/request mutation isolation still requires it. This rule removes fallback materialization, not retry isolation itself.
- Anti-pattern: `serializeRequestPayloadForRetry`, `restoreRequestPayloadFromRetrySnapshot`, `mode: 'serialized'`, or `{ ...snapshotPayload }` after clone failure.
- Evidence target: force clone failure in focused Jest, assert zero `JSON.stringify` calls, run request payload budget gate, TypeScript, no-fallback diff, and target diff check.
### L93-182 Snapshot queue memory budgets must be byte-aware

- Trigger: an observability queue has a fixed item capacity but can retain complete provider/request payloads.
- Action: estimate size from the borrowed value, reserve a global byte budget before enqueue, and release accounting on receive plus every failed-send branch.
- Gate both positive capacity and negative over-budget behavior; keep dropping confined to debug artifacts.
- Do not serialize payloads just to measure queue bytes, and never apply the diagnostic budget to live provider/client payloads.

### L93-183 Read-mostly Rust converters need an owned wrapper over one borrowed core

- Trigger: two pipeline stages clone complete `Vec<Value>` input/tools owners solely to call the same read-mostly projector.
- Action: keep the external owned/JSON API as a thin wrapper, add one internal lifetime-bound slice input, and make every internal caller borrow it. Clone only values that the returned projection must independently own.
- Anti-pattern: parallel owned and borrowed semantic implementations, complete array clone before iteration, fallback conversion, or moving request payloads into MetadataCenter.
- Evidence target: a source residue gate rejecting complete owner clones, focused semantic tests, feature/resource/mainline map bindings, rustfmt, native build, and no RSS claim without installed-runtime concurrent replay.
# Terminal provider failure logs

- Trigger: final HTTP failure line has status/code but cannot identify the selected target.
- Trace the actual final attempt target; attach `providerKey` and provider wire `providerModel` to the original error as diagnostic observation before terminal projection.
- Render only present values. Never infer provider/model from message text, aliases, route names, snapshots, or MetadataCenter payload mirrors; never overwrite existing observation truth or change retry/client payload semantics.

### L93-184 Provider failure blackboxes must use a controlled failing listener

- Trigger: a retry/reselection test obtains an ephemeral TCP port, drops its listener, and assumes later connections to that port will fail.
- Action: keep a dedicated controlled upstream listener alive and return the exact failure status/body required by the test, such as HTTP 503. Shut it down explicitly after the assertion.
- Reason: a released ephemeral port can be reused by the Server under test or another concurrent test, turning the intended closed-port failure into recursive/self traffic or an unrelated endpoint.
- Boundary: closed-port tests remain valid for a focused transport connection-error case when no later listener allocation can reuse the address. Multi-listener lifecycle/reselection tests must use controlled HTTP failure instead.
- Evidence target: repeat the focused reselection blackbox at least ten times, then run the full workspace and P0-P5 regression stack.
# L93-5520-default-floor-multi-candidate

- Trigger: live logs show `No available providers after applying routing instructions` or temporary route-unavailable projection while VR status still lists configured default tiers.
- Check: distinguish configuration-valid candidates from currently healthy candidates. Inspect both `gateway:<route>` and `gateway:default`; a non-empty default route must retain one ordered candidate even when every candidate is temporarily cooled, busy, or excluded.
- Owner: fix only Rust Virtual Router selection. Do not add a handler error swallow, TS retry loop, synthesized fallback chain, or config patch.
- Regression: cover multiple enabled default providers all temporarily unavailable, singleton exclusion, and disabled-only default. The first two must retain a protected candidate; disabled-only must remain fail-fast.
