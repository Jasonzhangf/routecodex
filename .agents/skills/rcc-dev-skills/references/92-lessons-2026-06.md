# 92 Lessons 2026-06

### L92-59 reasoningStop live closeout 只看 client-visible exec_command，不看内部 reasoning 文本
- 触发：5555 live probe 里仍能看到 reasoning summary 文本包含 `reasoningStop`，容易误判成客户端注入。
- 真源：`reasoningStop` 只要没有出现在 client-visible payload / required_action 里，就仍是内部 reasoning；真正对客户端可见的收口结果是 `exec_command(routecodex hook run reasoningStop --input-json ...)`。
- 动作：判断 stopless 是否闭环时，先看 `hasExecCommand` / `hasReasoningStop` / `required_action.tool_calls`，不要把内部 reasoning summary、日志或 provider raw body 里的 token 当成客户端透传。
- 反模式：看到内部 reasoning 文本里出现 `reasoningStop` 就直接判定“还在注入”；或者把第二轮 `Responses conversation expired or not found` 误诊成拦截失效。
- 验证：`node scripts/tests/stopless-5555-live-probe.mjs`、`tests/server/handlers/responses-handler.servertool-cli-projection.blackbox.spec.ts`、`cargo test -p servertool-core stopless --lib -- --nocapture`。

### L92-58 纯映射 helper 只要 Rust 已有真源，就别在 TS 留 switch
- 触发：`stop-message-auto.ts` 里的 `normalizeStoplessTriggerHintForMetadata()` 只是把 `stop_schema_*` reason code 映射成 `no_schema / invalid_schema / non_terminal_schema / budget_exhausted / schema_pass`，而 Rust `cli_contract.rs` 已经有同等映射。
- 真源：这种纯字符串映射属于 Rust owner；TS 只应保留 bridge 名字，不保留决策 switch。
- 动作：先把映射补成 Rust `pub fn`，再在 NAPI 导出同名 capability，最后让 TS 同名函数调用 native wrapper。
- 反模式：为了省一步而在 TS 留下“看起来无害”的 switch/map；这类 helper 很容易和 Rust 真源分叉。
- 验证：Rust focused test + `build-native-hotpath` + 串行 Jest + `verify-servertool-rust-only` + `build-core`，并确保 Jest 不是抢在 native rebuild 前并行启动。

### L92-45 retired backend/reenter mainline 的 unsupported_backend_plan_kind 不能穿透成成功结果
- 触发：`engine.ts` 已把 followup/reenter mainline 改成 `SERVERTOOL_REENTER_RETIRED`，但 `execution-handler-materialization-shell.ts` 遇到 native `unsupported_backend_plan_kind` 时没有显式处理，最后 `return planned as ServerToolHandlerResult`，把本应 fail-fast 的 backend plan 静默包装成成功。
- 真源：retired backend/reenter 不是“跳过执行后当成功结果返回”；native `planServertoolHandlerRuntimeActionWithNative()` 一旦给出 `unsupported_backend_plan_kind`，TS shell 必须立即投影成 `planServertoolHandlerContractErrorWithNative({ kind: 'unsupported_backend_plan_kind' ... })`，不能透传 plan。
- 动作：focused red test 不要再依赖已删除的 `executeServertoolBackendPlan` / `handlers/vision.ts` / `handlers/web-search.ts`，直接测 `materializeServertoolPlannedResult(...)`；shell 显式拦 `unsupported_backend_plan_kind`，和旧 compat action `execute_backend_vision_analysis_then_finalize` / `execute_backend_web_search_then_finalize` 同样 fail-fast。
- 反模式：看到 runtime action 不是 `finalize_without_backend` / `return_handler_result` 就默认 `return planned as result`；或者为了过 focused Jest 复活旧 backend executor TS 文件。
- 验证：`tests/servertool/execution-shell.backend-failfast.spec.ts`、`tests/servertool/stopless-metadata-center.spec.ts`、`tests/servertool/servertool-active-orchestration-audit.spec.ts` 绿；随后 `node scripts/verify-servertool-rust-only.mjs` 与 `node scripts/build-core.mjs` 也必须继续绿。

### L92-43 registry projection 的“记录回绑”也必须由 native 给 sourceIndex
- 触发：`registry-impl.ts` 已把 name order / trigger grouping 下沉给 native，但 `listRegisteredToolHandlerRecordsImpl()` 仍在 TS 本地 `findIndex + used set`，按 `name + trigger` 把 projection record 再匹配回 `rawRecords`。
- 真源：对于 registry projection，Rust 不只该决定“输出顺序”，还该决定“这个输出项对应哪个原始 record slot”。JS handler/function 不能进 Rust，但 `sourceIndex` 可以；TS 只应按索引绑定 `registration/handler`。
- 动作：给 `ServertoolRegistryProjectionRecordInput/Plan` 增加 `source_index`，Rust 在 projection 后原样带出；TS 改为 `rawRecords[recordPlan.sourceIndex]` 校验并绑定，不再自己 `findIndex`。
- 反模式：在 TS 里继续保留 `const used = new Set<number>()`、`rawRecords.findIndex(...)`、或 `native registry record order missing entry for ...` 这类“native 排完序，TS 再自己找一遍”的第二份语义。
- 验证：Rust focused `registry_projection_normalizes_names_groups_records_and_rejects_duplicate_auto_handlers`、router-hotpath-napi bridge test、`servertool-cli-native-bridge.spec.ts`、`server-side-tools.auto-hook-config.spec.ts`、`servertool-active-orchestration-audit.spec.ts`、`verify-servertool-rust-only.mjs`。

### L92-42 servertool focused Jest mock 要跟 native registration 真相同源
- 触发：`adhoc-handler-test-support.ts` 已改成只消费 native `normalize_servertool_registration_spec_json`，但 focused Jest 仍把 ad-hoc `trigger=auto` 默认成 `executionMode='guarded'` 且没有 `autoHook`，同时还在期待 `vision_auto/web_search` 继续存在于 registry builtin 列表。
- 真源：registration 默认值与 auto-hook 默认 `phase/priority` 都由 Rust `router-hotpath-napi/src/servertool_skeleton_config.rs::normalize_servertool_registration_spec_json` 定义；当前 builtin handler 真相只剩 `stop_message_auto`，不是 skeleton doc 里声明过的全部 internal tools。
- 动作：修 focused Jest 时，优先对齐 native mock 与 builtin catalog 当前真相：
  - `trigger === 'auto'` 默认 `executionMode='auto_hook'`，并产出 `autoHook = { id, phase: 'default', priority: 100 }`；
  - `listRegisteredServerToolHandlerNames/Records` 断言只验当前 registry 真正暴露的 builtin，而不是把已退役或未注册的 internal tool 名写死。
- 反模式：把 focused Jest 当成“设计愿景”去硬写旧 builtin 列表；或者为满足旧断言去复活 `vision_auto/web_search` TS builtin/runtime 入口。
- 验证：`tests/servertool/server-side-tools.auto-hook-config.spec.ts` + `tests/servertool/servertool-active-orchestration-audit.spec.ts` 绿；随后 `node scripts/verify-servertool-rust-only.mjs` 与 `node scripts/build-core.mjs` 也必须继续绿。
### L92-44 response-stage capabilities 不能再由 TS 壳层硬编码 false
- 触发：`server-side-tools-impl.ts` 之前把 response-stage `capabilities` 写死成 `{reenterPipeline:false, providerInvoker:false, clientInjectDispatch:false}`，导致 `planServertoolResponseStageGateWithNative` 永远拿不到“当前 runtime 是否具备该能力”的真实信息；focused failfast spec 必须把 capability 验回去。
- 真源：response-stage runtime capabilities 是 duck-typing 探测（`typeof record.providerInvoker === 'function'`），不应该由 TS 壳层伪造固定 false；TS 壳层也不应该用强转 `options as Record<string, unknown>` 来骗 helper。
- 动作：抽 `deriveServertoolRuntimeCapabilities(args: unknown)`，调用点直接 `deriveServertoolRuntimeCapabilities(options)`；failfast spec 的 native mock 对齐 Rust `normalize_servertool_registration_spec_json` 当前默认值。
- 反模式：在 TS shell 里继续写 `capabilities = { reenterPipeline:false, providerInvoker:false, clientInjectDispatch:false }`；或为了 helper 接受强类型结构而用 `as Record<string, unknown>` 撒谎式强转。
- 验证：`tests/servertool/server-side-tools.failfast.spec.ts` + `servertool-active-orchestration-audit.spec.ts` 绿；`node scripts/verify-servertool-rust-only.mjs` 与 `node scripts/build-core.mjs` 绿。

### L92-41 servertool verify gate 必须跟着退役设计改，不能为了过 gate 复活旧 TS
- 触发：`verify-servertool-rust-only.mjs` 仍要求 `web_search/vision_auto -> BackendRouteReenter`、`memory_cache_auto`、以及已删除的 `backend-route-mainline/reenter/bootstrap-replay` TS 文件，导致新设计明明已生效却被旧 gate 挡住。
- 真源：gate 只应该校验当前目标设计。对这条线，`web_search/vision_auto` 的真相是 client-visible servertool CLI + `routeHint` 恢复；旧 backend-route/reenter 文件必须保持物理删除。
- 动作：先用代码和 focused tests 确认当前真相，再改 gate 去检查 `route_hint_for_client_exec_tool`、`extract_servertool_cli_result_route_hint_from_request(_json)`、`executor-metadata` routeHint 恢复测试，以及旧 backend-route TS 壳缺失。
- 反模式：为了让旧 gate 变绿去恢复已退役的 TS backend-route 文件、`memory_cache_auto` 样例、或 `executeServertoolBackendPlan` 之类旧出口。
- 验证：`PATH=/opt/homebrew/opt/node@22/bin:$PATH node scripts/verify-servertool-rust-only.mjs` 绿，且 `node scripts/build-core.mjs` 绿；不允许以“恢复旧文件”方式达成。

### L92-40 legacy reasoningStop shell history must be physically dropped
- 触发：`codex-samples` 里出现旧坏历史：assistant/tool 把 `reasoningStop` 当成 `exec_command(cmd="reasoningStop")`，随后落到 `zsh:1: command not found: reasoningStop`。
- 关键判断：这类记录不是“还要继续修 schema 引导”，而是旧 shell 投影污染；只要请求里已经暴露标准 `reasoningStop` tool，shell 形态的裸 `reasoningStop` 历史就应当被物理移除，不能继续带进后续 provider-request。
- 可复用动作（最小闭环 3-4 步）：先把 sample 变成 red test；在 request normalization 里识别 `exec_command` + `cmd=reasoningStop` / `reasoning_stop` 且工具池已含 `reasoningStop`；把这对 assistant/tool 历史项删掉；再跑 sample 回放确认后续 request 不再包含 `reasoningStop` shell 痕迹。
- 反模式/边界：不要把 `command not found: reasoningStop` 当作普通 shell 输出保留；不要再通过文案修补去兜旧历史；如果是合法的 `reasoningStop` function tool，则应走结构化 tool-call / arguments，而不是 shell 命令。
- 验证口径：request-side normalize 后，`provider-request` / `messages` / `input` 中不应再出现旧 shell 形态的 `reasoningStop`，也不应残留对应 `command not found` tool output。

### L92-39 registry auto-hook descriptor 默认值和 duplicate id 也必须 native owner
- 触发：`registry-impl.ts` 已把 register/get action 下沉到 Rust，但 `collectAutoServerToolHooksImpl()` 仍在 TS 本地决定 `phase/priority/order` 默认值，并隐式容忍 duplicate hook id。
- 真源：auto-hook descriptor 的 canonical id、默认 `phase/priority/order`、duplicate id fail-fast 都属于 hook skeleton/registry 业务语义，必须由 native plan 输出；TS 只能把 native descriptor 回绑到 JS handler/reference。
- 动作：新增 native registry auto-hook descriptor plan；TS 仅传 raw `{id, phase, priority, order}`，再把返回的 descriptor 绑定回 `registration/handler`。
- 反模式：在 TS 里继续写 `entry.autoHook?.phase ?? 'default'`、`priority ?? 100`、`order ?? 0`，或默认 duplicate hook id 静默覆盖/并存。
- 验证：Rust focused `registry_contract`、router-hotpath-napi bridge test、`servertool-cli-native-bridge.spec.ts`、`servertool-active-orchestration-audit.spec.ts`、`verify-servertool-rust-only.mjs`。

### L92-38 servertool 全开发/debug 流程必须作为 skill 真源落盘
- 触发：servertool hook skeleton 目标骨架已经进 wiki/mainline，但团队开始按阶段 Rust 替换、黑盒闭环、删 TS 时，实施口径容易散落在聊天、goal 或 `note.md`。
- 真源：wiki/mainline/manifest 只锁目标骨架；`references/22-servertool-hook-skeleton-workflow.md` 与 `references/23-servertool-hook-dev-debug-flow.md` 才是 servertool 开发/debug 流程真源。
- 动作：把“先锁目标文档 -> 审计活 TS -> 红 gate -> Rust owner 下沉 -> 串行验证 -> 黑盒/旧样本 replay -> anchored map -> 删 TS”的主流程、ASCII、验证顺序直接写进 skill/lessons。
- 反模式：目标文档写得很全，但开发/debug 仍靠聊天口述；或只把流程记在 `note.md`，下次又重新解释。
- 验证：主 `SKILL.md` 能路由到 `22/23`；`23` 可独立指导执行；`60-note-memory-flow.md` 明确规定整条开发/debug 主流程属于 skill，而不是 note。

### L92-37 auto-hook caller 只执行 native action，不自判 result 分支
- 触发：`auto-hook-caller.ts` 已有 Rust `plan_auto_hook_execution_decision` / `plan_auto_hook_queue_progress`，但 TS 仍靠 `!planned`、`result`、`queueResult` 本地决定 continue/return/error。
- 真源：auto-hook 单次尝试 outcome 与 optional->mandatory queue progression 都属于 Rust owner；TS 只能执行 handler/materialization IO，再按 native action fail-fast。
- 动作：单次 hook 执行后统一先拿 native decision，再按 `return_result / continue_queue / rethrow_error` 执行；若 native action 和 materialization 事实矛盾，立即 fail-fast，不能静默吞掉。
- 反模式：看到 materialized result/non-result 就直接在 TS `if (result)` 分支返回；或看到 `planned === null` 就在 TS 本地 continue，不经过 native decision。
- 验证：focused `servertool-auto-hook-trace.spec.ts` 证明 mismatch 会 fail-fast；`servertool-active-orchestration-audit.spec.ts` 与 `verify-servertool-rust-only.mjs` 禁止 `if (!planned)` / `if (result)` 复活。

### L92-36 servertool Rust focused test 先确认 Cargo workspace 站位
- 触发：按标准顺序跑 `cargo test -p servertool-core ...`，结果直接报 `could not find Cargo.toml`。
- 真源：servertool Rust workspace 在 `sharedmodule/llmswitch-core/rust-core/`，不是 repo 根。
- 动作：所有 servertool Rust focused / bridge tests 先切到 `sharedmodule/llmswitch-core/rust-core/` 再跑；只有 workspace 站位正确后，失败才有资格算真实红测。
- 反模式：把 repo 根的 `Cargo.toml not found` 误判成 crate 丢失、slice 断线或 NAPI bridge 坏了。
- 验证：同一命令切到 `rust-core/` 后正常得到 test pass/fail 结果，而不是 workspace 错误。

### L92-35 servertool NAPI 黑盒必须先 build 再串行跑 Jest
- 触发：servertool Rust slice 明明已实现，但 focused Jest / CLI 黑盒成串报 `native unavailable`。
- 真源：`23-servertool-hook-dev-debug-flow.md` 的串行验证顺序；新 NAPI 导出只有在 `build-native-hotpath` 完成后才会进入 `.node`。
- 动作：固定顺序为 Rust focused tests -> NAPI bridge tests -> `build-native-hotpath` -> Jest/CLI blackbox `--runInBand` -> verify gate；不要把 native build 和 Jest 并行。
- 反模式：看到 `buildClientExecCliProjectionOutputJson native unavailable`、`planServertoolEngineSkipJson native unavailable`、`inspectStopGatewaySignal native unavailable` 就误判成业务逻辑回退或 bridge 缺失。
- 验证：重跑串行顺序后，假失败消失；剩余失败才值得继续查真实 owner。

### L92-34 servertool 目标真源和执行真源要分开锁
- 触发：servertool hook skeleton 已明确目标，但团队准备进入分阶段 Rust 替换、黑盒闭环、删 TS。
- 真源：wiki/mainline/manifest 锁目标骨架；`22-servertool-hook-skeleton-workflow.md` 与 `23-servertool-hook-dev-debug-flow.md` 锁实际开发/debug 流程。
- 动作：先更新目标文档，再把“审计活 TS -> 红 gate -> Rust owner 替换 -> 白盒/黑盒 -> 旧样本 replay -> anchored map -> 删 TS”写进 skill，避免流程只存在聊天上下文。
- 反模式：拿 wiki 当实施说明书，或把实施步骤散落在 note/goal/聊天里；下次执行时又得重新解释一遍。
- 验证：review 面负责看目标，skill 能单独指导执行，且 `binding pending` 阶段不会被误报成已实现。

### L92-33 servertool 开发与 debug 流程要写成独立 skill reference
- 触发：servertool hook skeleton 目标已经锁定，但团队开始进入分阶段 Rust 替换、黑盒闭环、删 TS 收口。
- 真源：`.agents/skills/rcc-dev-skills/references/22-servertool-hook-skeleton-workflow.md` + `23-servertool-hook-dev-debug-flow.md`；wiki/mainline 只锁目标，不代替动作流程。
- 动作：把“文档锁定 -> 红测/red gate -> Rust owner 替换 -> 白盒/黑盒 -> 旧样本 replay -> function-map/mainline anchored -> 删 TS”沉淀成独立 skill；主 `SKILL.md` 只保留路由。
- 反模式：把开发/debug 口径散落在 note、聊天或 goal 文档里；或把大段流程直接塞回主 `SKILL.md`。
- 验证：主 `SKILL.md` 有 `23` 路由，reference 文件可单独指挥实施与 debug，且不依赖聊天上下文。

### L92-32 servertool CLI lifecycle 与 hook 骨架不能混淆
- 触发：servertool / stopless / reasoning_stop / followup / reenter 改造，或用户要求“hook 注入/拦截流程骨架”。
- 真源：`docs/architecture/wiki/servertool-hook-skeleton-mainline-source.md`、`docs/architecture/mainline-call-map.yml` `servertool.hook_skeleton.mainline`；`function-map.yml` / `verification-map.yml` 只能在 Rust owner symbols 和 tests 存在后登记，禁止伪造 canonical builders。
- 动作：业务执行仍走 client-visible CLI：`routecodex hook run <toolName> --input-json <json>`；hook 只治理响应端拦截/schema 校验/hook 注入响应/followup/reenter/finalize，以及请求端结果解析/文本替换/工具注入/finalize。
- 反模式：把 hook 当成 CLI 替代方案；把 stopless CLI 改回 server-side followup/reenter；让 TS 注册/排序/选择业务 hook 或重判 schema/terminal/retry/backoff。
- 验证：unit 必须覆盖正常响应、异常响应、空 schema/no_schema、invalid/malformed、terminal/non-terminal/already-terminal、required missing、optional no-op、multi-hook 顺序；黑盒必须覆盖 client->provider->client CLI->request restore->provider 必经之路和 direct negative。

### L92-27 protocol 是入口/目标合同，不是 payload 自动解析结果
- 触发：`/v1/responses` entry + `openai-chat` provider 出现 response parser 502、或有人想按 SSE/JSON/body shape 推协议。
- 真源：entry path 锁 inbound/client protocol；VR target/outboundProfile 锁 provider outbound/response inbound protocol；SSE/JSON 只决定 framing。
- 动作：查 `providerProtocol/clientProtocol/entryEndpoint` 是否分别来自正确 owner；缺 protocol 必须 fail-fast；删除 metadata/SSE override 和默认 `openai-chat` 兜底。
- 反模式：用 `sseProtocol/clientSseProtocol/routeSseProtocol`、payload shape、响应是否有 `choices/output` 来改协议；把 responses entry 当 provider response parser 协议。
- 验证：Rust protocol fail-fast + response remap 测试，provider-direct/request-executor focused Jest，SSE normalize/stage residue gate。

### L92-01 real sample 必须先红后绿再在线回放
- 触发：修 bug / 新功能 / 请求标准化问题
- 真源：真实错误样本 + red test + live replay
- 动作：先固化样本，再红测，再修，再在线重放旧样本
- 反模式：先修代码、后补测试、只跑单测
- 验证：red->green + online replay

### L92-02 function map 已进入 owner registry 基线
- 触发：改关键 owner、加 `feature_id`
- 真源：function-map + verification-map + source anchor
- 动作：三件套同时改，再跑三大 gate
- 反模式：只补 map，不补 source/gates
- 验证：compile-gate + owner-queryability + growth-discipline

### L92-03 `~/.rcc` 是唯一配置真源
- 触发：查 provider / route / port 行为
- 真源：`~/.rcc/config.toml`、`~/.rcc/provider/<id>/config.v2.toml`
- 动作：先看 `~/.rcc`，后看 merged snapshot
- 反模式：先查 repo `config/`
- 验证：provider sample + live log

### L92-04 Responses continuation 必须三重隔离
- 触发：`previous_response_id` 串错协议 / 串 owner / 串 scope
- 真源：entry protocol + continuationOwner + session/conversation(+port/group)
- 动作：store/restore/materialize 都按三键隔离
- 反模式：只靠 session scope 命中历史
- 验证：store red/green + live continuation probe

### L92-05 direct/server boundary 要持续收缩
- 触发：server handler 开始自己解析 protocol、metadata、SSE shape
- 真源：responses request/response bridge
- 动作：把协议判断、payload normalization、continuation 决策迁到 bridge/native
- 反模式：handler 本地再长出第二套协议逻辑
- 验证：single-bridge surface gate

### L92-06 stopless schema 只把三轮当 no-change guard
- 触发：模型一直继续但 schema 已完整
- 真源：Rust `stop-message-core`
- 动作：完成态立即停；`no_change_count` 才算三轮 guard
- 反模式：把普通 used 计数当主预算
- 验证：Rust tests + live stopless probe

### L92-07 plain-text tool_result 里的 media key 不能误判成历史图片
- 触发：provider payload 把普通文本变成 `[Image omitted]`
- 真源：Rust compat/media scrub owner
- 动作：先看 outbound compat/media scrub，不要误怪 chat bridge
- 反模式：看到 `image_url` 字样就当历史图片
- 验证：provider-request snapshot + live replay

### L92-08 direct same-protocol tools 不等于必须 relay
- 触发：因为 `tools[]` 就想强制 relay
- 真源：same-protocol direct 只看 entry protocol 与 target protocol
- 动作：合法 flat Responses tools 继续 direct；协议 shape 错误去查 outbound owner
- 反模式：把“有工具”当 direct->relay 条件
- 验证：direct contract red tests + live `/v1/responses`

### L92-09 10000 loopback empty reply 先查 shadow listener
- 触发：`127.0.0.1:10000` 空回复，但别的地址正常
- 真源：本机 loopback shadow service
- 动作：先看 `netstat/lsof`，再用 LAN IP 验 RCC
- 反模式：立刻判定 RouteCodex handler 坏了
- 验证：`/health` on non-loopback local IP

### L92-10 build/install/health 版本真相看 `~/.rcc/install/current`
- 触发：怀疑当前运行的不是刚装的版本
- 真源：`~/.rcc/install/current` + `/health` + `routecodex --version`
- 动作：三者对齐后再做 live probe
- 反模式：只看本地 build 成功就当 runtime 已更新
- 验证：version + health + SSE completed/done

### L92-11 relay duplicated tool-batch 要先查 Rust req_inbound capture
- 触发：`orphan_tool_result`、`already-consumed call_id`、包装成 native capture unavailable
- 真源：Rust `hub_req_inbound_context_capture.rs`
- 动作：先固化真实 `/v1/responses` duplicated `function_call` + `function_call_output` 样本；修 Rust 归一化；同时补 Rust owner 测试和 bridge/native gate
- 反模式：去 handler/request bridge 本地重排 raw history，或把包装错误当成 export 缺失
- 验证：Rust red->green + live replay old sample

### L92-12 apply_patch grammar 400 查 Rust request tool schema
- 触发：same-protocol direct `/v1/responses` 上游报 `Invalid lark grammar ... unknown name: "begin_patch"`
- 真源：Rust `req_process_stage1_tool_governance_blocks/orchestrator.rs::normalize_apply_patch_freeform_tool_schema`
- 动作：确认工具发布为 Responses `custom` + 完整 lark grammar；跑 `verify:apply-patch-freeform-contract` 和 5520/5555 online smoke
- 反模式：在 server handler / SSE / relay store 里修补 apply_patch shape，或恢复 servertool apply_patch handler
- 验证：`custom_tool_call.input` 是 raw patch，`functionArgumentPatchLeakCount=0`

### L92-13 direct SSE `response.metadata` 先分事件名与内部 carrier
- 触发：`[response.sse.stream] direct passthrough SSE emitted non-Responses event "response.metadata"`
- 真源：`server.responses_sse_bridge_surface` / `responses-response-bridge.ts` direct SSE allowlist + metadata guard
- 动作：按 mainline/function map 定位 direct SSE bridge；普通 provider `event: response.metadata` 应允许透传，`providerKey` / `__rt` / `__routecodex*` 仍必须 fail-fast
- 反模式：把合法 provider metadata event 当成内部 metadata 泄漏，或在 provider runtime/Hub projection 里做转换补偿
- 验证：focused guard spec 正反路径 + 5520 live `/v1/responses` replay

### L92-14 direct `/v1/responses` 首包超时先查 provider SSE headers clock
- 触发：`router-direct.send` / direct `/v1/responses` 报 `UPSTREAM_HEADERS_TIMEOUT`，尤其是 longcontext/tools 路由
- 真源：`src/providers/core/utils/http-client.ts::postStream` 的 headers timeout，以及 `src/providers/core/runtime/responses-provider.ts` 传入的 `headersTimeoutMs`
- 动作：先看 `DEFAULT_TIMEOUTS.PROVIDER_STREAM_HEADERS_CAP_MS` 和 provider timeout 是否被 `min(...)` 过早截断，再查 metadata/profile 是否正确透传 `providerStreamHeadersTimeoutMs`
- 反模式：去清理入站、改 router-direct 出站、或在 direct SSE bridge 上做补偿；这些都不是 timeout owner
- 验证：`tests/provider/http-client-poststream-headers-timeout.spec.ts` + `tests/providers/runtime/responses-provider.direct-passthrough.spec.ts` + 5520 live `/v1/responses`

### L92-15 SSE 请求回 JSON 查 transport content-type
- 触发：provider 配了 `streaming="always"` 或请求 `stream=true`，日志报 `SSE_DECODE_ERROR`，但上游直测 JSON/SSE 本身可用。
- 真源：`src/providers/core/utils/http-client.ts::postStreamOrResponse` + `HttpRequestExecutor` / `ResponsesProvider` / OAuth replay 的实际响应类型分流。
- 动作：同一次上游请求按响应 `Content-Type` 处理；`text/event-stream` 走 SSE converter，`application/json` 直接走 provider JSON response path；旧 `postStream()` 不能把 JSON/text 包成假 SSE stream。
- 反模式：在 Hub Pipeline / chat process / response converter 猜测修补，或把 JSON-over-SSE 当 provider 不可用。
- 验证：`tests/providers/core/runtime/protocol-http-providers.unit.test.ts` 中 OpenAI chat + ResponsesProvider + DeepSeek refresh replay JSON-over-SSE 红测；`tests/provider/http-client-poststream-headers-timeout.spec.ts` 中真实 HTTP server content-type 分流红测。

### L92-15 architecture review surface 必须被 build wiring gate 锁住
- 触发：function map / mainline call map / wiki / manifest 已有 gate，但本地 build 或 CI 仍可能绕过其中一层
- 真源：`package.json` scripts + `scripts/architecture/verify-function-map-build-wiring.mjs`
- 动作：把 review surface 分 light/full 两档；build/build:min 跑 light，architecture-ci 跑 full + longtail，再让 build-wiring gate 反向检查这些入口
- 反模式：只新增 `verify:*` 脚本但不接入 build/CI，或接入后没有 gate 防止未来删掉
- 验证：`npm run verify:architecture-review-surface` + `npm run verify:architecture-ci-longtail` + `npm run verify:function-map-compile-gate`

### L92-16 request truth 不得回写平铺 metadata
- 触发：stopless/usage log/session backoff 明明应该读到真实 `sessionId`，但 merge/backfill 后又被顶层 `metadata.sessionId/conversationId` 污染或覆盖
- 真源：`src/server/runtime/http-server/metadata-center/metadata-center.ts` 的 `request_truth`
- 动作：request 入口只写 `MetadataCenter.request_truth`；attempt merge 后显式删除顶层 `sessionId/session_id/conversationId/conversation_id`；消费方统一走 `readRuntimeRequestTruthIdentifiers(...)`
- 反模式：把 request truth 再 materialize 回顶层 metadata，或从 tmux-only / flat aliases 猜 request session 真相
- 验证：focused Jest (`executor-metadata` / `request-truth-readers` / `session-storm-backoff` / `servertool-adapter-context`) + live 5555 stopless probe

### L92-17 executor 后续 consumer 也必须切到 request truth reader
- 触发：executor 带里 `registerRequestLogContext(...)` / goal-state persistence 仍在直接读 `initialMetadata.sessionId`、`mergedMetadata.conversationId`
- 真源：`readRuntimeRequestTruthIdentifiers(...)`
- 动作：继续扫 `request-executor-request-state.ts`、`goal-state-persistence.ts` 这类 followup consumer；有 center truth 时只认 center，没有 center truth 时宁可不激活也不伪造
- 反模式：因为 merge 后 flat 字段没了，就偷偷把 flat backfill 恢复回来；或把 continuation/request scope 混成一套 session 真相
- 验证：focused Jest (`request-executor-request-state.spec.ts` / `goal-state-persistence.spec.ts`) + `build:min` + install/restart + 5555 live stopless probe

### L92-18 metadata-center manifest 必须绑定代码
- 触发：`metadata-center-manifest.yml` 增加 family/slot，但 `MetadataCenter` 类型/state/writer/reader/release 未同步
- 真源：manifest + `MetadataCenter` 实现必须双向一致
- 动作：新增 family 时同步 type、state bucket、write/read 方法、`markReleased` 覆盖，并跑 `verify:architecture-metadata-center-manifest-code-sync`
- 反模式：wiki/manifest 已声明就宣称完成，或把 `client_attachment_scope` / `debug_snapshot` 当 request truth fallback
- 验证：manifest-code sync gate + request-truth negative Jest + review-surface-light

### L92-19 provider auth/quota 错误不得 client-visible
- 触发：客户端/SSE 出现 `Upstream authentication failed`、`HTTP_401/403`、provider request id。
- 真源：`error.client_projection` / `src/server/utils/http-error-mapper.ts`，handler 只能调用统一 public projection。
- 动作：先补 handler 黑盒 + mapper leak guard；401/403 投影为 generic `502 upstream_error`，不补 `request_id`；日志只保留 status/code 字段，不打 raw provider body。
- 反模式：只遮 raw upstream message 但保留 auth/quota 文案和 HTTP_401/403；或让 handler 绕过 ErrorErr06 gate 直接 `mapErrorToHttp`。
- 验证：`handler-utils.error-response-shape`、`http-error-mapper-public-leak`、`request-error-log`、`direct-passthrough-route-level`。

### L92-20 provider-health 持久化必须吃 ErrorErr runtime scope
- 触发：`verify:provider-failure-ban-blackbox` 发现 `provider-health.json` 写到 `RCC_HOME/sessions/provider-health.json` root，造成跨端口/跨 routing group 污染风险。
- 真源：Rust `virtual_router_engine::engine::events` 消费 ErrorErr04 时按 `event.runtime.sessionDir` 进入 state store；`routing_state_store` 只提供 scoped override / disabled guard。
- 动作：先让黑盒红测扫描所有 `provider-health.json` 路径；修 Rust ingress scope；TS reporter 只投影 request-scoped runtime hints，不负责重新拼路径。
- 反模式：在 `provider-error-reporter.ts` / request-executor 里用 `RCC_HOME` 或 server/port 字符串补一份路径；或让无 `sessionDir` 的 provider event 继承 ambient root。
- 验证：Rust 正反向测试 + `provider-error-reporter.spec.ts` + `build:base` + `verify:provider-failure-ban-blackbox`。

### L92-21 MetadataCenter side-channel 不能被 side-by-side JS 生成物劫持
- 触发：Jest/ESM 下 runtime-control 已写入 `MetadataCenter`，但 nested/retry metadata 读回 `undefined` 或错误值。
- 真源：`src/server/runtime/http-server/metadata-center/*.ts`，仓库源目录不得存在同名 `.js/.d.ts` 生成残留。
- 动作：先查同目录 side-by-side emit；删除陈旧 `.js/.d.ts`，把路径加入 `verify:architecture-deleted-path`；`MetadataCenter.attach/read` 用结构化 duck-type，不用 `instanceof`。
- 反模式：把读不到 side-channel 误修成 flat metadata / `__rt` fallback，或让 JSON retry clone 丢掉 symbol 绑定。
- 验证：focused followup Jest + `verify:architecture-deleted-path` + `verify:architecture-ci`。

### L92-22 SSE stream 不得从 normal body wrapper 取
- 触发：response handler / bridge 通过 `body.sseStream`、`hasResponsesSsePayloadForHttp`、`hasSsePayload` 决定 SSE。
- 真源：stream 只走 `PipelineExecutionResult.sseStream` 内部执行结果；forceSSE JSON bridge 只消费标准 Responses/chat JSON。
- 动作：删除 body wrapper predicate/export/barrel；client JSON guard 把 `sseStream` 当内部 carrier fail-fast；旧测试改成 result-level stream + 标准 body。
- 反模式：为修 SSE 缺流把 stream/read/probe 塞进 response body 或 `__routecodex_stream_*` wrapper。
- 验证：SSE wrapper contract + forceSSE JSON bridge + split-frame focused Jest，custom-payload audit，architecture-ci。

### L92-23 servertool 控制语义只走 MetadataCenter
- 触发：servertool/followup/stopless 代码读写 `__rt.serverToolFollowup`、`stoplessGoalState`、`stopMessageClientInject*`、`serverToolLoopState` 等控制字段。
- 真源：`MetadataCenter.runtime_control`；需要喂给 Rust/native 时只投影到内部 `runtime_control` side-channel，禁止回写 normal metadata 或 `__rt`。
- 动作：入口先绑定 MetadataCenter；nested/followup builder 剥离旧 flat/`__rt` 控制键；测试 fixture 也必须写 runtime_control，不准用 legacy key 当正向输入。
- 反模式：读不到 side-channel 时补 `__rt` fallback，或为持久化 helper 临时生成 `stopMessageClientInjectSessionScope` flat 字段。
- 验证：servertool focused Jest + `tsc --noEmit` + custom-payload audit/containment + grep legacy active reads。

### L92-24 provider error 不能只在类型层声明 defaultPoolAvailable
- 触发：`ErrorErr05` 已有 `mayProject/defaultPoolAvailable` 字段，但 provider 错误仍过早 client-visible，或 direct 401/403/auth/quota 被锁成 fail-fast。
- 真源：`error.execution_decision_consumer` 必须从 VR/default-pool planner 传入真实 `defaultTierAvailable`；`router-direct` 不得用 auth/quota 特例绕过 pool/default exhaustion gate。
- 动作：先查 `resolveRequestExecutorProviderFailurePlan -> resolveProviderRetryExecutionPlan` 是否传 default truth，再查 `direct-decision.ts` 是否有 terminal auth early return；旧测试若锁 401 fail-fast，先改红测再删早返。
- 反模式：只在 `ErrorErr05` 类型/文档里加 `defaultPoolAvailable`，调用点不传；或 `http-error-mapper` 用 `details.policyExhausted` 伪装成完整 execution decision。
- 验证：`error_chain_may_project_gate`、direct candidate-exhaustion、http-error-mapper policy-exhausted gate、Rust `primary_exhausted_to_default_pool`、5555 default-pool live replay。

### L92-25 auth/quota provider error live 验证不要改生产凭据
- 触发：目标要求验证 401/403/INVALID_API_KEY/INSUFFICIENT_QUOTA 先切，但生产 provider 当前没有自然 auth/quota 错误样本。
- 真源：installed `dist` runtime + mock upstream 黑盒能证明 ErrorErr05/direct/executor 真实链路；生产 5555 只用于自然 provider 5xx/default-pool replay。
- 动作：扩展 `verify:provider-failure-ban-blackbox`，mock primary 返回 401/403/429 quota，backup 200，断言 `provider-switch -> backup -> clientStatus=200`；不要改真实 `~/.rcc/provider/*` 凭据制造错误。
- 反模式：为拿 live 样本篡改生产 key/config，或只用 unit test 宣称 auth/quota live closure。
- 验证：`npm run verify:provider-failure-ban-blackbox` 输出 `scenario401/scenario403/scenarioInsufficientQuota clientStatus=200`，再用 5555 live 日志证明自然 5xx/model_not_found 先切后 200。

### L92-26 provider 配置错误映射与 forwarder sticky 要拆开修
- 触发：上游把账号池耗尽报成 HTTP 400，且同 session 反复命中同一坏 provider。
- 真源：错误映射在 provider config `errorMapping.rules[]` / `extensions.errorMapping.rules[]`，单条规则只包含 `origin` 和 `to`；经 Rust provider bootstrap 进入 runtime extensions，再由 provider HTTP normalizer 在 catalog/policy 前归一。provider target sticky 真源在 Rust `ForwarderRegistry`，不得在 host/executor 补清理逻辑。
- 动作：先分别补 provider error mapping 红测和 VR no-sticky 红测；错误映射只改 provider runtime 归一化结果，sticky 直接禁用 provider pin 读写。
- 反模式：把 `All available accounts exhausted` 写死进全局 catalog，或在 retry consumer 里靠失败后清 sticky 兜底。
- 验证：`provider-http-executor-utils` + `provider-factory` + `virtual-router-bootstrap-provider-auth-alias` focused Jest，`npm run verify:vr-forwarder-runtime`，`scripts/tests/no-provider-sticky-physical-regression.mjs`。

### L92-28 所有问题先看 codex samples
- 触发：任何 RouteCodex provider、Hub Pipeline、direct/relay、模型名、请求体、metadata、错误链或 live 行为问题。
- 真源：`~/.rcc/codex-samples/` 与 `/Volumes/extension/.rcc/codex-samples/` 下同 requestId / provider / endpoint 的 `provider-request*.json`、`provider-response*.json`、`client-request.json`、`client-response*.json`、`__runtime.json`。
- 动作：先按端口、入口协议、provider/model、时间排序定位样本；从样本确认 direct/relay mode、canonical model、payload、metadata、上游响应/错误，再看 `~/.rcc/diag`、日志、配置和代码。
- 反模式：未看原始 provider/request sample 就判断上游 shape、协议错位、parser 根因、模型名/请求体/路由问题，或用别的 provider/model 样本证明当前 provider 闭环。
- 验证：结论必须引用具体 sample path / requestId / providerKey / 响应 shape；修复后必须回放原样本或同入口新样本，并检查新 codex sample 证明语义正确。

### L92-29 live debug 必须确认 sample 端口
- 触发：本机端口 live probe 带唯一 marker 后，`codex-samples` 命中的是当前 Codex 会话 `port-5520` 样本，而不是目标端口样本。
- 真源：sample path / `__runtime.json` / `provider-request.json` 内的 endpoint、entryPort、matchedPort、providerKey 必须同时指向目标端口与目标 provider。
- 动作：发请求后按目标端口目录优先找样本；若全局 marker 命中其它端口，必须判为噪音，继续查目标端口 server log、snapshot gate、provider executor。
- 反模式：只凭 marker/request id 全局命中就认定拿到当前请求样本；把当前 Codex 对话经 5520 产生的 provider-request 当成被测端口请求。
- 验证：交付结论引用的 sample path 必须包含正确端口目录或文件内正确 entryPort/matchedPort。

### L92-30 startup credential-skip 后必须同步收缩 VR config
- 触发：启动日志已出现 `[provider.init.skip] providerKey=... reason=credential_missing`，但 live 请求仍在 `virtual-router-hit` 命中这个 provider，随后 request 期报 `provider not found for runtimeKey: ...`。
- 真源：`setupRuntime()` / HubPipeline / Rust Virtual Router config；不能只跳过 runtime handle，不同步刷新 VR provider registry。
- 动作：先确认 `startupExcludedProviderKeys` 已记录，再把这些 provider 从 `currentRouterArtifacts.config.providers` 与所有 route tier `targets` 物理删除，并刷新 primary hub pipeline + routing-group pipelines。
- 反模式：在 host/request metadata 注入临时 fallback，或只修 `provider-direct` / runtime resolver，让 stale provider 继续留在 VR 可选面。
- 验证：focused setupRuntime Jest + build + restart + live `virtual-router-hit` 必须从 `provider[backup]` 变为 `provider[default]`，且不再出现 `provider not found for runtimeKey`.

### L92-31 provider errorMapping 必须覆盖 ResponsesProvider direct 路径
- 触发：`provider-http-executor-utils` 的映射测试已绿，但 live `/v1/responses` 仍把上游 `All available accounts exhausted` 暴露成 `HTTP_400`。
- 真源：provider config `errorMapping.rules[]` 属于 provider runtime 共享骨架；`ResponsesProvider` 的 direct SSE/JSON/submit_tool_outputs catch 也必须消费同一个 normalizer。
- 动作：抽唯一共享 helper，再让 `provider-http-executor-utils.ts` 和 `responses-provider.ts` 共同调用；红测至少同时覆盖 generic executor 与 `responses-provider.direct-passthrough`。
- 反模式：只在 generic executor 修，或在 server handler / http-error-mapper 层把 `400` 改写成 `429`。
- 验证：focused `responses-provider.direct-passthrough.spec.ts` + `provider-http-executor-utils.spec.ts` + `provider-failure-policy.spec.ts` + `request-executor-provider-failure-plan.spec.ts` 全绿，`tsc --noEmit` 通过。

### L92-40 registry 投影视图也算活业务语义，不能留在 TS
- 触发：`registry-impl.ts` 看起来只是 list helper，但仍本地 owner 名字 canonicalize/dedupe/sort、auto handler order、`tool_call -> auto` record grouping。
- 真源：这些 projection 也是 hook skeleton/registry contract 的一部分；必须下沉到 `servertool-core::registry_contract`，TS 只回绑 `registration/handler`。
- 动作：新增 `plan_servertool_registry_projection(...)` + NAPI/required export/native wrapper；同时用 audit/verify gate 明确禁止 TS 复活 `new Set(...).sort()`、auto list 拼接、`entry.registration.trigger === ...` 分组逻辑。
- 反模式：把“只是 view helper”当成可留在 TS 的灰区，导致排序/分组/duplicate 语义在 JS 侧继续漂移。
- 验证：`cargo test -p servertool-core registry_contract -- --nocapture`、`cargo test -p router-hotpath-napi plans_servertool_registry_actions_via_servertool_core_bridge -- --nocapture`、focused Jest 4 suites、`node scripts/verify-servertool-rust-only.mjs`。

### L92-41 scope key 是否存在，也必须进入 native pre-command selection 合同
- 触发：`server-side-tools-impl.ts` 虽然已经把 direct/runtime/persisted state 选择交给 Rust，但仍本地 owner `if (!persistentScopeKey) return undefined` 这种“是否允许进入 persisted load”的条件分支。
- 真源：只要会决定是否进入下一阶段，就是活业务语义；应继续沿用 `pre_command_hook_contract` 扩展 `has_persistent_scope_key`，由 native 决定 `use_selected/none` 还是 `load_persisted`。
- 动作：给 `RuntimePreCommandStateSelectionInput` 增加 `has_persistent_scope_key`；bridge/Jest/audit/verify 同步锁住“无 scope key 不得请求 persisted load”。
- 反模式：把“这只是一个空判断”留在 TS，导致 persisted state load 入口条件仍漂在 JS 壳里。
- 验证：`cargo test -p servertool-core pre_command_hook_contract -- --nocapture`、`cargo test -p router-hotpath-napi plans_runtime_pre_command_state_selection_via_servertool_core_bridge -- --nocapture`、focused Jest 2 suites、`node scripts/verify-servertool-rust-only.mjs`。

### L92-42 最外层 entry guard 也是活语义，不能留在 TS 入口壳
- 触发：`server-side-tools-impl.ts` 最前面的 `if (!base)` 和 `if (isAdapterClientDisconnected(...))` 看起来像薄壳早返，但其实仍在决定是否进入 hook/response-stage 主线。
- 真源：这类 entry preflight 必须进入 native 合同；新增 `server_side_tool_entry_contract`，由 Rust 返回 `return_passthrough_non_object_chat | throw_client_disconnected | continue_to_tool_flow`，TS 只执行 action。
- 动作：加 `plan_servertool_entry_preflight(...)` + NAPI/required export/native wrapper；audit/verify 明确禁止 TS 复活上述两个顶层分支。
- 反模式：把“非 object passthrough / disconnected fail-fast”当成纯 IO 壳留在 TS，导致主线入口条件仍漂在 JS。
- 验证：`cargo test -p servertool-core server_side_tool_entry_contract -- --nocapture`、`cargo test -p router-hotpath-napi plans_servertool_entry_preflight_via_servertool_core_bridge -- --nocapture`、focused Jest 3 suites、`node scripts/verify-servertool-rust-only.mjs`。

### L92-43 native 已经选中 CLI projection target 时，TS 不得再按 id 重找一遍
- 触发：`execution_branch_contract` 已判定 `client_exec_cli_projection`，但 `server-side-tools-impl.ts` 仍在 `.find(toolCall.id === projectedToolCallId)`，继续把目标选择语义留在 TS。
- 真源：既然 projection target 已由 Rust owner 判定，native plan 就应同时给出 `projected_tool_call_index`；TS 只按 index 取 `dispatchPlan.executableToolCalls[index]`。
- 动作：扩展 `ServertoolExecutionBranchPlan` / wrapper / bridge test，audit/verify 禁止 `projectedToolCallId` id-lookup 和旧 missing-id fail-fast 文案复活。
- 反模式：把“只是从数组里找一下”当成无害薄壳，导致 CLI projection target 选择仍在 JS 漂移。
- 验证：`cargo test -p servertool-core execution_branch_contract -- --nocapture`、`cargo test -p router-hotpath-napi plans_servertool_execution_branch_via_servertool_core_bridge -- --nocapture`、focused Jest 3 suites、`node scripts/verify-servertool-rust-only.mjs`。

### L92-44 retired backend-route owner 删除后，测试也必须同步物理删除
- 触发：`backend-route-bootstrap-replay-block.ts` 已物理删除，但 `followup-bootstrap-replay.spec.ts` 仍 import 旧文件；另一个 no-reenter 红测未绑定 `MetadataCenter`，继续假设可用裸 adapterContext。
- 真源：stopless/runtime control 唯一入口是 `MetadataCenter`；已退休的 backend-route/reenter owner 不再拥有正向测试面。
- 动作：no-reenter / stopless 红测一律先绑定 `MetadataCenter` 再调用 runtime；旧 backend-route/replay 正向 spec 直接物理删除，不改成 compat wrapper。
- 反模式：为了保测试绿而恢复 deleted file、重建 compat import，或继续让 stopless tests 走 flat metadata / 裸 adapterContext。
- 验证：`tests/servertool/stop-message-auto-no-reenter.red.spec.ts tests/servertool/exec-command-guard.spec.ts tests/servertool/reasoning-only-continue.spec.ts tests/servertool/engine.stopless-session-thin-shell.spec.ts --runInBand`、`node scripts/verify-servertool-rust-only.mjs`。

### L92-45 response-stage support 只允许 Rust 默认语义，不保留 TS capability 推导壳
- 触发：`server-side-tools-impl.ts` 仍用 `runtime-capabilities.ts` 从 `providerInvoker/reenterPipeline/clientInjectDispatch` 推导 gate 输入；`ServerToolHandlerContext.capabilities` 也继续把这三元挂进 handler context。
- 真源：当前 Rust `plan_servertool_response_stage_gate_json()` 只把这三元折叠成 `has_servertool_support`；若 TS 不再需要 legacy runtime executor，就不该继续保留三元细节或 support 布尔 owner。
- 动作：删除 `runtime-capabilities.ts`；response-stage gate 调用不再显式传 `capabilities` 或 `hasServertoolSupport`，直接依赖 native 缺省 false；handler context 也删掉未消费的 `capabilities` 字段。
- 反模式：把三元 callback 是否存在继续当成 TS 侧业务真相，或把 `hasServertoolSupport: false` 这种单布尔继续留在 TS 壳里当 owner marker。
- 验证：`tests/servertool/server-side-tools.failfast.spec.ts tests/servertool/execution-shell.auto-hook-failfast.spec.ts tests/servertool/servertool-auto-hook-trace.spec.ts tests/servertool/servertool-active-orchestration-audit.spec.ts --runInBand`、`node scripts/verify-servertool-rust-only.mjs`、`node scripts/build-core.mjs`。

### L92-46 退休 servertool 路径删除后，必须同步清 map/gate/spec 绑定
- 触发：`server-side-web-search.spec.ts` / `vision-flow.spec.ts` 已删，但 `function-map`、`verification-map`、`verify-servertool-rust-only`、`ci-jest` 仍把它们当 active required coverage；同时旧 spec 继续断言 retired reenter / 旧 stopMessage counter 语义。
- 真源：删除 TS 业务路径不是只删文件；feature owner 的 required tests、verify registry、CI suite 列表、focused spec 口径都必须一起切到当前 Rust 真相。
- 动作：先改 verification 真源，再改 spec。backend-route 当前 active required coverage 用 `servertool-mixed-tools` + `server-side-tools.dispatch-native` + `execution-shell.backend-failfast` + dual-port blackbox；`continue_execution_flow` 命中 `SERVERTOOL_REENTER_RETIRED` 后，相关 logging spec 必须改成 fail-fast 断言，不再把它当“正常但无日志”的旧主线。
- 反模式：保留已删 spec 的 map/gate 绑定制造假红，或继续用旧 spec 名义硬测已经退休的 session counter / reenter 语义。
- 验证：`node scripts/verify-servertool-rust-only.mjs`、`npm run verify:function-map-compile-gate`、focused Jest（`stop-message-auto.goal-default` / `servertool-progress-logging` / `resp-process-stage3-reentry`）全绿。

### L92-47 没有 runtime 消费的 callback type 可以先删
- 触发：`sharedmodule/llmswitch-core/src/servertool/types.ts` 里的 `ProviderInvoker` 只剩类型壳，仓库里已无真实消费。
- 真源：如果一个 callback 只作为历史形状存在、runtime 和 tests 都不再消费它，就不该继续留在 active type surface。
- 动作：先删最明确的死类型入口，再跑 `verify-servertool-rust-only` 和相关 focused tests；若编译面没有真实消费，说明可以继续收口。
- 反模式：把“类型删起来麻烦”误当作保留理由，让 dead shape 长期占据 active owner surface。
- 验证：`rg -n "ProviderInvoker" sharedmodule/llmswitch-core/src tests src` 归零，`node scripts/verify-servertool-rust-only.mjs` 和 focused Jest 通过��
### L92-46 stopless live replay 要先看首轮是否漏 raw reasoningStop，再判续轮寿命
- 触发：2026-06-22 `node scripts/tests/stopless-5555-live-probe.mjs` 第二轮在长链路上报 `Responses conversation expired or not found`，但首轮已经按预期产出 `exec_command(routecodex hook run reasoningStop ...)`。
- 关键判法：live 闭环不能把“续轮会话过期”误判成“response hook 没拦 raw reasoningStop”。先看首轮 `hasExecCommand=true && hasReasoningStop=false && leakedStopSchema=false`；只有当首轮不达标，才去查 `ServertoolRespHook01Intercepted` / schema validator。
- 可复用动作：每次跑 stopless live probe，固定按三步判：
  1) 首轮是否产出标准 `exec_command` CLI 投影；
  2) 是否还有 raw `required_action.reasoningStop` 或可见 schema 泄漏；
  3) 续轮失败时先查 `conversation expired / not found` / provider 4xx，而不是直接回滚 hook 改动。
- 反模式：把 probe 进程的 `exit != 0` 当成骨架回退；或对长链路 stopless 直接断言 `finalStatus=completed`。
- 验证：probe 输出 `attempts[*].hasExecCommand && !attempts[*].hasReasoningStop`；续轮失败需在 note 明确是会话寿命/provider 4xx，不是 hook skeleton。

### L92-54 纯命名别名层也可以折叠成直接导出，前提是测试覆盖真函数名
- 触发：`execution-shell.ts` 里存在 `buildServertoolDispatchPlanInputThinShellShell` / `...OutcomePlan...ShellShell` 这类只为导出命名而存在的中间别名。
- 真源：如果别名不承载任何额外逻辑，且测试已经直接覆盖真函数名，那么可以把导出改成直接指向真函数，去掉中间命名层。
- 动作：把 `ShellShell` 别名移除，导出直接指向 `execution-dispatch-outcome-shell.ts` 的真函数名；focused Jest 和 verify/build 一起回归。
- 反模式：保留双层同义名只是为了“读起来像分层”，实际上没有业务语义。
- 验证：`execution-shell.auto-hook-failfast`、`server-side-tools.failfast`、`execution-dispatch-outcome-shell`、`servertool-active-orchestration-audit`、`servertool-cli-native-bridge`、`server-side-tools.cli-projection-guard`、`verify-servertool-rust-only`、`build-core`。

### L92-53 execution-shell 里的纯转发 wrapper 也要删，auto-hook 真源只留一个
- 触发：`ServertoolAutoHookDescriptor` 和 `runAutoHookExecutionQueue(...)` 只是把调用再转一层到 `auto-hook-caller.ts`，没有独立语义。
- 真源：auto-hook 队列执行只能有一个 owner；如果 execution-shell 只是传参+转发，就应删掉这层 wrapper，避免双壳维护。
- 动作：删 wrapper 和配套类型，测试直接导向 `auto-hook-caller.ts`；verify gate / focused Jest / build 一起回归。
- 反模式：为了保持路径“看起来整齐”而保留一个纯转发 shell，导致 TS surface 继续膨胀。
- 验证：`execution-shell.auto-hook-failfast`、`server-side-tools.cli-projection-guard`、`server-side-tools.failfast`、`servertool-active-orchestration-audit`、`servertool-cli-native-bridge`、`verify-servertool-rust-only`、`build-core`。

### L92-52 identity wrapper 没有真实消费就直接删，不要保留“占位导出”
- 触发：`bindServertoolContractWithNative<T>(value: T): T` 只是 identity 函数，仓库里没有真实 runtime 调用，只有测试 mock 还挂着同名字段。
- 真源：如果一个导出既不承载契约、也不参与 runtime，只是“先留个口子”，那它就是死语义，应物理删除。
- 动作：删实现、删导入、删导出；测试里的 mock 可以暂时保留占位，不必反向证明该导出还活着。
- 反模式：因为 tests mock 里还出现同名字段，就误以为 runtime 也必须保留该导出。
- 验证：`server-side-tools.cli-projection-guard`、`server-side-tools.failfast`、`servertool-active-orchestration-audit`、`servertool-cli-native-bridge`、`execution-shell.auto-hook-failfast`、`verify-servertool-rust-only`、`build-core`。

### L92-51 response-stage wrapper 如果没有真实消费，就别保留纯 cast 壳
- 触发：`bindResponseStageGateNativeShell(...)` 只是 `respStageGateNative(args) as JsonObject` 的单层转发，没有任何额外语义，也没有仓库真实消费。
- 真源：这类 wrapper 的存在价值只有在它承载独立契约或真实入口时才成立；如果只是空转 cast 壳，就可以删掉。
- 动作：删 wrapper 本体和对应 import；verify gate 从“要求 alias 存在”改成“禁止 wrapper 复活”。
- 反模式：把没有任何消费的导出函数当成“为了以后方便”继续挂着，导致 shell surface 一直膨胀。
- 验证：`server-side-tools.cli-projection-guard`、`servertool-active-orchestration-audit`、`servertool-cli-native-bridge`、`execution-shell.auto-hook-failfast`、`verify-servertool-rust-only`、`build-core`。

### L92-50 纯 alias wrapper 可以直接删，前提是 gate 同步改成禁复活而不是要求旧形态
- 触发：`runServertoolAutoHookCallerViaImplThinShell(...)` 只是再包一层 `await runServertoolAutoHookCallerViaThinShell(args)`，没有任何额外语义。
- 真源：如果 wrapper 只是 name alias / await alias，没有行为差异，就应该物理删除；同时 verify gate 必须从“要求旧 wrapper 还在”改成“禁止旧 wrapper 复活”。
- 动作：删掉 alias 函数体，保留直接导出；`verify-servertool-rust-only` 对应条目改成检查直接导出存在、旧 wrapper 名不存在；focused Jest 和 build 重新跑。
- 反模式：为了 gate 绿把旧 wrapper 继续保留，或者删了实现却忘了把 verify gate 一起更新，制造假红。
- 验证：`tests/servertool/server-side-tools.auto-hook-caller-guard.spec.ts`、`tests/servertool/servertool-cli-native-bridge.spec.ts`、`tests/servertool/servertool-active-orchestration-audit.spec.ts`、`tests/servertool/execution-shell.auto-hook-failfast.spec.ts`、`node scripts/verify-servertool-rust-only.mjs`、`node scripts/build-core.mjs`。

### L92-49 废弃兼容字段如果没有新调用面，就应物理删除而不是长期保留
- 触发：`hasServertoolCliProjectionContext` 已不再被 TS 主线需要，只剩 Rust/bridge 的兼容残留。
- 真源：兼容字段只有在还存在真实旧调用面时才有保留价值；一旦主调用面和测试都已转向真实事实输入，就应从 contract / bridge / spec 一起删掉。
- 动作：先把主线调用删掉，再把 Rust struct 和 native wrapper 的兼容字段清空，最后删掉测试中的显式传参���验证只靠 `executionContext.servertoolCliProjection` 仍能推导正确 action。
- 反模式：把“可选字段”误当成“有存在价值的历史记录”长期保留，导致 bridge/type/test 三处一起拖着死语义。
- 验证：`build-native-hotpath`、focused Jest、`verify-servertool-rust-only`、`build-core` 全绿。

### L92-48 engine runtime-action 兼容字段缺省要让 Rust 承接，TS 不再自写硬编码能力位
- 触发：`engine.ts` 还在给 `planServertoolEngineRuntimeActionWithNative(...)` 显式传 `hasServertoolCliProjectionContext: false`，但 Rust 自身已经能从 `execution_context.servertoolCliProjection` 推导。
- 真源：当 Rust contract 已经能自带 fallback 推导时，TS 端的“能力不存在”硬编码就是死语义；主调用面应只传真实事实输入，让 native 兼容默认值。
- 动作：Rust `engine_runtime_action_contract::ServertoolEngineRuntimeActionInput` 给 `has_servertool_cli_projection_context` 加 `#[serde(default)]`；native wrapper 同步把字段改可选；`engine.ts` 不再传该字段；focused spec 同步去掉该字段的硬编码断言。
- 反模式：保留 `hasServertoolCliProjectionContext: false` 当显式签名以“保留阅读线索”，或让 focused spec 继续在调用点显式赋值，反向锁死 TS 仍是该语义的所有者。
- 验证：`node sharedmodule/llmswitch-core/scripts/build-native-hotpath.mjs`、focused Jest（`servertool-cli-native-bridge` / `engine.stopless-session-thin-shell` / `servertool-active-orchestration-audit` / `execution-shell.auto-hook-failfast`）、`node scripts/verify-servertool-rust-only.mjs`、`node scripts/build-core.mjs`。

### L92-47 build:min 是 servertool 闭环必跑入口，不允许用 build 跳过它
- 触发：2026-06-22 这轮收尾时只看到 `build-core` 和黑盒通过，没有再跑 `npm run build:min`，导致 dev/install 链路是否同步无证据。
- 真源：`package.json` 的 `build:min` 才把 `verify:architecture-review-surface-light` + `verify:function-map-compile-gate` + `build:base` + `verify:servertool-rust-only` 串起来；它也是 dev/install 前置的轻量门禁。
- 动作：每次“看上去修好了 servertool 一段”，收尾必须按顺序跑：
  1) `npm run build:min`（确认 servertool Rust-only + 架构 + 编译 gate 全绿）；
  2) `routecodex restart --port 5555/5520` + `/health`；
  3) live probe 或黑盒回归。
- 反模式：用 `node scripts/build-core.mjs` 或单条 `cargo test` 当成 dev/install 闭环；或 build/min 跑挂之后不修就推到 live。
- 验证：`npm run build:min` 退出 0；`/health` 报 `ready=true` 且 version 一致；live probe 首轮仍命中 `exec_command`。

### L92-52 server-side-tools 纯 *Impl alias 出口删除时，verify/mock 必须一起改成禁复活
- 触发：`server-side-tools-impl.ts` 末尾 4 个 `*Impl` 只是指向同文件真薄壳函数的命名 alias，本身没有任何语义 owner。
- 真源：这种 alias 可以直接物理删除，但删除后必须同时改三处：
  1) `server-side-tools.ts` 改为直接重导出真薄壳名；
  2) Jest mock 改成真薄壳 export 名；
  3) `verify-servertool-rust-only` 从“要求旧 alias 存在”切到“真薄壳存在 + 旧 alias 不得复活”。
- 反模式：只删实现、不改 mock；或只改实现和 mock、不改 verify gate，都会制造假红或编译挂。
- 验证：`server-side-tools.cli-projection-guard`、`server-side-tools.auto-hook-caller-guard`、`servertool-active-orchestration-audit`、`verify-servertool-rust-only`、`build-core`。

### L92-53 execution-shell 的纯 alias import/export，优先改成 export-from 直接重导出
- 触发：`execution-shell.ts` 里只是把 `execution-handler-materialization-shell.ts` / `execution-dispatch-outcome-shell.ts` 的真函数名字再包一层本地 alias，再导出去。
- 真源：如果本地没有额外语义，优先用 `export { ... } from` 直接重导出，减少一层 import/alias surface。
- 动作：把本地 alias import/export 收成 direct re-export；gate 只锁真函数名存在，不锁具体是 `export const` 还是 `export-from`。
- 反模式：为了视觉统一保留无意义的本地 alias import/export，导致壳层仍然胖。
- 验证：`execution-shell.auto-hook-failfast`、`execution-dispatch-outcome-shell`、`server-side-tools.failfast`、`verify-servertool-rust-only`、`build-core`。

### L92-54 纯 alias 可以通过 direct re-export 收口；但真正 active owner 函数不能靠改名混成 thin shell
- 触发：`execution-dispatch-outcome-shell.ts` 末尾 `ThinShell` alias 删除后，`execution-shell.ts` 用 direct re-export 继续对外兼容。
- 真源：如果下层文件只是 alias，本体可以删；如果是实逻辑 owner（例如 `materializeNativeToolCallExecutionOutcome` / `runServertoolIoExecutionQueue`），不能靠改名让 audit 通过。
- 动作：删 alias，但把 remaining 真实 owner 留在 gate 中；下一步要么继续下沉子逻辑，要么把 audit 拆成更精确的 pending-owner 清单，不能假装已 closeout。
- 反模式：把 active owner 函数当纯壳删，或把 gate 改成忽略真实 owner 以求绿。
- 验证：`execution-dispatch-outcome-shell.spec.ts`、`server-side-tools.failfast.spec.ts`、`servertool-auto-hook-trace.spec.ts`、`execution-shell.auto-hook-failfast.spec.ts`、`build-core`。

### L92-55 server-side-tools 顶层转发壳可以继续收成 direct re-export
- 触发：`server-side-tools.ts` 先前还保留本地 `export const` 转发层，实际上只是重导出 `server-side-tools-impl.ts` 和 `auto-hook-caller.ts` 的真符号。
- 真源：如果只是导出聚合面，没有额外语义，本地 `export const` 可以换成 direct re-export；这比保留一层本地命名壳更接近物理收口。
- 动作：把 `server-side-tools.ts` 改成 direct re-export，并把 verify/mock 也同步到 direct re-export 形态。
- 反模式：把“对外兼容名”误判成“必须保留的本地 owner”，导致 shell surface 迟迟不收。
- 验证：`server-side-tools.auto-hook-caller-guard`、`server-side-tools.cli-projection-guard`、`verify-servertool-rust-only`、`build-core`。

### L92-56 required response hook empty 必须由 Rust contract 决策，TS/集成层只消费
- 触发：`server-side-tools-impl.ts` 的 response-stage 仍在 TS 里分别判 `run_auto_hooks` / `return_auto_hook_result` / required-empty，容易把“是否需要报错”留在 TS 分支里。
- 真源：`response_stage_runtime_action_contract.rs` 新增 `response_hook_required` 输入和 `return_required_response_hook_empty` 动作后，required-empty 这一类决策必须由 Rust contract 一次性产出；TS 只负责执行 auto-hook、消费 action、必要时抛 native error plan。
- 动作：如果集成 blackbox 受 mock 影响不稳定，优先用 Rust contract + native bridge 锁 action，再让 TS 黑盒只验证“消费该 action 的结果”。
- 反模式：把 required-empty 继续留在 TS 二段 if 里，或者拿单一集成 mock 失败就否定 Rust contract 已下沉的事实。
- 验证：`response_stage_runtime_action_contract` Rust test、`servertool-cli-native-bridge`、`server-side-tools.failfast`、`verify-servertool-rust-only`、`build-core`。

### L92-57 direct/provider-direct stopless guard 也属于 preflight 真源，不要留在 TS helper
- 触发：`engine.ts` 之前仍通过 `direct-stopless-route-guard.ts` 读取 `routeName` 前缀来判 `router-direct` / `provider-direct`，这会把 stopless preflight 的一部分留在 TS。
- 真源：`engine_preflight_contract.rs` 应同时负责 synthetic-control early return 和 direct-route stopless bypass；`adapterContext.routeName / metadata.routeName / __rt.routeName` 的读取也要并进 Rust preflight，而不是先在 TS 算布尔值再传进去。
- 动作：把 `stoplessDisabledOnDirectRoute` 改成 optional override，并支持 `adapterContext` 直传；`engine.ts` 只把 `adapterContext` 交给 native preflight，随后物理删除 `direct-stopless-route-guard.ts`。
- 反模式：保留一个“很小所以无所谓”的 TS guard helper；这种 helper 会长期留下第二真源。
- 验证：`cargo test -p servertool-core engine_preflight_contract -- --nocapture`、`build-native-hotpath`、`servertool-cli-native-bridge`、`engine.stopless-session-thin-shell`、`verify-servertool-rust-only`、`build-core`。

### L92-59 纯 alias wrapper 要连 gate 一起反向锁死，先删壳再改验证
- 触发：`server-side-tools.ts` 里的 `runServertoolAutoHookCallerViaThinShell as runServertoolAutoHookCaller` 只是对真实薄壳的纯命名别名，没有独立 owner。
- 真源：这类 alias 应直接物理删除；对应 gate 也要从“要求 alias 存在”切成“alias 不得复活”，否则会把死名继续养成第二真源。
- 动作：删 alias 后，同步改 `verify-servertool-rust-only.mjs` 的检查语义，再跑 guard spec + verify gate。
- 反模式：只删代码不改 gate，或只改 gate 不删代码，都会留下假绿/假红和重复设计。
- 验证：`server-side-tools.auto-hook-caller-guard`、`verify-servertool-rust-only`。

### L92-60 纯记录包装函数也可以物理删掉，直接内联到调用点
- 触发：`engine.ts` 里的 `recordServertoolExecutionSnapshot(...)` 只是把 `stageRecorder.record(...)` 包了一层，没有独立 owner，也不改变决策。
- 真源：这类纯记录包装可以直接删掉，调用点内联即可；只要行为不变，就不要保留额外 helper 壳。
- 动作：删 helper，保留内联记录；再用 engine focused spec + verify gate + build-core 确认没有副作用。
- 反模式：把纯记录包装也当成“为了可读性必须保留”的长期壳，继续堆 TS 表面层。
- 验证：`engine.stopless-session-thin-shell`、`servertool-active-orchestration-audit`、`verify-servertool-rust-only`、`build-core`。
### L92-61 纯包装函数删除后，gate 必须立刻改成禁止复活
- 触发：`execution-dispatch-outcome-shell.ts` 里的 `applyServertoolExecutionResult(...)` 只是 `replaceJsonObjectInPlace(...)` 的单层包装，没有独立 owner。
- 真源：这类 helper 可以物理删除，但删除后 gate 不能继续要求旧符号存在，必须改成 `assertMissing` 或等价反向锁，避免假绿和二次复活。
- 动作：删 helper 后同步改 `verify-servertool-rust-only.mjs`，再跑 focused Jest、`build-core`、`verify-servertool-rust-only`。
- 反模式：只删代码不改 gate，或者只改 gate 不删代码，都会留下第二真源。
- 验证：`tests/servertool/execution-dispatch-outcome-shell.spec.ts`、`tests/servertool/execution-shell.backend-failfast.spec.ts`、`tests/servertool/servertool-active-orchestration-audit.spec.ts`、`verify-servertool-rust-only`、`build-core`。
### L92-62 re-export alias 也要收口到真实 owner 文件，避免 execution-shell 继续变胖
- 触发：`execution-shell.ts` 里的 `resolveToolCallExecutionOutcomeThinShell` / `runToolCallExecutionLoopThinShell` 只是对 `execution-dispatch-outcome-shell.ts` 的纯 alias re-export；`server-side-tools-impl.ts` 通过这层间接连接真实 owner。
- 真源：如果真实 owner 已经是 `execution-dispatch-outcome-shell.ts`，调用点应直接连 owner 文件；`execution-shell.ts` 只保留自己真正拥有的 pre-command hook / handler materialization 语义。
- 动作：把 `server-side-tools-impl.ts` 改成直接 import `materializeNativeToolCallExecutionOutcome` 和 `runServertoolIoExecutionQueue`；同步更新 failfast mock、dispatch-native spec，并把 `servertool-active-orchestration-audit` + `verify-servertool-rust-only` 改成禁止这些 alias 回到 `execution-shell.ts`。
- 反模式：把“只是导出聚合层”的 alias 留在中间文件，导致 TS surface 看起来更大、owner 更模糊。
- 验证：`tests/servertool/server-side-tools.failfast.spec.ts`、`tests/servertool/server-side-tools.dispatch-native.spec.ts`、`tests/servertool/servertool-active-orchestration-audit.spec.ts`、`verify-servertool-rust-only`、`build-core`。
### L92-63 导出名壳如果不承载语义，也要从 impl 文件本体删掉
- 触发：`server-side-tools-impl.ts` 里的 `runServerSideToolEngineViaThinShell`、`collectAdditionalClientToolCallsViaImplThinShell`、`extractToolCallsViaImplThinShell` 只是把真实对外名多包了一层命名壳；`server-side-tools.ts` 还要再 alias 一次。
- 真源：如果函数本体就是实际 owner，但 `ViaThinShell` / `ViaImplThinShell` 这层名字不承载额外契约，应该直接把 impl 文件导出名改成最终对外名，再让聚合层 direct re-export。
- 动作：把 impl 文件改成直接导出 `runServerSideToolEngine`、`collectAdditionalClientToolCalls`、`extractToolCalls`；同步更新 `server-side-tools.ts`、focused mock 和 `verify-servertool-rust-only` 的 marker。
- 反模式：为了让文件名和导出名“看起来像薄壳”而长期保留双重 alias，这会把 TS 表面层越养越厚。
- 验证：`tests/servertool/server-side-tools.cli-projection-guard.spec.ts`、`tests/servertool/server-side-tools.failfast.spec.ts`、`tests/servertool/servertool-active-orchestration-audit.spec.ts`、`verify-servertool-rust-only`、`build-core`。

### L92-64 5520 thinking 1:1 先看真源，再看 builder 展开，再看测试断言
- 触发：用户要求把 `glm-5.2` 加入 `thinking` 做 1:1 load balancing 时，容易误判成“还要改配置”，但真源可能已经在 `~/.rcc/config.toml`。
- 真源：先核对 `~/.rcc/config.toml`，再核对 `src/config/virtual-router-builder.ts` 对 forwarder 的展开，最后核对 `tests/config/virtual-router-builder.forwarder-10000.spec.ts` 是否按 builder 真相断言。
- 动作：如果三层口径一致，就不要做重复修改；直接记录为已满足，并补 focused Jest 证明。
- 反模式：只看仓库配置或只看测试，不核对 runtime 真源；或者把 `fwd.glm.glm-5.2` 误当成单个 providerKey。
- 验证：`tests/config/virtual-router-builder.forwarder-10000.spec.ts`、`routecodex config validate`、`~/.rcc/config.toml` 现场核对。

### L92-65 direct-stopless preflight 必须并进 Rust engine preflight，TS guard 文件必须物理删除
- 触发：`servertool/engine.ts` 仍通过 `direct-stopless-route-guard.ts` 读取 `routeName` 前缀来判 `router-direct` / `provider-direct`，这会让 preflight 的一部分继续留在 TS。
- 真源：`engine_preflight_contract.rs` 必须同时负责 synthetic-control early return 和 direct-route stopless bypass；`adapterContext.routeName / metadata.routeName / __rt.routeName` 的读取也要并进 Rust preflight，而不是先在 TS 算布尔值再传进去。
- 动作：把 `stoplessDisabledOnDirectRoute` 改成 optional override，并支持 `adapterContext` 直传；`engine.ts` 只把 `adapterContext` 交给 native preflight，随后物理删除 `direct-stopless-route-guard.ts`。
- 反模式：保留一个“很小所以无所谓”的 TS guard helper；这种 helper 会长期留下第二真源。
- 验证：`engine.stopless-session-thin-shell`、`servertool-cli-native-bridge`、`servertool-active-orchestration-audit`、`verify-servertool-rust-only`、`build-core`。

### L92-66 required response hook empty 必须从 Rust contract 投影，不回到 TS throw
- 触发：`server-side-tools-impl.ts` 的 response-stage 仍可能在 TS 里手工拼“required response hook empty”错误。
- 真源：`orchestration_policy_contract.rs` 新增 `plan_servertool_required_response_hook_empty_error(...)` 后，TS 必须新增一个 native wrapper，例如 `createServertoolRequiredResponseHookEmptyError(...)`，并只通过 `timeout-error-block.ts` 暴露。
- 动作：先在 Rust 单元里固化 reason_code/message；TS 调用点只传 `requestId + responseHookName`；不要在 impl 文件里重新 throw 字符串。
- 反模式：把 required-empty 错误保留在 TS 里当第二真源，或者用 TS 通用错误代替专用 plan。
- 验证：`timeout-error-block.ts` 单元 spec、`servertool-cli-native-bridge`、`server-side-tools.failfast`、`verify-servertool-rust-only`、`build-core`。

### L92-67 单次 metadata write wrapper 可以直接内联删除，再把 helper 名写进 gate 禁复活
- 触发：`response-stage-orchestration-shell.ts` 里的 `markServertoolResponseOrchestration(...)` 只是单次 `writeRuntimeControlToBoundMetadataCenter(...)` 包装，没有独立 owner。
- 真源：这种 helper 可以直接删掉，把写入调用内联到主流程；但删完必须立刻把 helper 名加入 gate 反向锁，防止旧壳复活。
- 动作：用内联替代 wrapper，保留实际 metadata write 行为和 writer 标记；验证时继续看 response-stage 黑盒和 `verify-servertool-rust-only`。
- 反模式：把纯记录/纯 metadata write 包装当成长期公共 API 壳保留，继续堆 TS 表面层。
- 验证：`resp-process-stage3-reentry`、`stop-message-flow-followup-reentry`、`servertool-active-orchestration-audit`、`verify-servertool-rust-only`、`build-core`。
---
### L92-61 servertool applyServertoolExecutionResult wrapper physical delete

- 触发：`sharedmodule/llmswitch-core/src/servertool/execution-dispatch-outcome-shell.ts` 的 `applyServertoolExecutionResult(baseForExecution, nextChatResponse)` 仅是 `replaceJsonObjectInPlace(baseForExecution, nextChatResponse)` 的单层包装，没有独立 owner；`execution-shell.ts` 还在 re-export 这个 alias。
- 动作：物理删除 `applyServertoolExecutionResult` 函数，调用点 3 处全部内联为 `replaceJsonObjectInPlace(...)`，并从 `execution-shell.ts` 的 re-export 列表里删除 `applyServertoolExecutionResult`。
- gate 反向锁：`scripts/verify-servertool-rust-only.mjs` 删除 `servertool-execution-state-ts-thin-shell` 中那条“必须包含旧包装”的旧断言，替换为 `assertMissing('export function applyServertoolExecutionResult(')`，禁止复活。
- 验证：
  - `PATH=/opt/homebrew/opt/node@22/bin:$PATH node --experimental-vm-modules ./node_modules/jest/bin/jest.js tests/servertool/execution-dispatch-outcome-shell.spec.ts tests/servertool/execution-shell.backend-failfast.spec.ts tests/servertool/servertool-active-orchestration-audit.spec.ts --runInBand`
  - `node scripts/build-core.mjs`
  - `PATH=/opt/homebrew/opt/node@22/bin:$PATH node scripts/verify-servertool-rust-only.mjs`
- 反模式：不要把“已删 alias/wrapper”留给下一次 PR 处理；删除瞬间必须把 gate 改成“禁止复活”，避免第二真源悄悄回来。

### L92-68 删 thin shell 后 canonical-builder anchor 预算要从功能 map 上重新平衡

- 触发：`hub.servertool_flow_presentation` 删除 `flow-presentation-block.ts` 后，只剩 `servertool_skeleton_config.rs` 命中 `resolve_servertool_progress_tool_name_json` / `should_use_servertool_gold_progress_highlight_json`，`verify-architecture-feature-anchor-coverage` 因“至少 2 个 builder 命中文件”报红。
- 真源：thin shell 物理删除后，这个 feature 的 canonical builder 本来就应只由 Rust owner 文件承载；TS native wrapper 不应塞 fake anchor。
- 动作：把 `docs/architecture/function-map.yml` 该 feature 的 `owner_scope` 明确标为 `file-scoped Rust owner for ...`，同步 `docs/architecture/wiki/servertool-ownership-map.md`，并让 `scripts/verify-servertool-rust-only.mjs` 反向锁死 `flow-presentation-block.ts` 不得复活。
- 反模式：为了凑 2 个 anchor，在 native wrapper 文件里硬塞 random `feature_id:` 注释或 fake builder 字符串；那样会出现“gate 绿，但 owner 不真”的假闭环。
- 验证：`npm run verify:function-map-compile-gate`、`node scripts/verify-servertool-rust-only.mjs`、`node scripts/build-core.mjs`、focused Jest。
