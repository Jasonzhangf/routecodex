# Provider Core Config Module

Provider core config contains service profiles and debug hooks for Provider V2. OAuth flow config has been removed.

## Key Files

- `service-profiles.ts`: service profile definitions.
- `provider-hooks.ts`: provider lifecycle hooks and debug hooks.
