/**
 * Re-export barrel — canonical implementations live in oauth-lifecycle/error-detection.
 */
export {
  extractStatusCode,
  isGoogleAccountVerificationRequiredMessage,
  extractGoogleAccountVerificationUrl,
} from '../oauth-lifecycle/error-detection.js';
