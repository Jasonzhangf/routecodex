# Provider Auth Module

Provider auth only supports API key authentication. OAuth, token-file auth, and browser-login auth flows have been removed.

## Key File

- `apikey-auth.ts`: API key authentication implementation.
