/**
 * Default pipeline configuration
 */

import type { PipelineManagerConfig } from '../interfaces/pipeline-interfaces.js';

/**
 * Default pipeline configuration
 */
export const defaultPipelineConfig: PipelineManagerConfig = {
  pipelines: [],
  settings: {
    defaultTimeout: 30000,
    maxRetries: 3,
    debugLevel: 'detailed'
  }
};