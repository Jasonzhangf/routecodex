#!/bin/sh
set -eu
branch=$(git symbolic-ref --short HEAD 2>/dev/null || true)
case "$branch" in
  main|master) echo "commits to protected branch $branch are forbidden" >&2; exit 1 ;;
esac
exec npm run verify:local --silent
