#!/bin/sh
set -eu
while read -r local_ref local_sha remote_ref remote_sha; do
  case "$remote_ref" in
    refs/heads/main|refs/heads/master) echo "pushes to protected ref $remote_ref are forbidden" >&2; exit 1 ;;
  esac
done
exec npm run verify:ci --silent
