# RouteCodex V4 Cordis Plugins

This directory is reserved for thin Cordis plugin factories. Plugin identity,
ordering, resource permissions, and execution handles remain owned by the
compiled Rust `NodePluginPlan`; this package must not become a second registry.
