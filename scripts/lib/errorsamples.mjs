import { writeErrorsampleJson } from '../../src/debug/errorsamples.js';

export async function writeErrorSampleJson({ group, kind, payload }) {
  return writeErrorsampleJson({ group, kind, payload });
}
